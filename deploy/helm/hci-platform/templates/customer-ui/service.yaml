apiVersion: v1
kind: Service
metadata:
  name: customer-ui
  namespace: {{ include "hci.namespace" . }}
  labels:
    {{- include "hci.podLabels" (dict "name" "customer-ui" "Release" .Release "Chart" .Chart) | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.customerUI.port }}
      targetPort: {{ .Values.customerUI.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "hci.selectorLabels" (dict "name" "customer-ui" "Release" .Release) | nindent 4 }}
