{{- if .Values.diagnosisService.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: diagnosis-service
  namespace: {{ include "hci.namespace" . }}
  labels:
    {{- include "hci.podLabels" (dict "name" "diagnosis-service" "Release" .Release "Chart" .Chart) | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.diagnosisService.port }}
      targetPort: {{ .Values.diagnosisService.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "hci.selectorLabels" (dict "name" "diagnosis-service" "Release" .Release) | nindent 4 }}
{{- end }}
