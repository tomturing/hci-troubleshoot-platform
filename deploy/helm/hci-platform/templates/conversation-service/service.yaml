apiVersion: v1
kind: Service
metadata:
  name: conversation-service
  namespace: {{ include "hci.namespace" . }}
  labels:
    {{- include "hci.podLabels" (dict "name" "conversation-service" "Release" .Release "Chart" .Chart) | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.conversationService.port }}
      targetPort: {{ .Values.conversationService.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "hci.selectorLabels" (dict "name" "conversation-service" "Release" .Release) | nindent 4 }}
