apiVersion: v1
kind: Service
metadata:
  name: scheduler-service
  namespace: {{ include "hci.namespace" . }}
  labels:
    {{- include "hci.podLabels" (dict "name" "scheduler-service" "Release" .Release "Chart" .Chart) | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.schedulerService.port }}
      targetPort: {{ .Values.schedulerService.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "hci.selectorLabels" (dict "name" "scheduler-service" "Release" .Release) | nindent 4 }}
