{{- if and .Values.global.domain (ne .Values.global.domain "example.com") (eq .Values.secrets.postgresPassword "dev_password_123") -}}
  {{- fail "❌ 检测到生产域名，但 secrets.postgresPassword 仍为默认弱密码 'dev_password_123'，请在 override 文件中设置强密码。" -}}
{{- end -}}
apiVersion: v1
kind: Secret
metadata:
  name: hci-secrets
  namespace: {{ include "hci.namespace" . }}
  labels:
    {{- include "hci.labels" . | nindent 4 }}
type: Opaque
stringData:
  POSTGRES_PASSWORD: {{ .Values.secrets.postgresPassword | quote }}
  OPENCLAW_GATEWAY_TOKEN: {{ .Values.secrets.openclawToken | quote }}
  ZAI_API_KEY: {{ .Values.secrets.zaiApiKey | quote }}
  GF_SECURITY_ADMIN_PASSWORD: {{ .Values.secrets.grafanaAdminPassword | quote }}
  # 内部 API 鉴权（LearningClaw / ProductionClaw 调用内部服务）
  INTERNAL_API_TOKEN: {{ .Values.secrets.internalApiToken | quote }}
