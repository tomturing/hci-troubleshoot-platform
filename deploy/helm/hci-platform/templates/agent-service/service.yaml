apiVersion: v1
kind: Service
metadata:
  name: agent-service
  namespace: {{ include "hci.namespace" . }}
  labels:
    {{- include "hci.podLabels" (dict "name" "agent-service" "Release" .Release "Chart" .Chart) | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.agentService.port }}
      targetPort: {{ .Values.agentService.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "hci.selectorLabels" (dict "name" "agent-service" "Release" .Release) | nindent 4 }}
