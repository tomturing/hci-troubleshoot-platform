apiVersion: v1
kind: Service
metadata:
  name: admin-ui
  namespace: {{ include "hci.namespace" . }}
  labels:
    {{- include "hci.podLabels" (dict "name" "admin-ui" "Release" .Release "Chart" .Chart) | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.adminUI.port }}
      targetPort: {{ .Values.adminUI.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "hci.selectorLabels" (dict "name" "admin-ui" "Release" .Release) | nindent 4 }}
