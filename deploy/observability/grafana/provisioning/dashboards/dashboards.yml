apiVersion: 1

providers:
  - name: HCI Platform
    orgId: 1
    folder: HCI
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
