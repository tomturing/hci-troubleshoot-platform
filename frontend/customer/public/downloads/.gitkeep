# 此目录用于托管 terminal_bridge.exe 静态下载文件
#
# 使用方法：
#   将编译好的 terminal_bridge.exe 放入本目录，
#   然后执行 bash scripts/k3s-release.sh --services customerUI
#
# Vite 构建时会将 public/ 目录原样复制到 dist/，
# Nginx 容器启动后可通过 /downloads/terminal_bridge.exe 访问。
#
# 注意：exe 文件不应提交到 Git（已在 .gitignore 排除），
#       应通过 CI/CD 或手动方式在构建前注入。
