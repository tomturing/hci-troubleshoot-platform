# HCI 智能排障平台 - 项目文件清单

## 文档版本: 2.0 | 更新日期: 2026-02-25

本文档列出了最新的项目文件脉络。系统的后端基座、所有可观测性基建堆栈以及中文文档体系已全部完成闭环设计和实装。

---

## 目录结构拓扑图

```text
hci-troubleshoot-platform/
├── README.md                                   # 项目总览及使用大纲
├── .gitignore                                  
├── .env / .env.example                         # 环境变量模版与全局密钥管理
├── docker-compose.yml                          # 核心业务组件容器编排 (API Gateway, Case, Conversation, Scheduler, DB)
├── test_manual.sh                              # 全流 E2E 测试脚本工具
│
├── docs/                                       # 核心文档集 (100% 中文化)
│   ├── 00_需求说明.md                          # 需求规格说明书
│   ├── 01_架构设计.md                          # 全局架构与拓扑设计
│   ├── 02_数据库设计.md                        # ER 图、表结构说明
│   ├── 03_接口设计.md                          # REST & WebSocket API 文档
│   ├── 04_可观测性设计.md                    # OTel + Loki/Tempo/Grafana 部署与双向追踪
│   ├── 05_K8s设计.md                          # K3s 部署架构与 Helm Chart 设计
│   ├── 06_开发指南.md                          # 本地环境搭建及依赖配置指南
│   ├── 07_测试指南.md                          # 测试用例、E2E 流程说明
│   ├── 08_文件清单.md                          # ✅ (当前文件) 最新文件层级梳理映射
│   └── 09_项目进展.md                          # 迭代状态追踪、完成度分析与下一步计划
│
├── backend/                                    # 后端 Python 微服务矩阵
│   ├── shared/                                 # 级联共享通用包
│   │   ├── database/                           # PG 与 Redis 底层 Session 连接生命维持模块
│   │   ├── models/                             # Pydantic 类型约束规范定义集
│   │   └── utils/                              # JSON Logger, TraceID 生成与 OTel (otel.py) 初始化组件
│   │
│   ├── api-gateway/                            # 流量中枢：API 网关服务 (Http: 8000)
│   │   ├── app/
│   │   │   ├── main.py                         # 挂载 FastAPI 中间件与 OpenTelemetry
│   │   │   ├── config.py                       
│   │   │   ├── routes/                         # 对外部暴露向内部转发代理的各类端点控制器
│   │   │   └── services/                       # 下层依赖的 HTTPX 通信客户端交互封装
│   │   ├── Dockerfile
│   │   └── requirements.txt
│   │
│   ├── case-service/                           # 业务分支：工单管理微服务
│   │   ├── app/
│   │   │   ├── main.py                         
│   │   │   ├── config.py                       
│   │   │   ├── routes/                         # Case CRUD 与状态机控制访问
│   │   │   ├── services/                       # 解耦业务隔离封装层
│   │   │   └── repositories/                   # 数据持久化底层封装
│   │   ├── Dockerfile
│   │   └── requirements.txt
│   │
│   ├── conversation-service/                   # 业务分支：对话链路微服务
│   │   ├── app/
│   │   │   ├── main.py
│   │   │   ├── routes/                         # 下游接受发问输入并输出 Streaming 响应片
│   │   │   ├── services/                       # 发起外联大模型网络并托管交互生命周期
│   │   │   └── repositories/                   # 会话记录沉淀器
│   │   ├── Dockerfile
│   │   └── requirements.txt
│   │
│   └── scheduler-service/                      # 业务分支：环境调度器微服务
│       ├── app/
│       │   ├── main.py
│       │   ├── routes/                         # 热备池调度分配访问端
│       │   ├── services/                       # 触发 Kubernetes API 的逻辑载体
│       │   └── models/                         
│       ├── Dockerfile
│       └── requirements.txt
│
├── deploy/                                     # 部署清单与加强组件
│   ├── docker/
│   │   └── docker-compose.yml              # 业务栈编排（含 OpenClaw、各微服务、PG、Redis）
│   └── observability/                          # 系统可观测性堆栈套件
│       ├── config/
│       │   ├── promtail-config.yaml            # Docker Stdout 抓取器配置策略
│       │   └── tempo-config.yaml               # Tempo Trace 聚合接收器启动文件
│       ├── grafana/
│       │   └── provisioning/
│       │       └── datasources/
│       │           └── datasources.yml     # Loki + Tempo 数据源自动配置
│       └── docker-compose-obs.yml              # 可观测数据站一键拉起编排
│
├── frontend/                                   # 前端平台 (待投入实现)
│   └── ...                                     
│
└── tests/                                      # 涵盖各个组件与集成的测例目录
```

---

## 阶段总结梳理

本项目上述的 `docs` 组件体系、`backend` 四大微服务骨架结构及其内部关联交互代码已 **彻底实现 100% 结构化设计和构建落地**并支持全生命周期调测。当前代码树不存在关键代码死角缺口，拥有着极其完备和统一的依赖、配置格式。

新进开发可根据此树状目录迅速理清服务边界并发起更上层的增量建设。
