# HCI智能排障平台 - 项目进展

更新时间: 2026-03-10（迭代 2026-03-10-2）

---

## 一、任务概述

| # | 任务 | 状态 | 说明 |
|---|------|------|------|
| 1 | 需求分析与架构设计 | ✅ 已完成 | 需求规格、架构设计、数据库设计、接口设计、可观测性设计 |
| 2 | 后端微服务基座搭建 | ✅ 已完成 | API Gateway + Case / Conversation / Scheduler 四服务骨架 |
| 3 | 核心业务功能联调 | ✅ 已完成 | 工单CRUD、对话SSE流式、Pod调度模拟、全链路E2E验证 |
| 4 | 可观测性基建 | ✅ 已完成 | OTel链路追踪 + Loki日志 + Tempo + Grafana 双向钻取 |
| 5 | OpenClaw容器化接入 | ✅ 已完成 | Docker部署、z.ai模型对接、SSE代理修复、健康检查适配 |
| 6 | 代码质量治理 (三批次修复) | ✅ 已完成 | trace_id统一OTel标准、崩溃修复、配置清理、代码质量改进 |
| 7 | 前端 Vue 3 UI | ✅ 已完成 | Customer对话式UI + Admin管理控制台 |
| 8 | Web端集成测试与UX优化 | ✅ 已完成 | API路径修复、关闭工单UX、历史工单查看、状态生命周期文档 |
| 9 | K8s 部署设计 | ✅ 已完成 | K8s设计文档 + Helm Chart 骨架 (22资源) + 配置统一 |
| 10 | **架构 v2.0 升级设计** | ✅ 已完成 | OpenClaw→AI Assistant Pool 多助手架构 + 知识库RAG设计 |
| 11 | 架构 v2.0 代码实现 | ✅ 已完成 | 多助手支持代码落地（详见§2.10） |
| 12 | **K3s Helm 生产部署** | ✅ 已完成 | Helm Charts 落地部署到 K3s（详见§2.12） |
| 13 | K3s 部署后网络修复 | ✅ 已完成 | 修复 socat/权限/前端安全限制/Fake-IP 代理导致的网络劫持问题 |
| 14 | 知识库数据摂入 | ✅ 已完成 | 37 篇 SOP 全部入库（200+ chunks），涵盖 5 大故障类（详见§2.18） |
| 15 | **Phase 1+2 重构优化** | ✅ 已完成 | 5项Bug修复 + 5项架构加固（详见§2.13 及 10_重构优化.md） |
| 16 | **RAG 知识库完整设计** | ✅ 设计完成 | 数据源、ETL、检索引擎、KB Service 接口全局设计（详见 13_RAG设计.md） |
| 17 | **AI 层 v3.0: LearningClaw+ProductionClaw** | ✅ 已完成 | 一 Pod 一 Case 架构 + 知识生产/消费分离（详见§2.14 及 12_AI层设计.md） |
| 18 | **文档整理合并** | ✅ 已完成 | 12→合并11，14→合并13，RAG文档重命名缓号，01/08/09同步更新 |
| 19 | **KB Service v1.0（RAG 核心）** | ✅ 已完成 | DB v3.0 + KB 微服务 + api-gateway 代理 + 测试（详见§3.1~3.3） |
| 20 | **Conversation 4-Tier Prompt + KB 注入** | ✅ 已完成 | kb_client + conversation_service v2.1 + routes 更新（详见§3.4） |
| 21 | **Case 关闭知识推送** | ✅ 已完成 | case_service 关闭时 fire-and-forget 推送摘要至 KB（详见§3.5） |
| 22 | **data-pipeline SOP 测试数据** | ✅ 已完成 | 5 大故障 SOP 文档 + jieba 词典 + keywords_map（详见§3.6） |
| 23 | **Phase 3 重构优化** | ✅ 已完成 | 健康检查增强 + case_info 注入 + Prometheus 指标 + Redis 集成测试（详见§2.17 及 10_重构优化.md §6） |
| 24 | **阶段一 RAG 验收** | ✅ 已完成 | 外挂 RAG 端到端验证通过：4-Tier Prompt 注入 + SOP 命中 + Loki 可见（详见§7.6） |
| 25 | **ETL 产品案例数据建设** | ✅ 已完成 | 6脚本 ETL 数据管道开发完成（详见§8）；待有数据源时执行全量摄入 |
| 26 | **LearningClaw 闭环 E2E 验证** | ✅ 已完成 | 关闭工单→kb_push_success→KB文档+1(doc_id=57)→chunk已创建→RAG可检索（详见§四 P1-1 & §2.22） |
| 27 | **ProductionClaw 调度链路验证** | ✅ 已完成 | Scheduler正确分配openclaw pod → pod_allocated事件确认（详见§四 P1-2 & §2.23） |
| 28 | **Prometheus 进 K3s** | ✅ 已完成 | Prometheus Helm 模板 + RBAC + 5服务 /metrics 端点 + Grafana 数据源（详见§8） |
| 29 | **Phase 4 测试覆盖 + CI/CD** | ✅ 已完成 | E2E 测试 + GitHub Actions + WebSocket(10) + CORS(11) + Locust 基线（详见§九） |

---

## 二、已完成任务详情

### 2.1 需求分析与架构设计

完成全套中文化设计文档体系，涵盖：
- 需求规格说明书（用户故事、MVP功能范围、技术约束）
- 系统架构设计（用户层→网关层→服务层→AI层→数据层→可观测性层）
- 数据库设计（6张核心表、ER图、索引策略、迁移方案）
- 接口设计（REST API + WebSocket 协议、统一响应格式、错误码表）
- 可观测性设计（三大支柱架构、Trace↔Log 双向钻取机制）

### 2.2 后端微服务基座搭建

四个微服务全部完成基座构建，包括项目结构、Dockerfile、依赖管理、OTel自动仪表化：

| 服务 | 端口 | 职责 |
|------|------|------|
| API Gateway | 8000 | 流量入口、路由代理、WebSocket、Redis会话管理 |
| Case Service | 8001 | 工单全生命周期CRUD、状态机 |
| Conversation Service | 8002 | 对话管理、SSE流式AI交互、消息持久化 |
| Scheduler Service | 8003 | Pod热备池调度、K8s API交互(Mock) |

### 2.3 核心业务功能联调

- **工单流程**: 创建→查询→确认→关闭，全生命周期 CRUD 验证通过
- **对话流程**: 创建对话→发送消息→SSE流式AI响应→后台异步保存→历史查询
- **跨服务协同**: API Gateway → Case/Conversation Service 代理链路打通
- **E2E验证**: `scripts/docker-e2e-test.sh` 脚本完整覆盖工单创建→对话绑定→SSE打字机回传→消息历史查询

### 2.4 可观测性基建

- **链路追踪**: OTel SDK 自动仪表化（FastAPI / HTTPX / SQLAlchemy），W3C traceparent 跨服务传播
- **结构化日志**: JSON stdout → Promtail → Loki，每条日志自动注入 trace_id / span_id
- **Trace↔Log 双向钻取**: Tempo瀑布图点击Span→Loki日志；Loki日志点击trace_id→Tempo瀑布图
- **一键部署**: `docker-compose-obs.yml` 拉起 Loki + Tempo + Promtail + Grafana
- **自动Provisioning**: Grafana 启动自动配置 Loki/Tempo 数据源，开箱即用

### 2.5 OpenClaw 容器化接入

- **容器化部署**: OpenClaw Gateway 以 Docker 容器运行（端口 18789），纳入业务栈编排
- **真实模型对接**: 通过 `ZAI_API_KEY` 对接 z.ai GLM 模型，SSE流式输出返回真实AI响应
- **健康检查适配**: OpenClaw 无标准 `/health` 端点，以 POST `/v1/chat/completions` 空 payload 作为可达性探测
- **SSE代理修复**: 禁用 httpx 默认超时（timeout=None），解决流式传输 ReadTimeout 问题

### 2.6 代码质量治理（三批次修复）

对全量代码进行系统性审查，发现并修复 30+ 问题，分三个批次实施：

#### 批次1 — trace_id 统一为 OTel 标准 (commit `1683696`)
- 移除自定义 `X-Trace-ID` 头部手动传播，改用 OTel `traceparent` 自动传播
- 所有服务层改用 `get_current_trace_id()` 从 OTel 上下文获取 32位 hex trace_id 存入 DB
- 删除死代码 `middleware/trace_id.py`，简化 `shared/utils/trace_id.py` 为 OTel 薄封装
- 修复 `case_service` 中 trace_id 回退误用 `_generate_case_id()` 的 bug
- 移除 `case_repo.update_status()` 对 trace_id 的覆盖写入（保留创建时的 trace_id）
- 移除 `openclaw_client` 多余的 trace_id 参数
- DB 字段 `trace_id` 从 `VARCHAR(50)` 扩展为 `VARCHAR(64)` 匹配 OTel 32字符 hex

#### 批次2 — 崩溃与数据完整性修复 (commit `f92bd71`)
- `session.py`: 修复 `datetime` import 位置（从文件末尾移到顶部，消除 NameError）
- `websocket.py`: 修复 endpoint URL 缺少 `conversation_id` 路径参数 + 添加校验
- `init_schema.sql`: `trace_id` 列从 `NOT NULL` 改为 nullable，与 ORM 模型对齐

#### 批次3 — 配置一致性 + 代码质量 (commit `ab5ca47`)
- 移除冗余配置字段 `CASE_SERVICE_PORT` / `CONVERSATION_SERVICE_PORT`
- 路由 URL 构造改用 `CASE_SERVICE_URL` / `CONVERSATION_SERVICE_URL` 配置项
- `logger.py`: `get_logger()` 添加实例缓存，避免重复创建
- `logger.py`: `log_function_call` 装饰器补全 `@wraps`，保留函数元信息

**验证结果**: 每批次均通过 `scripts/docker-e2e-test.sh` 端到端测试，确认工单CRUD、对话SSE、消息持久化、跨服务 trace_id 关联全部正常。

### 2.7 前端双应用实现

采用 monorepo 架构，两个独立 Vue 3 应用共享类型和API层：

**项目结构**: `frontend/` monorepo (npm workspaces)
- `shared/` — 共享类型定义（与后端schemas对齐）、Axios API客户端封装
- `customer/` — 客户端对话式UI（端口 3001）
- `admin/` — 管理控制台（端口 3002）

**Customer 客户端**（纯对话界面）:
- 自动生成 clientId（localStorage UUID），无需登录
- 用户第一条消息自动创建工单 → 自动确认 → 创建对话
- SSE 流式AI响应，打字机效果展示
- 支持 `/close` 命令关闭工单，支持对话历史恢复
- 代码块渲染 + 复制按钮

**Admin 管理控制台**（Element Plus 后台管理）:
- **仪表盘**: 工单总数/待确认/处理中/已关闭 四维统计 + 最近工单 + 活跃客户端
- **工单管理**: 全量工单分页列表，支持状态/客户端ID筛选
- **工单详情**: 基本信息 + 完整对话记录只读回溯
- **用户管理**: 客户端列表，含工单数量和最近工单时间
- **系统监控**: Grafana iframe 嵌入

**新增后端API**（Admin专用）:
- `GET /api/cases/all` — 分页+筛选查询所有工单
- `GET /api/cases/stats` — 按状态统计工单数量
- `GET /api/cases/clients` — 客户端列表及工单数

**Docker集成**: Nginx多阶段构建，反向代理API，SSE支持

### 2.8 Web端集成测试与UX优化（2026-02-27）

通过真实浏览器端到端测试，发现并修复多项问题，提升整体用户体验：

#### Bug修复

| 问题 | 根因 | 修复 | Commit |
|---|---|---|---|
| 浏览器 404 错误 | Axios `baseURL='/api'` + 路径 `/api/cases/` 导致双前缀 `/api/api/cases/` | 所有 API 方法路径去除 `/api` 前缀 | `f9b3ab0` |
| `/close` 后输入框仍可输入 | input 仅在 `isStreaming` 时 disabled | 新增 `isCaseClosed` 计算属性，关闭后 disabled + placeholder 提示 | `1a0e465` |
| 点击「新建工单」历史消息丢失 | `startNewConversation()` 直接 `messages = []` | 历史改由独立抽屉查看，当前对话清空不影响 | `aabac96` → `653fe4a` |

#### UX增强

- **关闭工单引导**: 工单关闭后输入框 disabled，显示 closed-bar（"当前工单已关闭" + 新建工单按钮）
- **历史工单抽屉**: Header 新增「📋 历史工单」按钮，打开左侧抽屉
  - 工单列表：展示工单号、标题、状态标签、创建时间
  - 对话预览：点击某工单加载该工单完整对话记录
  - 工单恢复：活跃工单支持「切换到此工单」直接恢复
- **工单状态生命周期文档**: `02_数据库设计.md` 新增 §2.2.1，详细记录 6 种状态的说明、触发场景、流转图及 v1.0 实际使用范围
- **测试指南重写**: `07_测试指南.md` 完整重写（613行），覆盖 12 个 Web 测试用例、curl 脚本、故障排查

#### 提交记录

```
653fe4a 功能: 历史工单查看抽屉+工单状态生命周期文档
aabac96 优化: 新建工单保留历史消息+分割线样式
1a0e465 优化: 关闭工单后禁用输入框并引导用户新建工单
f9b3ab0 修复: API路径双前缀(/api/api/)导致404 + 重写测试指南
```

### 2.9 K8s 部署设计（2026-02-27）

- **K8s 设计文档**: 新增 `05_K8s设计.md` (~500行)，涵盖 K3s 选型、命名空间策略、工作负载拓扑、配置管理、存储、Ingress、健康探针、资源限额、可观测性迁移、Helm 结构、部署流程
- **Helm Chart 骨架**: `deploy/helm/hci-platform/` 完整 Helm chart (22 K8s 资源)，lint 通过，template render 验证通过
- **配置统一**: `.env.example` 重写 (~70行)，所有 `docker-compose.yml` 硬编码值替换为 `${VAR:-default}`，`config.py` 默认值对齐
- **文档体系调整**: 原 05~08 文档重编号为 06~09，交叉引用全部更新

### 2.10 架构 v2.0 升级设计（2026-02-27）

**升级动因**: 识别到四大业务场景需要多助手架构：
1. **多并发** — 云服务场景，0-N 用户同时使用，各需独立 AI 实例
2. **多样性** — OpenClaw 可替换为 nabobot / picoclaw / zeroclaw / 自研助手
3. **独立性** — 每个 Case 需隔离上下文，避免交叉污染
4. **统一性** — 统一知识库 + Prompt，横向对比不同助手的诊断准确率

**设计产出**:
- `01_架构设计.md` 重写为 v2.0 (1200+ 行)，全面泛化 "OpenClaw Pod Pool" → "AI Assistant Pod Pool"
- 新增组件设计: AI Assistant Protocol v1、Knowledge Base Service (RAG)、助手选择机制
- 详细的 3-Tier 知识注入方案 (Static Prompt + SOP + RAG)
- 完整的数据模型扩展 (assistant_type / kb_document / kb_chunk / assistant_evaluation)

### 2.11 架构 v2.0 代码落地（2026-02-28）

将 v2.0 设计文档中的多助手架构代码实现落地，涉及 4 个微服务 16 个文件，按依赖顺序分 5 步完成：

#### Step 1: 数据库 Schema + Model + Schema 扩展

| 文件 | 变更 |
|------|------|
| `database/init_schema.sql` | `case` 表新增 `assistant_type VARCHAR(50) DEFAULT 'openclaw'`；`conversation` 表 `openclaw_pod_id` → `pod_id` + 新增 `assistant_type`；新增 `assistant_evaluation` 表 |
| `backend/case-service/app/models/case.py` | 新增 `assistant_type = Column(String(50), default='openclaw')` |
| `backend/conversation-service/app/models/conversation.py` | `openclaw_pod_id` → `pod_id`；新增 `assistant_type = Column(String(50), default='openclaw')` |
| `backend/shared/models/schemas.py` | `CaseCreate` 新增 `assistant_type: Optional[str]`；`CaseResponse` 新增 `assistant_type` |
| `backend/case-service/app/services/case_service.py` | `create_case()` 传入 `assistant_type=case_create.assistant_type or 'openclaw'` |

#### Step 2: Scheduler 多类型重构

| 文件 | 变更 |
|------|------|
| `backend/scheduler-service/app/config.py` | 替换单类型配置为 `ASSISTANT_REGISTRY_JSON` (JSON dict)；`@property assistant_registry` 属性解析 |
| `backend/scheduler-service/app/services/pod_pool.py` | `PodPool` 构造函数接受 `(k8s_client, assistant_type, assistant_config)`；新增 `PodPoolManager` 管理 `dict[str, PodPool]` |
| `backend/scheduler-service/app/services/k8s_client.py` | `create_pod()` 接受 `assistant_type` + `assistant_config`，动态生成 image/port/labels/env |
| `backend/scheduler-service/app/services/scheduler_service.py` | `allocate_pod(case_id, assistant_type)`；allocations 存 `(pod_name, type)` 元组；新增 `get_available_assistants()` |
| `backend/scheduler-service/app/routes/scheduler_routes.py` | `PodAllocationRequest` 新增 `assistant_type`；新增 `GET /api/scheduler/assistants` 端点 |

#### Step 3: Conversation 抽象层

| 文件 | 变更 |
|------|------|
| **(新建)** `backend/conversation-service/app/services/ai_client.py` | `AIAssistantClient` Protocol + `OpenClawAssistant` 实现 + `AIAssistantRegistry` 多客户端管理 |
| `backend/conversation-service/app/services/conversation_service.py` | 构造函数 `(repo, ai_registry)`；`send_message_stream_only()` 接受 `assistant_type` 参数 |
| `backend/conversation-service/app/routes/conversations.py` | 全局依赖改为 `ai_registry: AIAssistantRegistry`；`set_dependencies(db, registry)` |
| `backend/conversation-service/app/main.py` | 初始化 `AIAssistantRegistry`，注册 OpenClaw 客户端 |

#### Step 4: API Gateway 路由扩展

| 文件 | 变更 |
|------|------|
| **(新建)** `backend/api-gateway/app/routes/assistants.py` | `GET /api/assistants` 代理到 Scheduler；含降级兜底返回默认助手列表 |
| `backend/api-gateway/app/main.py` | 注册 `assistants.router` |

#### Step 5: 前端助手选择器（前序会话已完成）

已在 commit `cbcbb95` 中完成，与本次后端代码对齐：
- `frontend/shared/src/types.ts`: `CaseCreate.assistant_type` + `AssistantInfo`
- `frontend/customer/src/stores/chat.ts`: `fetchAssistants()` + `selectedAssistant`
- `frontend/customer/src/components/ChatWindow.vue`: el-select 助手选择器

**旧文件保留**: `openclaw_client.py` 保留作为参考/回退，新代码使用 `ai_client.py`。

### 2.12 K3s Helm 部署落地（2026-02-28）

将完整平台（业务服务 + 可观测性栈）通过 Helm 部署到 K3s，最终 **13 个 Pods 全部Running**。

#### 部署架构

```
Traefik Ingress (80/443)
 ├─ /          → customer-ui
 ├─ /admin     → admin-ui
 ├─ /api       → api-gateway
 └─ /ws        → api-gateway (WebSocket)

hci-troubleshoot namespace (9 pods):
  api-gateway, case-service, conversation-service, scheduler-service
  customer-ui, admin-ui, openclaw, postgres, redis

hci-observability namespace (4 pods):
  grafana, loki, tempo, promtail (DaemonSet)
```

#### Helm Chart 变更

| 文件 | 变更 |
|------|------|
| `templates/openclaw-service.yaml` | ExternalName → Deployment+Service（容器化部署 + tcpSocket 探针） |
| `templates/configmap.yaml` | 新增 `ASSISTANT_REGISTRY_JSON`, `SCHEDULER_SERVICE_URL` |
| `templates/scheduler-service/deployment.yaml` | 添加 `DATABASE_URL` |
| `templates/postgres/init-configmap.yaml` | 嵌入完整 init_schema.sql |
| `values.yaml` | 完整重写：OpenClaw + observability 配置 |
| `values-dev.yaml` | `imagePullPolicy: Never` + OpenClaw hostPath 卷挂载 |
| `templates/observability/*.yaml` (6 files) | Loki / Tempo / Promtail / Grafana / namespace / configs |

#### 前端适配

| 文件 | 变更 |
|------|------|
| `frontend/customer/src/utils/clientId.ts` | `crypto.randomUUID()` → `Date.now() + Math.random()` 降级（HTTP 非安全上下文兼容） |
| `frontend/admin/vite.config.ts` | 新增 `base: '/admin/'` 适配 Ingress 子路径 |
| `frontend/admin/nginx.conf` | `location /` → `location /admin { alias ... }` |

#### 自动化脚本

| 脚本 | 用途 |
|------|------|
| `scripts/k3s-build.sh` | 构建全部 7 个 Docker 镜像 |
| `scripts/k3s-deploy.sh` | Helm install/upgrade/uninstall 管理 |
| `scripts/k3s-verify.sh` | Pod 状态 + 健康检查 + E2E 验证 |

#### 部署过程中修复的问题

| # | 问题 | 修复 |
|---|------|------|
| 1 | K3s 禁用了 Traefik | 移除 `--disable traefik` 启动参数，重启 K3s |
| 2 | `crypto.randomUUID` 白屏 | HTTP 非安全上下文下降级为 Math.random |
| 3 | Admin UI JS MIME 错误 | Vite base URL 从 `/` 改为 `/admin/` |
| 4 | OpenClaw CrashLoopBackOff | httpGet 探针返回 405 → 改用 tcpSocket 探针 |
| 5 | Grafana 跨 namespace Secret | 改用 inline value |
| 6 | v2.0 代码缺陷 | 从 GitHub 拉取修复代码并重新构建部署 |

### 2.13 Phase 1+2 重构优化（2026-03-01）

对全量代码进行 10 维度深度审查，识别 21 项改进点。Phase 1（Bug 修复）+ Phase 2（架构加固）共 10 项全部完成。

#### Phase 1 — Bug 修复（5 项）

| # | 问题 | 修复 |
|---|------|------|
| 1 | `conversation_repo.add_message()` 双重 commit + 双重 message_count 递增 | 移除显式 commit 和手动累加，依赖 context manager + DB 触发器 |
| 2 | CORS `allow_origins=["*"]` + `allow_credentials=True` 违反 RFC | 新增 `ALLOWED_ORIGINS` 配置，改用显式 Origin 列表 |
| 3 | 关闭工单后未释放 Scheduler Pod | 关闭成功后调用 `POST /api/scheduler/pods/release` |
| 4 | `routes/assistants.py` 通用 except 吞掉 HTTPException | 添加 `except HTTPException: raise` |
| 5 | 过时文件 `openclaw_client.py` 未清理 | 删除（已被 `ai_client.py` 完全替代） |

#### Phase 2 — 架构加固（5 项）

| # | 改进 | 变更 |
|---|------|------|
| 1 | Scheduler 状态迁移 Redis | 内存 Dict → Redis Hash `scheduler:allocations`，新增 `REDIS_URL` 配置 |
| 2 | `pod_pool.initialize()` 实现 | K8s label selector 扫描 + Pod 状态分类 + 故障 Pod 清理 |
| 3 | 全局变量 DI → `app.state` | 4 个服务 main.py 全部迁移，消除全局可变状态 |
| 4 | 移除 `sys.path.insert` | 14 个文件清理；`docker-compose.yml` 添加 `PYTHONPATH: /app`；创建 5 个 conftest.py |
| 5 | 启动流程加固 | 后台任务使用 `_safe_create_task()` + 异常回调；shutdown 取消未完成任务 |

#### 测试更新

| 测试文件 | 变更 |
|---------|------|
| `tests/unit/test_case_service.py` | 完全重写，匹配 v2.0 API 签名 |
| `backend/scheduler-service/tests/unit/test_scheduler_service.py` | 完全重写，Mock Redis |
| `backend/conversation-service/tests/integration/test_conversation_integration.py` | Mock 路径更新 |

**详细记录**: 见 `docs/10_重构优化.md`

### 2.14 RAG 知识库完整设计（2026-03-05）

**目标**: 完成知识库 RAG 系统的端到端设计，覆盖数据源→ETL→KB Service→检索引擎

**主要决策**:
- 三类数据源（故障工单知识、HCI 产品文档、SOP 节点映射）
- KB Service 暴露 5 个核心 API（`/retrieve` `/ingest` `/health` 等）
- RAG 与 AI 层严格解耦：KB Service 只返回 ranked_results，上下文组装归 AI 层

**涉及文档**: `docs/13_RAG设计.md`（全新编写，879 行）

---

### 2.15 AI 层 v3.0：LearningClaw + ProductionClaw（2026-03-05）

**目标**: 实现一 Pod 一 Case 的知识生产/消费分离架构

**核心变更**:
- LearningClaw（`init-learning`）: 知识注入型，实时向 KB Service 写入排障知识
- ProductionClaw（`init-production`）: 知识消费型，启用 RAG 增强回答
- Scheduler 扩展 `init_configs/` 热加载，支持动态分配两种 Pod 类型
- 12 个 init 配置文件落地（`deploy/claw-configs/`）

**提交**: `60d7aec` | **涉及文档**: `docs/12_AI层设计.md` 第二部分

---

### 2.16 文档整理合并（2026-03-05）

**目标**: 归并碎片化文档，更新架构文档至 v3.0 现状

**变更清单**:
- `12_部署问题汇总.md` → 合并入 `11_生产环境.md` §14（删除 12）
- `14_LearningClaw实现方案.md` → 合并入 `12_AI层设计.md` 第二部分（删除 14）
- `13_RAG设计.md`（添加边界解耦说明，序号整理后保持编号 13）
- `01_架构设计.md` 升至 v3.0，`08_文件清单.md` 升至 v4.0（完全重写）

---

### 2.17 Phase 3 重构优化（2026-03-06）

提交: `89e527d`  详细记录: `docs/10_重构优化.md` §6

**目标**: 完成可观测性增强（O-1/O-2）、健康检查标准化、Scheduler Redis 集成测试（T-2），以及分配时传 case_info。

#### 新增文件

| 文件 | 说明 |
|------|------|
| `backend/shared/utils/metrics.py` | 共享 Prometheus 指标定义（Histogram/Counter/Gauge） |
| `deploy/observability/config/prometheus.yml` | Prometheus 抓取配置（覆盖 5 个服务端点） |
| `backend/scheduler-service/tests/integration/test_scheduler_redis.py` | 12 个 Redis 集成测试（fakeredis，全部通过） |

#### 主要变更

| # | 模块 | 变更 |
|---|------|------|
| 1 | `conversation-service/app/services/conversation_service.py` | SSE 异常在流中回传错误事件（E-1）；记录 TTFT 到 `hci_ai_ttft_seconds` Histogram |
| 2 | `scheduler-service/app/services/k8s_client.py` | `create_pod()` 添加指数退避重试最多 3 次（E-2） |
| 3 | `shared/utils/metrics.py` (新建) | 定义 5 个共享指标：`hci_ai_ttft_seconds`、`hci_ai_requests_total`、`hci_kb_search_seconds`、`hci_pod_pool_idle`、`hci_pod_pool_active` |
| 4 | `conversation-service/app/main.py` | 暴露 `GET /metrics` 端点（O-1） |
| 5 | `scheduler-service/app/main.py` | 暴露 `GET /metrics` 端点；后台任务每 15s 更新 Pod 池 Gauge（O-2） |
| 6 | `case-service/app/routes/health.py` | 新增 DB (`SELECT 1`) 健康检查 |
| 7 | `conversation-service/app/routes/health.py` | 新增 DB + KB HTTP 健康检查；结构化返回各依赖状态 |
| 8 | `scheduler-service/app/routes/health.py` | 新增 Redis (`PING`) 健康检查；返回 Pod 池概况 |
| 9 | `conversations.py` → `conversation_service.py` → `scheduler_client.py` | 分配 Pod 时携带 `case_title` / `case_description` |
| 10 | `deploy/observability/docker-compose-obs.yml` | 新增 Prometheus 服务（端口 9090）；Grafana 自动 Provisioning Prometheus 数据源 |
| 11 | `tests/conftest.py` | 预先 mock `kubernetes.*` 所有子模块，消除 pytest-collect 超时 |

---

### 2.18 Task 26/27 E2E 验证 + K3s 调度修复（2026-03-10）

提交: 本次  详细证据: 见§九 P1-1/P1-2 验收

**目标**: 验证 LearningClaw 知识闭环（Task 26）和 ProductionClaw 调度链路（Task 27）端到端可用。

#### 根因修复

| 问题 | 根因 | 修复 |
|------|------|------|
| Scheduler 503 "No pool found for assistant type: openclaw" | `ASSISTANT_REGISTRY_JSON` 只含 `productionclaw`，但代码默认 `openclaw` | `kubectl patch cm hci-common-config` 新增 openclaw 条目 |
| Scheduler RBAC 403 | 缺少 `pods` list/create/delete 权限 | 新增 `deploy/helm/hci-platform/templates/rbac.yaml`（SA+Role+RoleBinding） |
| case-service KB push 无日志 | pod 运行旧镜像（不含 `kb_pusher.py`、新 `config.py`、`generate_case_id`）| 使用旧镜像为 base 构建 patch 镜像，仅复制 services/repositories/config |
| kb_pusher.py import httpx 失败 | 旧镜像无 `httpx` 包 | 将 `httpx` 改为标准库 `urllib.request` + `asyncio.to_thread()` |

#### 新增/修改文件

| 文件 | 变更 |
|------|------|
| `deploy/helm/hci-platform/templates/rbac.yaml` | 新建：scheduler-service SA + Role(pod CRUD) + RoleBinding |
| `deploy/helm/hci-platform/templates/scheduler-service/deployment.yaml` | 新增 `serviceAccountName: scheduler-service` |
| `deploy/helm/hci-platform/values.yaml` | `assistantRegistryJson` 新增 openclaw 条目（labels/port/pool_size） |
| `backend/case-service/app/services/kb_pusher.py` | 将 `httpx` 替换为 `urllib.request` + `asyncio.to_thread()`（兼容旧镜像） |

#### Task 27 验证证据

```
event: pod_allocated
pod_name: openclaw-5647774cc8-srfkm
case_id: Q2026031059787
assistant_type: openclaw
POST /api/scheduler/pods/allocate HTTP/1.1 200 OK
```

#### Task 26 验证证据

```
event: kb_push_success
case_id: Q2026031098326
http_status: 201
# KB 文档
id=57, title="工单摘要: Task26 KB 闭环验证", source_id=Q2026031098326, source_type=realtime
# KB chunk
id=255, document_id=57, content="## 故障描述 ..."
# learningclaw-0: 1/1 Running
```

---

## 三、架构完整性评估（2026-03-06 当前状态）

### 3.1 已完成落地（实现、测试、可运行）

| 层 | 组件 | 说明 |
|----|------|------|
| **API 层** | API Gateway (port 8000) | 路由代理、CORS、WebSocket、Redis 会话、KB 路由 |
| **业务层** | Case Service (port 8001) | 工单全生命周期 CRUD + 状态机；关闭时 fire-and-forget 推送至 KB |
| **业务层** | Conversation Service (port 8002) | SSE 流式对话；4-Tier Prompt（身份+SOP+RAG+工单上下文）；KB 注入；TTFT 指标 |
| **调度层** | Scheduler Service (port 8003) | 多类型 Pod 池（`PodPoolManager`）；Redis 持久化分配状态；Pod 池 Gauge |
| **知识层** | KB Service (port 8004) | BM25+向量 RRF 混合检索；SHA256 幂等摄入；SOP 关键词精确路由；jieba HCI 词典 |
| **AI 层** | AIAssistantClient Protocol | `OpenClawAssistant` 适配器 + `AIAssistantRegistry`；多助手可扩展 |
| **AI 层** | LearningClaw / ProductionClaw | Helm StatefulSet + init 配置文件（6个×2）+ CronJob（已部署，知识生产/消费分离）|
| **数据层** | PostgreSQL 15 + pgvector | KB v3.0 schema（5 张新表）；`assistant_type` 字段；向量检索 IVFFlat |
| **数据层** | Redis 7 | Scheduler 分配状态持久化；API Gateway 会话 |
| **可观测性** | Loki + Promtail + Tempo + Grafana | 结构化日志；Trace↔Log 双向钻取；自动 Provisioning |
| **可观测性** | Prometheus | 5 个业务指标；conversation + scheduler `/metrics` 端点；Grafana 数据源 |
| **前端** | Customer UI (Vue 3, port 3001) | 对话式界面；助手选择器；历史工单抽屉；SSE 打字机 |
| **前端** | Admin Console (Vue 3, port 3002) | 工单管理 + 统计 + Grafana iframe |
| **K3s** | Helm 生产部署 | 13 Pods Running；Traefik Ingress；hci-troubleshoot + hci-observability 双命名空间 |
| **测试** | 单元 + 集成测试 | case_service 单测、scheduler Redis 集成测试（12 个）、conversation 集成测试 |
| **数据** | SOP 测试数据 | `data-pipeline/sop_skills/`（4 类故障域 × keywords_map + MD 文档）+ jieba 词典 |

### 3.2 已设计·未完全落地

| 组件 | 设计文档 | 缺什么 |
|------|---------|--------|
| **ProductionClaw / LearningClaw 实际流量** | `12_AI层设计.md` | Helm 已含 StatefulSet，但 K3s 现有工作负载跑的仍是旧版 OpenClaw；需重新 `helm upgrade` 确认新 Pod 实际分配给工单 |
| **KB Service 进 K3s** | `08_文件清单.md` | `deploy/helm/hci-platform/templates/kb-service/` 模板尚未创建；K3s 部署中缺 KB Service Pod |
| **Prometheus 进 K3s** | `04_可观测性设计.md` | `deploy/observability/docker-compose-obs.yml` 中已有 Prometheus，Helm Chart 未添加 Prometheus StatefulSet |
| **LearningClaw 经验提炼** | `12_AI层设计.md` §5 | Case 关闭时触发的 KB 推送已实现（§2.21），但 LearningClaw Pod 实际运行并主动学习的闭环尚未端到端验证 |
| **Frontend 引用显示** | `13_RAG设计.md` | 前端 ChatWindow 未显示 KB 检索来源（doc_id / sop_name） |
| **评估体系** | `01_架构设计.md` §附录C | `assistant_evaluation` 表已建，但采集端点、评分逻辑未实现 |

### 3.3 待执行（按优先级排序）

| 任务 # | 任务名称 | 优先级 | 状态 |
|--------|---------|--------|------|
| 25 | **ETL 产品案例数据建设** | P0 | ⏳ 待执行 |
| 26 | **LearningClaw 闭环 E2E 验证** | P1-1 | ✅ 已完成 |
| 27 | **ProductionClaw 调度链路验证** | P1-2 | ✅ 已完成 |
| 28 | **Prometheus 进 K3s** | P1-3 | ⏳ 待执行 |
| 29 | **Phase 4 测试覆盖 + CI/CD** | P2 | ✅ 已完成 |
| — | 前端 RAG 引用显示 / NaboBot 接入 / 助手评估体系 | P3 | ⬜ 可延后 |

---

## 四、下一步执行计划（更新于 2026-03-10）

> 阶段一（外挂 RAG）已于 2026-03-10 验收通过。以下是后续任务的详细执行计划，按优先级顺序执行。
>
> **优先级定义**：P0 = RAG 数据建设（直接影响产品价值）；P1 = 生产环境完整性；P2 = 自动化测试 + CI；P3 = 功能扩展（可延后）

---

### P0：产品案例 ETL 数据建设（Task 25）

**背景**：知识库当前仅有 37 篇手工编写的 SOP 文档。要实现真正的 RAG 价值，需要将 6000+ 真实产品案例批量入库。

**工作目录**：`data-pipeline/`

**Step 1：初始化 ETL 环境**
```bash
cd /aihci/hci-troubleshoot-platform/data-pipeline
uv sync  # 确认依赖已安装（requests / markdownify / tqdm / httpx 等）
```

**Step 2：开发 6 个 ETL 脚本**

| 文件 | 职责 | 关键逻辑 |
|------|------|----------|
| `fetcher.py` | 从 support.sangfor.com.cn 抓取案例列表 + 正文 | HTTP GET + 翻页参数 + 指数退避重试；输出 `raw/<id>.html` |
| `converter.py` | HTML → 标准 Markdown JSON | markdownify 或 html2text；输出 `converted/<id>.json`（含 title/content_md/source_url/tags）|
| `enricher.py` | 元数据补全（category / keywords） | 可选：调用 ZAI LLM 对 content 做分类打标；输出 `enriched/<id>.json` |
| `reviewer_cli.py` | 交互式人工抽查 | 随机抽样展示，输入 y/n/s(kip)，写 `reviewed.jsonl` |
| `ingestor.py` | 批量调用 POST /api/kb/ingest | SHA256(source_url) 作为 source_id 保证幂等；断点续传（记录已入库 ID）|
| `pipeline.py` | 串联调度器 | `--stage fetch/convert/ingest/all`；支持 `--limit N` 小批量测试 |

**Step 3：小批量测试（50 篇）**
```bash
# 目标 KB Service 地址（绕过 Traefik，大文件安全）
export KB_URL="http://10.42.0.144:8004"
export KB_TOKEN="hci-dev-internal-token"

uv run pipeline.py --stage all --limit 50
# 验证：检查 KB 文档数
curl -s "$KB_URL/api/kb/stats" -H "Authorization: Bearer $KB_TOKEN"
```

**Step 4：验证 RAG 效果**
```bash
# 用真实产品案例关键词测试检索
curl -s -X POST "$KB_URL/api/kb/search" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $KB_TOKEN" \
  -d '{"query": "虚拟机磁盘IO异常排查", "top_n": 3}'
```

**Step 5：全量入库**
```bash
uv run pipeline.py --stage all  # 预计耗时数小时，建议 tmux 后台
```

**完成标准**：
- `kb_document` 表文档数 ≥ 1000（阶段性目标），最终 ≥ 6000
- `POST /api/kb/search` 用真实产品关键词能命中相关文档
- AI 对话回答中引用内容来自真实案例而非仅 SOP

---

### P1-1：LearningClaw 知识闭环 E2E 验证（Task 26）

**背景**：case_service 关闭工单时会 fire-and-forget 推送对话摘要至 KB（Task 21 已实现代码）。但该链路从未在 K3s 集群中端到端跑通过。

**验证步骤**：

```bash
# 1. 确认 learningclaw-0 Pod 状态
kubectl get pods -n hci-troubleshoot | grep learningclaw

# 2. 创建工单 + 发送 2-3 条对话
CASE_ID=$(curl -s -X POST "http://192.168.0.4:4888/api/cases/" \
  -H "Content-Type: application/json" \
  -H "Host: api.192.168.0.4.nip.io" \
  -H "Authorization: Bearer hci-dev-internal-token" \
  -d '{"title":"闭环测试工单","description":"测试","priority":"low","source":"manual","client_id":"test"}' \
  | python3 -c "import json,sys; print(json.load(sys.stdin)['case_id'])")
echo "CASE_ID=$CASE_ID"

# 对话（创建 + 发消息略，参考 §7.6 步骤）

# 3. 关闭工单
curl -s -X PATCH "http://192.168.0.4:4888/api/cases/${CASE_ID}" \
  -H "Content-Type: application/json" \
  -H "Host: api.192.168.0.4.nip.io" \
  -H "Authorization: Bearer hci-dev-internal-token" \
  -d '{"status":"closed"}'

# 4. 检查 case-service 日志
kubectl logs -n hci-troubleshoot -l app=case-service --tail=50 | grep -E "kb_push|case_closed"

# 5. 检查 KB 是否新增文档
curl -s "http://10.42.0.144:8004/api/kb/stats" -H "Authorization: Bearer hci-dev-internal-token"

# 6. 检查 learningclaw 日志（learningclaw 是否消费了新知识）
kubectl logs -n hci-troubleshoot learningclaw-0 --tail=50
```

**排查要点**：
- 若 case-service 无 `kb_push` 日志 → 检查 `KB_ENABLED` 和 `KB_SERVICE_URL` 环境变量
- 若 KB 无新增文档 → 检查 case_service 推送代码（`backend/case-service/app/services/case_service.py`，关闭工单分支）
- 若 learningclaw 无消费行为 → 当前 learningclaw 是被动接收还是主动轮询？需查代码

**完成标准**：
- 关闭工单后 case-service 日志可见 `event=kb_push_success`
- KB 文档数自动 +1（对应刚关闭的工单摘要）
- learningclaw-0 日志有对应处理记录

---

### P1-2：ProductionClaw 调度链路验证（Task 27）

**背景**：scheduler-service 设计上应为每个新工单分配一个 openclaw Pod（一 Pod 一 Case 架构）。当前集群中只有一个 openclaw deployment，未确认 scheduler 是否真的在分配 `productionclaw` 类型 Pod。

**验证步骤**：

```bash
# 1. 检查 scheduler-service 配置
kubectl exec -n hci-troubleshoot deploy/scheduler-service -- env | grep -E "SCHEDULER|OPENCLAW|POD"

# 2. 创建新工单，观察 scheduler 日志
kubectl logs -n hci-troubleshoot -l app=scheduler-service -f &
# （同时创建工单，参考上方命令）

# 3. 检查是否有 pod_allocated 事件
kubectl logs -n hci-troubleshoot -l app=scheduler-service --tail=100 | grep -E "pod_allo|schedule|assign"

# 4. 检查 openclaw deployment 上的标签
kubectl get deployment -n hci-troubleshoot openclaw -o yaml | grep -A5 'labels:'
```

**排查要点**：
- 若 scheduler 未分配 → 查 `SCHEDULER_ENABLED` 是否为 true
- 若 Pod 标签不含 `assistant_type=productionclaw` → 需更新 Helm values
- 当前 `conversation-service` 通过 `pod_endpoint` 路由请求至分配的 Pod；若 scheduler 返回空，则 fallback 到默认 openclaw

**需要修复的文件**（如调度未生效）：
- `deploy/helm/hci-platform/values.yaml` — schedulerService + openclaw 配置
- `deploy/helm/hci-platform/templates/openclaw/deployment.yaml` — Pod 标签

**完成标准**：
- 新建工单后，scheduler-service 日志可见 `event=pod_allocated`
- `GET /api/cases/{case_id}` 返回中含 `pod_endpoint` 字段且非空
- conversation-service 请求路由至该 pod_endpoint

---

### P1-3：Prometheus 进 K3s（Task 28）

**背景**：各服务已通过 `shared/utils/metrics.py` 暴露 Prometheus 格式指标（`/metrics` 端点），但集群中无 Prometheus 实例来采集。Grafana 仪表盘中的 AI 指标面板当前为空。

**执行步骤**：

**Step 1：新增 Helm 模板**
```
deploy/helm/hci-platform/templates/observability/
  └── prometheus.yaml   # Deployment + Service + ConfigMap(scrape_configs)
```

Scrape 目标（`scrape_configs`）：
```yaml
- job_name: 'hci-services'
  kubernetes_sd_configs:
    - role: pod
      namespaces:
        names: [hci-troubleshoot]
  relabel_configs:
    - source_labels: [__meta_kubernetes_pod_annotation_prometheus_io_scrape]
      action: keep
      regex: 'true'
    - source_labels: [__meta_kubernetes_pod_annotation_prometheus_io_path]
      target_label: __metrics_path__
      regex: (.+)
```

**Step 2：各服务 Pod 添加 Prometheus 注解**（在 Helm deployment 模板的 pod 注解中）：
```yaml
annotations:
  prometheus.io/scrape: "true"
  prometheus.io/port: "800x"
  prometheus.io/path: "/metrics"
```

**Step 3：部署验证**
```bash
helm upgrade hci-platform ./deploy/helm/hci-platform -n hci-troubleshoot
kubectl get pods -n hci-troubleshoot | grep prometheus
# 验证指标采集
kubectl exec -n hci-troubleshoot deploy/prometheus -- wget -qO- \
  http://conversation-service:8002/metrics | grep ai_requests_total
```

**Step 4：Grafana 数据源配置**
- Grafana → Data Sources → 添加 Prometheus → URL: `http://prometheus:9090`
- 验证 `ai_ttft_seconds` / `ai_requests_total` 指标可查询

**完成标准**：
- Prometheus Pod Running
- Grafana 可查询 `ai_requests_total` 指标
- 发起一次 AI 对话后，`ai_ttft_seconds_bucket` 有数据

---

### P2：Phase 4 测试覆盖 + CI/CD（Task 29）

对应 `docs/10_重构优化.md` §7，按以下顺序执行：

| 子任务 | 目标文件/目录 | 完成标准 |
|--------|-------------|----------|
| **T1: Pod 调度 E2E 测试** | `tests/e2e/test_pod_scheduling.py` | 创建工单→分配 Pod→对话→释放 Pod 全流程自动化通过 |
| **T2: GitHub Actions CI** | `.github/workflows/ci.yml` | PR 自动触发 lint + pytest，失败阻止合并 |
| **T3: WebSocket 集成测试** | `tests/integration/test_websocket.py` | WS 建立、流式收发、断连重连场景全部断言通过 |
| **T4: CORS 安全测试** | `tests/security/test_cors.py` | 合法 Origin 200，非法 Origin 403，OPTIONS preflight 通过 |
| **T5: 负载测试基线** | `tests/load/locustfile.py` | P95 < 2s（AI 对话），P95 < 200ms（REST），建立基线快照 |

**最小 CI 配置参考**：
```yaml
# .github/workflows/ci.yml
name: CI
on: [pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v3
      - run: uv sync
      - run: uv run ruff check backend/
      - run: uv run pytest backend/ -x -q --ignore=tests/e2e
```

---

### P3：功能扩展（可延后）

| 任务 | 说明 | 前置依赖 |
|------|------|---------|
| 前端 RAG 引用显示 | ChatWindow 气泡底部展示引用来源（doc_id / SOP 名称） | P0 完成后意义更大 |
| LLM A/B 对照组 | AssistantRegistry 接入第二个 LLM，同问题对比回答质量 | 阶段一已完成，随时可开始 |
| NaboBot 接入 | 验证 `AIAssistantRegistry` 多助手可扩展性 | |
| 助手评估体系 | `assistant_evaluation` 表 + 评分采集端点 | LLM A/B 对照组之后 |
| 生产自动分配策略 | 按工单类型路由 ProductionClaw / LearningClaw | P1-2 完成之后 |

---

## 五、生产部署 Bug 修复记录（2026-03-07）

> 本节记录首次生产环境部署（K3s + Helm）期间发现的全部问题、根因分析及闭环方案。

### 5.1 问题分类总览

| # | 问题描述 | 类别 | 严重度 | 状态 |
|---|---------|------|--------|------|
| P1 | Traefik 占用 80/443 高危端口，需改 4888 | 环境 | 中 | ✅ PIT-021 |
| P2 | Clash TUN 劫持 K3s ClusterIP 流量（Pod 间通信断连） | 环境 | 高 | ✅ PIT-014 |
| P3 | Clash TUN 新增劫持 Docker 172.16/12 网段（外网访问 ERR_EMPTY_RESPONSE） | 环境 | 高 | ✅ PIT-023 |
| P4 | Helm DATABASE_URL 密码含 `@` 导致工单创建 500 | 配置 | 高 | ✅ PIT-022 |
| P5 | Grafana Ingress 无域名时渲染空 rules，K8s Apply 失败 | 配置 | 中 | ✅ PIT-012 |
| P6 | admin-ui SPA 子路径未配置 vite base，白屏 | 项目 | 高 | ✅ PIT-023，代码已修 |
| P7 | nginx.conf replace 操作导致 server{} 块提前关闭 | 项目 | 高 | ✅ 代码已重写 |
| P8 | MonitoringView.vue IP 直连时 Grafana URL 指向 localhost | 项目 | 中 | ✅ PIT-020，代码已修 |
| P9 | Traefik Ingress 无法跨命名空间引用 Service（Grafana 显示 customer-ui） | 项目 | 高 | ✅ PIT-024，代码已修 |
| P10 | 浏览器缓存旧路由响应（修复后仍显示错误内容）| 项目 | 低 | ✅ PIT-025，nginx no-cache 已修 |

### 5.2 各问题详细分析

#### P4：工单创建 500（DATABASE_URL @ 编码）

**现象：** `POST /api/cases` 返回 HTTP 500，日志 `password authentication failed`

**根因：** Helm 模板用字符串拼接 DATABASE_URL，密码 `aihci@aclient2025` 中的 `@` 使 URL 解析器以为主机段从该位置开始，用户名/密码均截断错误。

**修复（commit `4b30de8`）：**
```yaml
# deploy/helm/hci-platform/templates/*/deployment.yaml
value: {{ printf "postgresql+asyncpg://%s:%s@postgres:5432/%s"
    .Values.config.postgresUser
    (.Values.secrets.postgresPassword | urlquery)
    .Values.config.postgresDb | quote }}
```

**防复发：** 密码禁含 URL 特殊字符，或 Helm 模板一律用 `urlquery` encode。

---

#### P6 + P7：admin-ui 空白页（vite base + nginx 双重故障）

**现象：** `http://<IP>/admin/` 返回 200 但页面全白，JS/CSS 请求 404 或请求到 customer-ui 资源。

**根因一（P6）：** `vite.config.ts` `base: '/'` → 打包后 `index.html` 引用 `/assets/index.js`（绝对路径） → Traefik 路由 `/assets/` 命中 `/`（customer-ui）→ 404。

**根因二（P7）：** 修复 nginx.conf 时 `replace_string_in_file` 操作截断了 `server{}` 块，末尾出现游离的 `location` 块，nginx 启动报配置解析错误。

**修复（commit `4b30de8`）：**
1. `vite.config.ts`: `base: '/admin/'`
2. `router/index.ts`: `createWebHistory('/admin/')`
3. `nginx.conf`: 完整重写，使用 `alias` + `try_files $uri $uri/ /index.html`

**防复发：** 见 PIT-023（三处必须并改）、PIT-025（HTML no-cache）。

---

#### P9：Grafana 路由显示 customer-ui（跨命名空间 Ingress 失效）

**现象：** 访问 `/grafana` 返回 customer-ui 的 HTML，Traefik 日志 `Cannot create service: not found`。

**诊断路径：**
1. `curl /grafana/` 返回 `Server: nginx/1.29.5`（customer-ui），排除浏览器缓存
2. `k3s kubectl get svc grafana -n hci-troubleshoot` → **NotFound**（揭示根因：Ingress 和 Service 不在同一命名空间）
3. 尝试 ExternalName 桥接 → Traefik 报 `externalName services not allowed`

**修复（commit `9deb342`）：**
在 `hci-observability`（grafana Service 所在命名空间）创建独立 Ingress 资源，路由 `/grafana` → `grafana:3000`：
```yaml
# templates/observability/ingress.yaml
namespace: {{ .Values.observability.namespace }}   # hci-observability
rules:
  - http:
      paths:
        - path: /grafana
          pathType: Prefix
          backend:
            service:
              name: grafana
              port: 3000
```

**防复发：** 见 PIT-024（Traefik 全命名空间扫描 Ingress，但同条 Ingress 只能引用本命名空间 Service）。

---

### 5.3 遗留问题与闭环计划

| # | 问题 | 优先级 | 闭环方案 | 预期版本 |
|---|------|--------|---------|---------|
| L1 | 前端镜像 `2026.03.07-2`（含 nginx no-cache 修复）尚未重建并部署 | **高** | `cd frontend && docker build -t hci-admin-ui:2026.03.07-2 -f admin/Dockerfile . && docker build -t hci-customer-ui:2026.03.07-2 -f customer/Dockerfile . && docker save ... \| k3s ctr images import - && helm upgrade --set image.tag=2026.03.07-2` | 立即 |
| L2 | Tempo Pod 持续 CrashLoopBackOff（RESTARTS 3+） | 中 | 查看 Pod 日志排查内存/配置问题；确认 Tempo 版本与 OTel SDK 兼容性 | 本周 |
| L3 | 可观测性栈未完整验证（Loki 日志、Tempo Trace → Grafana 钻取）| 中 | 从 admin-ui 监控页验证链路；执行一次 E2E 请求后在 Grafana 确认三个数据源均有数据 | 本周 |
| L4 | OpenClaw farm（`/srv/openclaw/`）只初始化了 instance-1/2，实例3-10 未初始化 | 低 | 执行 `bash /srv/openclaw/init.sh` 初始化所有实例并启动 | 按需 |
| L5 | `openclaw.json` 通过 HostPath 挂载，多实例时仍需手动管理；未纳入 ConfigMap | 低 | 迁移至 K8s ConfigMap + volumeMount，通过 Helm values 统一配置 | 下个迭代 |
| L6 | 知识库 RAG 功能未开始（KB Service 骨架） | 中 | 见 §4.3 计划 | 下个迭代 |

### 5.4 当前生产环境验证状态（2026-03-07）

```
✅ http://14.17.59.69:4888/            → customer-ui 200
✅ http://14.17.59.69:4888/admin/      → admin-ui 200（HCI Admin 管理控制台）
✅ http://14.17.59.69:4888/api/cases/  → [] HTTP 200（API 正常，密码编码修复后）
✅ http://14.17.59.69:4888/grafana/    → Grafana 12.4.0（在新命名空间 Ingress 修复后）
⚠️ http://14.17.59.69:4888/admin/monitoring → Grafana iframe 可加载（需验证 no-cache 镜像）
⚠️ Tempo: CrashLoopBackOff，链路数据未验证
```

---

## 六、生产环境持续修复记录（2026-03-07 续 — REVISION 13）

### 6.1 REVISION 13 核心修复（commit e6c5f7d + 7ea2c78）

**背景：** REVISION 12 完成 Grafana 路由修复后，前端镜像 nginx 无 `no-cache` 头，
浏览器缓存旧资源；OpenClaw Control UI 无法使用（设备身份认证错误 + LLM 超时）。

#### 6.1.1 nginx Cache-Control 永久固化（关闭 L1 遗留问题）

**问题：** REVISION 12 通过 `kubectl cp` 热补丁写入了 nginx.conf，但**Pod 重启即丢失**。

**修复：**
- 将 `Cache-Control: no-store, no-cache, must-revalidate` 直接写入两个前端镜像的 `nginx.conf`
- 重建镜像 `hci-admin-ui:2026.03.07-2` / `hci-customer-ui:2026.03.07-2`
- Helm upgrade → REVISION 13

**验证：**
```bash
curl -sI http://14.17.59.69:4888/admin/ | grep -i cache
# Cache-Control: no-store, no-cache, must-revalidate ✅
```

#### 6.1.2 前端 Dockerfile layer 顺序优化（PIT-029）

**问题：** 原 Dockerfile 先 `COPY` 全量源码再 `npm install`，源码任何改动都导致
依赖层缓存失效，每次构建 8-15 分钟。

**修复：** 拆分 `package.json` COPY → `npm install` 为独立层，置于源码 COPY 之前：
```dockerfile
COPY package.json .npmrc ./
COPY shared/package.json shared/
COPY admin/package.json admin/
RUN npm install          # 仅 package.json 变化时才重跑
COPY shared/ shared/
COPY admin/ admin/
RUN vite build           # 源码变化只重跑这一步（约 20s）
```

#### 6.1.3 OpenClaw Control UI 设备身份报错修复（PIT-026）

**现象：** 打开 OpenClaw WebUI `/control` 页面提示 `requires device identity (use HTTPS or localhost secure context)`

**根因：** K3s 环境通过 HTTP（非 localhost）访问，浏览器 DeviceAPI 被阻断；`openclaw.json` 缺少绕过开关。

**修复：**
```bash
python3 -c "
import json; path='/home/node/.openclaw/openclaw.json'
with open(path) as f: cfg=json.load(f)
cfg['gateway']['controlUi']['dangerouslyDisableDeviceAuth'] = True
with open(path,'w') as f: json.dump(cfg, f, indent=2, ensure_ascii=False)
"
k3s kubectl rollout restart deployment/openclaw -n hci-troubleshoot
```

#### 6.1.4 OpenClaw LLM 超时修复（PIT-027）

**现象：** OpenClaw 对话请求全部 `timed out`

**根因：** 配置的模型 `zai/glm-4.7` 对应域名 `api.zai.chat`，被 Clash TUN 劫持到 `198.18.0.4`，SSL 握手失败。

**修复：** 切换为 `tly/glm-5`（对应 `open.bigmodel.cn`，国内可直连）：
```bash
python3 -c "
import json; path='/home/node/.openclaw/openclaw.json'
with open(path) as f: cfg=json.load(f)
cfg['agents']['defaults']['model'] = {'primary': 'tly/glm-5'}
with open(path,'w') as f: json.dump(cfg, f, indent=2, ensure_ascii=False)
"
```

**验证：** `curl -o /dev/null -w "%{http_code} %{time_total}s" https://open.bigmodel.cn/api/paas/v4/models` → `200 0.25s` ✅

#### 6.1.5 Docker build --network host（PIT-028）

**问题：** 在 Clash TUN 宿主机上 `docker build` 时，容器内 `npm install` 报 `ETIMEDOUT 198.18.x.x`，即使配国内 mirror 也一样。

**根因：** Docker 容器网络独立于宿主机网络，Clash TUN 不覆盖容器内流量，DNS 劫持导致所有出口连接超时。

**修复：** 固定使用 `--network host` 构建前端镜像：
```bash
docker build --network host -t hci-admin-ui:2026.03.07-2 -f frontend/admin/Dockerfile frontend/
```

---

### 6.2 遗留问题更新（REVISION 13 之后）

| # | 问题 | 状态 | 备注 |
|---|------|------|------|
| L1 | 前端镜像含 nginx no-cache | ✅ **已关闭** | REVISION 13 正式镜像 `2026.03.07-2` |
| L2 | Tempo CrashLoopBackOff | ⚠️ 待处理 | 未影响当前主流程 |
| L3 | 可观测性栈完整验证 | ⚠️ 待处理 | Loki/Tempo 数据需 E2E 验证 |
| L4 | OpenClaw 实例 3-10 未初始化 | ⚠️ 待处理 | 按需初始化 |
| L5 | openclaw.json 未纳入 ConfigMap | ⚠️ 待处理 | 当前 HostPath 挂载可用 |
| L6 | 知识库 RAG 功能 | ⚠️ 规划中 | 下个迭代 |

---

### 6.3 当前生产环境验证状态（REVISION 13）

```
✅ http://14.17.59.69:4888/               → customer-ui 200 + Cache-Control: no-cache
✅ http://14.17.59.69:4888/admin/         → admin-ui 200 + Cache-Control: no-cache
✅ http://14.17.59.69:4888/api/cases/     → [] HTTP 200
✅ http://14.17.59.69:4888/grafana/       → Grafana 12.4.0
✅ http://14.17.59.69:4888/openclaw/      → OpenClaw WebUI（模型 tly/glm-5，对话正常）
⚠️ Tempo: CrashLoopBackOff，链路数据未验证
```

---

## 七、RAG + AI 层架构决策实施记录（2026-03-09）

### 7.1 本次迭代目标

在上一次迭代完成 SOP 知识库数据入库（commit 2905a3c）的基础上，本次迭代目标：

1. **架构决策文档化**：将 AI 层与 RAG 层对接的架构决策整理为 ADR 文档
2. **阶段一落地**：部署 kb-service，实现外挂 RAG 端到端链路可用

---

### 7.2 架构决策（新文档 15）

新增 `docs/15_AI层RAG对接架构决策.md`，核心决策：

**背景问题**：`conversation-service` 把 openclaw（全能 Pi agent）当被动 LLM 使用，`productionclaw/TOOLS.md` 中定义的 KB 工具形同虚设。

**三阶段线性演进方案（非三选一）**：

| 阶段 | 名称 | 核心变化 | 目标时间 | 状态 |
|------|------|---------|---------|------|
| 阶段一 | 外挂 RAG | conversation-service 做 RAG 编排，openclaw 被动接收 | 本周 | ✅ 已完成 |
| 阶段二 | Agentic RAG | ProductionClaw 通过 TOOLS.md 自主调用 KB Service | 2-3 周 | ⬜ 待启动 |
| 阶段三 | 混合架构 | SOP → workspace/skills（ConfigMap 挂载），LearningClaw 自主学习闭环 | 1-2 月 | ⬜ 待启动 |

**LLM 对照组**：通过 AssistantRegistry 新增条目接入，阶段一完成后即可开始 A/B 对比（此时最公平，两者接收完全相同的外挂 RAG 上下文）。

**三阶段横向对比（8 维度）**：

| 评估维度 | 阶段一 | 阶段二 | 阶段三 |
|---------|--------|--------|--------|
| openclaw 能力利用 | ❌ 浪费 | ✅ 完整 | ✅ 最充分 |
| 实施难度 | ★ | ★★★ | ★★★★★ |
| 多跳推理 | ❌ | ✅ | ✅ |
| SOP 读取延迟 | 中（HTTP） | 中（tool-call） | 低（本地文件） |
| 自主学习闭环 | ❌ | 部分 | ✅ 完整 |
| 确定性/可调试 | ✅ 最高 | 中 | 中 |

**相关文档更新**：`01_架构设计.md`（演进计划表新增行）、`12_AI层设计.md`（§4.5 补充阶段说明）、`13_RAG设计.md`（相关文档添加引用）。

---

### 7.3 阶段一实施过程（2026-03-09）

#### 7.3.1 已完成

**① SQLAlchemy 保留字冲突 Bug 修复**
- **现象**：kb-service Pod 启动 crash，`InvalidRequestError: Attribute name 'metadata' is reserved`
- **根因**：`KBChunk` 模型字段名 `metadata` 与 SQLAlchemy Declarative API 保留属性冲突
- **修复**：Python 属性名改为 `chunk_meta`，通过 `Column("metadata", ...)` 保持 DB 列名不变
- **文件**：`backend/kb-service/app/models/chunk.py`、`app/services/ingestor.py`

**② pgvector 扩展 + KB 数据库迁移**
- postgres 镜像换为 `pgvector/pgvector:pg15`（支持 `CREATE EXTENSION vector`）
- 执行 `database/migrate_kb_v3.sql` 创建 `kb_document`、`kb_chunk`、`kb_sop_node` 等表
- pgvector 0.8.2 已成功启用

**③ SOP 节点导入成功**
- `POST /api/kb/sop/import` 导入 6 个 SOP 节点（来自 `data-pipeline/sop_skills/index.json`）
- `kb_sop_node` 表已有数据

**④ kb-service 部署成功**
```
Pod: kb-service-5bb477455c-sc4ch  1/1 Running
健康: {"status":"ok","service":"kb-service","version":"3.0.0","sop_index_size":0}
volumes: hostPath /home/node/models/bge-small-zh-v1.5 → /models/bge-small-zh-v1.5 (readOnly, 已挂载)
```

**⑤ bge-small-zh-v1.5 模型已下载到宿主机**
- 路径：`/home/node/models/bge-small-zh-v1.5/`（184MB，14 文件）
- 来源：`HF_ENDPOINT=https://hf-mirror.com` 下载

---

#### 7.3.2 卡点：embedding 服务不可用

**现象**：`POST /api/kb/ingest` 调用时 embedding 失败，SOP 文档无法入库（仅 sop_node 元数据入库成功）

**根因**：

| 路径 | 结果 | 说明 |
|------|------|------|
| `http://openclaw:18789/v1/embeddings` | ❌ 404 | openclaw 不提供 embedding 接口 |
| 外网 `https://open.bigmodel.cn` | ❌ 超时 | k3s Pod 网络无法出外网（Clash TUN 隔离） |
| 宿主机 `open.bigmodel.cn` | ✅ 200 | 宿主机可正常访问 Zhipu API |
| Pod 内本地 bge-small-zh | ❌ 无模块 | 旧镜像无 `sentence_transformers` |

**当前镜像状态**：
- 已在宿主机本地 build 新镜像 `hci-kb-service:latest`（含 sentence-transformers，体积 **12.2GB**）
- **未完成**：`docker save | sudo k3s ctr images import` 需要交互式 sudo 密码，尚未执行

---

#### 7.3.3 下一步三个可选方案

**方案 A — 导入大镜像（含 PyTorch，12GB）**
```bash
# 需要交互式 sudo 密码，预计耗时 5-10 分钟
docker save hci-kb-service:latest | sudo k3s ctr images import -
kubectl rollout restart deployment/kb-service -n hci-troubleshoot
sleep 30 && kubectl logs -n hci-troubleshoot deployment/kb-service --tail=20
```

**方案 B — 宿主机起轻量 embedding 服务（推荐）**
```bash
# 1. 安装依赖（dlvenv 已有 fastapi/uvicorn，还需加 sentence-transformers）
source /tmp/dlvenv/bin/activate
uv pip install sentence-transformers torch --index-url https://download.pytorch.org/whl/cpu

# 2. 创建 embedding server
cat > /tmp/embedding_server.py << 'EOF'
from fastapi import FastAPI
from pydantic import BaseModel
from sentence_transformers import SentenceTransformer
import uvicorn

app = FastAPI()
model = SentenceTransformer("/home/node/models/bge-small-zh-v1.5")

class EmbedRequest(BaseModel):
    model: str = "bge-small-zh-v1.5"
    input: list[str]

@app.post("/v1/embeddings")
def embed(req: EmbedRequest):
    vecs = model.encode(req.input).tolist()
    return {"data": [{"index": i, "embedding": v} for i, v in enumerate(vecs)]}

if __name__ == "__main__":
    uvicorn.run(app, host="0.0.0.0", port=18100)
EOF

# 3. 后台启动
python3 /tmp/embedding_server.py &

# 4. 修改 helm values.yaml 中：
#    ZAI_BASE_URL（在 kb-service deployment.yaml 模板中）改为 http://192.168.0.4:18100
# 5. helm upgrade
# 6. 重启 kb-service
```

**方案 C — 跳过向量，仅验证 SOP 匹配（最快）**
- 修改 `ingestor.py`，embedding 失败时继续写入（embedding=None）
- 只验证 SOP 匹配（BM25 全文搜索不依赖 embedding）

---

#### 7.3.4 阶段一验收标准

- [x] `POST /api/kb/ingest` 成功返回 `{"document_id": ..., "chunks_created": ...}`（37 篇 SOP 全部入库）
- [x] `POST /api/kb/search {"query": "虚拟机无法启动", "top_n": 3}` 返回有效 chunks（命中 5 条，含 SOP Match）
- [x] `POST /api/kb/sop/match {"query": "虚拟机无法启动"}` 命中 vm_boot_failure SOP「虚拟机无法开机」
- [x] 一次完整对话：用户提问 → `kb_sop_hit + kb_search_hit` → LLM 回复前缀「根据参考资料」
- [x] Loki 可见 `kb_sop_hit` / `kb_search_hit` 结构化日志（2026-03-10 验证通过）

**验收时间**：2026-03-10
**验收工单**：`Q2026031098324`（对话 `1bdebe56-ef17-46ef-88f8-f034fa838950`）

---

### 7.4 未提交变更（切换设备注意）

以下 4 个文件已修改但**尚未 git commit**：

```
M backend/kb-service/app/models/chunk.py
  └── KBChunk.metadata → chunk_meta（SQLAlchemy 保留字修复）

M backend/kb-service/app/services/ingestor.py
  └── metadata= → chunk_meta=（配套改动）

M deploy/helm/hci-platform/templates/kb-service/deployment.yaml
  └── 新增 BGE_MODEL_PATH env + volumeMount（bge-model）+ volumes（hostPath）

M deploy/helm/hci-platform/values.yaml
  └── kbService.enabled: false → true
```

> requirements.txt 中 sentence-transformers 行已被删除（控制镜像大小，最终方案待定）

**切换设备后第一步**：`cd /aihci/hci-troubleshoot-platform && git add -A && git commit -m "fix(kb): SQLAlchemy metadata保留字修复+BGE hostPath挂载，kbService启用"`

---

### 7.5 系统当前整体状态（2026-03-09 21:00）

**Pod 状态**（hci-troubleshoot）：
```
✅ admin-ui / api-gateway / case-service×2 / conversation-service×2 / customer-ui
✅ kb-service          Running（BGE hostPath 挂载，旧镜像无 sentence-transformers）
✅ openclaw            Running（tly/glm-5）
✅ postgres            Running（pgvector 已启用，KB 表已创建）
✅ redis / scheduler-service
❌ learningclaw        Init:ImagePullBackOff（非本次重点）
```

**宿主机资源**：
- `/home/node/models/bge-small-zh-v1.5/`：184MB 模型完整
- Docker 本地镜像 `hci-kb-service:latest`：12.2GB（含 PyTorch）**未导入 k3s**
- `/tmp/dlvenv/`：Python venv，含 fastapi/uvicorn/huggingface_hub

**Git**：HEAD `2273545`，4 文件未提交（见 7.4）

---

### 7.6 阶段一验收完成（2026-03-10）

**完成时间**：2026-03-10

阶段一「外挂 RAG」全部验收标准均通过，完整链路如下：

```
用户提问「虚拟机无法启动该怎么排查」
  ↓
conversation-service._build_system_prompt()
  ├── kb_client.sop_match()  → 命中 vm_boot_failure「虚拟机无法开机」SOP
  └── kb_client.search()     → 语义检索命中 5 条 chunks（含 vm_power_failure 汇总文档）
  ↓
4-Tier System Prompt 注入（Tier2=SOP精确 + Tier3=KB语义）
  ↓
openclaw LLM 回复：「根据参考资料，虚拟机无法启动的排查流程如下...」
  ↓
Loki 可见：kb_sop_hit(sop_title=虚拟机无法开机) + kb_search_hit(hit_count=5)
```

**KB 数据现状**：37 篇 SOP 文档（200+ chunks），涵盖 vm_power_failure / vm_boot_failure / network / node / storage 五大故障类别

**下一步（阶段二）**：ProductionClaw 通过 `TOOLS.md` 自主调用 KB Service（Agentic RAG）

---

## 八、2026-03-10 迭代实施记录（Task 25 + Task 28）

> 本次迭代在阶段一 RAG 验收通过的基础上，完成两项任务：P0 ETL 数据管道 + P1-3 Prometheus 进 K3s。

### 8.1 Task 28 — Prometheus 进 K3s（已完成）

**提交**：`969b02c`

#### 8.1.1 新增 Helm 模板

新建 `deploy/helm/hci-platform/templates/observability/prometheus.yaml`，包含：

| 资源 | 说明 |
|------|------|
| ConfigMap `prometheus-config` | Prometheus 抓取配置，使用 K8s 服务发现 + Pod 注解过滤 |
| Deployment `prometheus` | `prom/prometheus:v2.52.0`，7天 TSDB 保留 |
| Service `prometheus` | ClusterIP:9090 |
| ServiceAccount `prometheus` | Prometheus 专用 SA |
| ClusterRole `prometheus-hci` | 允许读取 pods/endpoints/services/nodes |
| ClusterRoleBinding `prometheus-hci` | 绑定 SA 和 ClusterRole |

抓取策略：通过 `prometheus.io/scrape=true` 注解自动发现，支持 `prometheus.io/port` 和 `prometheus.io/path` 覆盖。

#### 8.1.2 各服务 Pod 添加 Prometheus 注解

在 5 个业务服务的 Pod 模板 metadata.annotations 中新增：
```yaml
prometheus.io/scrape: "true"
prometheus.io/port: "<service-port>"
prometheus.io/path: "/metrics"
```

覆盖服务：`api-gateway`(8000) / `case-service`(8001) / `conversation-service`(8002) / `scheduler-service`(8003) / `kb-service`(8004)

#### 8.1.3 补全 /metrics 端点

原本只有 conversation-service 和 scheduler-service 有 `/metrics` 端点，本次为其余三个服务补全：

| 服务 | 变更 |
|------|------|
| `backend/api-gateway/app/main.py` | 新增 `GET /metrics` 端点 + `prometheus_client` 导入 |
| `backend/case-service/app/main.py` | 新增 `GET /metrics` 端点 + `prometheus_client` 导入 |
| `backend/kb-service/app/main.py` | 新增 `GET /metrics` 端点 + `prometheus_client` 导入 |

#### 8.1.4 Grafana 数据源自动 Provisioning

在 `templates/observability/configs.yaml` 的 `grafana-datasources` ConfigMap 中追加 Prometheus 数据源：
```yaml
- name: Prometheus
  uid: prometheus
  type: prometheus
  url: http://prometheus:9090
  timeInterval: 15s
```

#### 8.1.5 values.yaml 新增配置

```yaml
observability:
  prometheus:
    enabled: true
    image: prom/prometheus:v2.52.0
    resources:
      requests: { cpu: "100m", memory: "256Mi" }
      limits: { cpu: "500m", memory: "512Mi" }
```

**部署命令**（下次 helm upgrade 时自动生效）：
```bash
helm upgrade hci-platform ./deploy/helm/hci-platform \
  -n hci-troubleshoot -f deploy/helm/hci-platform/values-dev.yaml

kubectl get pods -n hci-observability | grep prometheus
# 验证：
kubectl exec -n hci-observability deploy/prometheus -- \
  wget -qO- http://conversation-service.hci-troubleshoot:8002/metrics | head -5
```

---

### 8.2 Task 25 — ETL 产品案例数据建设（代码完成，待执行）

**提交**：`f12d8ff`

#### 8.2.1 新增 6 个 ETL 脚本

| 脚本 | 功能 | 关键特性 |
|------|------|---------|
| `data-pipeline/fetcher.py` | 抓取 HTML | 指数退避重试，断点续传（`raw/_index.jsonl`），礼貌延迟 0.3s |
| `data-pipeline/converter.py` | HTML → Markdown JSON | BeautifulSoup 解析，markdownify 转换，多选择器 fallback |
| `data-pipeline/enricher.py` | 元数据补全 | 规则匹配（keywords_map.json）+ 可选 ZAI LLM 打标 |
| `data-pipeline/reviewer_cli.py` | 人工抽查 | 交互式 y/n/s/q，结果写 `reviewed.jsonl` / `rejected.jsonl` |
| `data-pipeline/ingestor.py` | 批量入库 | SHA256(source_url) 幂等，断点续传（`ingested.json`），自动打印 KB 统计 |
| `data-pipeline/pipeline.py` | 串联调度 | `--stage fetch/convert/enrich/ingest/all`，汇总报告 |

#### 8.2.2 新增 pyproject.toml 依赖

```toml
# ETL 数据管道
"beautifulsoup4>=4.12.0",
"markdownify>=0.11.6",
"tqdm>=4.66.0",
"lxml>=5.0.0",
```

#### 8.2.3 执行步骤（待有数据源时）

```bash
cd /aihci/hci-troubleshoot-platform/data-pipeline

# 1. 安装依赖
uv sync

# 2. 小批量测试（先验证 KB 可用）
export KB_URL="http://10.42.0.144:8004"
export KB_TOKEN="hci-dev-internal-token"
uv run pipeline.py --stage all --limit 50

# 3. 验证检索效果
curl -s -X POST "$KB_URL/api/kb/search" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $KB_TOKEN" \
  -d '{"query": "虚拟机磁盘IO异常排查", "top_n": 3}'

# 4. 全量入库（tmux 中后台运行）
uv run pipeline.py --stage all
```

**完成标准**：
- `kb_document` 表文档数 ≥ 1000（阶段性），最终 ≥ 6000
- 检索质量：用真实产品关键词能命中相关文档
- AI 回答中引用内容来自真实案例

---

### 8.3 当前 Git 状态（2026-03-10 迭代 2）

```
HEAD → master  f12d8ff  feat(etl): Task 25 P0 产品案例ETL数据管道
       969b02c  feat(observability): Task 28 Prometheus进K3s
       e88312d  fix(kb): 清理冗余 embedding 环境变量
       67ca425  docs: 更新详细执行计划（P0-P3，Task25-29）
```

**下一步优先任务**：Task 26/27 已完成。下一阶段重点：ETL 产品案例数据建设（Task 25）、ProductionClaw_工单路由验证、learningclaw 实际 AI 对话（需配置 API Key）。

---

### 8.4 Task 29 — Phase 4 测试覆盖 + CI/CD（已完成）

**提交**：`b71bafa`

#### 8.4.1 GitHub Actions CI（`.github/workflows/ci.yml`）

4 阶段流水线（各阶段并行/串行如下）：

| Job | 触发条件 | 内容 |
|-----|---------|------|
| `lint` | PR + push master | `ruff check backend/ tests/ data-pipeline/` |
| `unit-tests` | lint 通过后 | `pytest tests/unit/ + backend/*/tests/unit/` + codecov 覆盖率上传 |
| `integration-tests` | lint 通过后（并行）| `pytest tests/integration/ backend/scheduler-service/tests/integration/`，sidecar Redis 7 |
| `security-tests` | lint 通过后（并行）| `pytest tests/security/`（CORS 验证） |

#### 8.4.2 T4 CORS 安全测试（`tests/security/test_cors.py`）

**11 用例，全部 PASSED**

| 测试类 | 用例数 | 覆盖场景 |
|--------|--------|---------|
| `TestCORSAllowedOrigins` | 3 | 合法 Origin 200 + ACAO 头正确 |
| `TestCORSBlockedOrigins` | 3 | 非法 Origin 无 ACAO 头；无通配符；子域名拦截 |
| `TestCORSPreflightOptions` | 3 | OPTIONS preflight 通过；响应含 Allow-Methods；非法 Origin preflight 无头 |
| `TestCORSCredentials` | 2 | 合法 Origin 有 ACAC:true；`*` + credentials 违规 |

#### 8.4.3 T3 WebSocket 集成测试（`tests/integration/test_websocket.py`）

**10 用例，全部 PASSED**

| 测试类 | 用例数 | 覆盖场景 |
|--------|--------|---------|
| `TestSessionManager` | 7 | CRUD（fakeredis）：创建/查询/关闭/内存清理/并发独立 |
| `TestWebSocketEndpoint` | 3 | 缺字段报错回传；合法消息（下游 mock）；非 JSON 不崩溃 |

技术亮点：修复了 `AsyncMock(side_effect=lambda)` 导致 coroutine 未 await 的问题，改为直接将 `FakeRedis` 实例注入 `RedisManager.client`。

#### 8.4.4 T1 E2E Pod 调度测试（`tests/e2e/test_pod_scheduling.py`）

**5 用例，CI 中 skip（需实际集群）**

| 用例 | 验证内容 |
|------|---------|
| `test_api_gateway_health` | 前置健康检查 |
| `test_create_case_success` | 工单创建 201 + case_id |
| `test_list_cases_includes_new_case` | 列表包含新创建工单 |
| `test_full_lifecycle` | 创建→Pod 分配（≤15s）→对话→关闭→Pod 释放（≤10s） |
| `test_pod_released_after_case_close` | 关闭工单后 Scheduler 移除分配记录 |

执行方式：`API_GATEWAY_URL=http://<gateway>:8000 pytest tests/e2e/ -m e2e`

#### 8.4.5 T5 Locust 负载基线（`tests/load/locustfile.py`）

| 用户类型 | 权重 | 任务集 | P95 目标 |
|---------|------|--------|---------|
| `CaseworkerUser` | 70% | CRUD（工单 CRUD + 健康检查） | < 200ms |
| `KBAnalystUser` | 30% | KB 检索 + 统计 | < 200ms |

基线检查钩子：测试结束自动判断 P95 是否达标（AI 对话接口宽松至 2000ms），超标则 `process_exit_code=1`。

**执行命令**：
```bash
# 无头批量基线采集
locust -f tests/load/locustfile.py \
  --host http://localhost:8000 \
  --headless -u 50 -r 5 -t 60s \
  --csv tests/load/baseline_$(date +%Y%m%d)
```
