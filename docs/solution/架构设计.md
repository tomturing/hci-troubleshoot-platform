# HCI 智能排障平台 - 架构设计文档

## 文档信息
- **版本**: 8.2
- **作者**: Claude
- **创建日期**: 2026-02-15
- **更新日期**: 2026-05-26
- **状态**: 持续更新

---

## 变更历史

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| **8.2** | **2026-05-26** | **文档命名与阶段优化**：① Agent 对外命名统一为 OPS Agent / PAI Agent（实现类名保留在代码注释）；② 补充 S0-S6 执行层语义阶段优化说明（Triage/Investigation/Remediation/Closure）；③ 更新表数量为 18 张（sop_tree/sop_chunk 已合并入 sop_document） |
| **8.1** | **2026-05-24** | **同步远端 PR #325 agent-service 方案 B 重构**：① conversation-service 简化为对话状态机与 SSE 转发层，移除本地 knowledge_retriever/prompt_builder；② agent-service 改为 `domain/` + `adapters/agents/` + `adapters/clients/` 分层，新增 `BaseAgent`（think/act/run）与 Triage/Investigation/Remediation 语义 Agent；③ 新增 `KBClient.search_cases_with_steps()` 与案例差异诊断（CDD）协议；④ 补充 [agent基类设计.md](agent/agent基类设计.md) 和 [案例差异诊断协议.md](agent/案例差异诊断协议.md) |
| **7.8** | **2026-05-25** | **SOP 表整合（PR #326）**：① 删除 `sop_chunk` 表（死代码，embedding 从未被查询）；② `sop_tree` 1:1 合并入 `sop_document`（新增 tree_* 列，`GET /api/sop/{id}/tree` 端点不变）；③ KBClient.search_cases_with_steps() API 路径修正 `/cases/search` → `/kbd/search` |
| **7.7** | **2026-05-24** | **agent-service 方案 B 架构重构（PR #325）**：① conversation-service 简化为纯状态机层（移除 knowledge_retriever/prompt_builder）；② agent-service 新增分层 Agent 架构（TriageAgent → InvestigationAgent → RemediationAgent）；③ 新增 KBClient.search_cases_with_steps() API 支持案例差异诊断；④ 新增 BaseAgent 最简抽象基类（think/act/run 循环）；详见 [agent基类设计.md](agent/agent基类设计.md) |
| **7.6** | **2026-05-21** | **配置迁移至 dashscope 网关（PR #314）**：移除本地 OpenClaw/ProductionClaw/LearningClaw Pod 池，统一使用 dashscope 网关调用 GLM-5；环境变量重命名 `OPENCLAW_*` → `LLM_*`；更新 ai_client.py 使用新配置 |
| **7.5** | **2026-05-21** | **共享模块架构重组（PR #312）**：①新增 `shared/clients/` 子包（ai_client/scheduler_client/kb_client 从 agent-service/conversation-service 迁移）；②新增 `shared/models/conversation.py`（统一 Conversation Model 定义，删除 case-service/conversation-service 本地副本）；③删除残留文件（agent-service 的 knowledge_retriever/prompt_builder，conversation-service 的 confirm_service）；④清理 conversation_service.py 残留代码（send_message_react_stream 方法、agent_mode_available 属性等）；详见 [目录结构.md](目录结构.md) |
| **7.4** | **2026-05-20** | **Docker CACHEBUST 修复补强（PR #307）**：PR #306 的 `ARG CACHEBUST` 定义不会真正破坏 Docker 缓存，补强为在 `COPY shared` 前添加 `RUN echo CACHEBUST` 命令 |
| **7.3** | **2026-05-20** | **Docker CACHEBUST 参数修复 shared 目录缓存问题（PR #306）**：所有后端服务 Dockerfile 新增 `CACHEBUST` ARG，CI 构建时传入 `GITHUB_SHA`，强制每次构建都重新 COPY shared 目录 |
| **6.9** | **2026-05-07** | **助手选择器 Bug 修复 + ops-agent 注册**：修复 scheduler-service 可用性判断 key 不匹配（`idle_count` → `idle`）；ops-agent 注册到助手选择器（直连模式）；conversation-service 改用统一配置源；改进直连模式判断逻辑（增加 base_url 检查），详见 [事件文档](events/2026-05-07-助手选择器Bug修复.md) |
| **7.0** | **2026-05-10** | **AI 层架构全面修正（Ports & Adapters + 三大助手族 + ACP REST）**：①修正 Mermaid 架构图和 ASCII 图，新增 ops-agent-service（单实例，端口 8006，走 ACP REST 协议）；②引入 BrainPort/BrainRouter 抽象层（Ports & Adapters），明确 HTPBrainAdapter（openclaw+direct_model）和 OpsAgentBrainAdapter（ops-agent）两个管道；③新增三大助手族对照（assistant_family：openclaw/direct_model/ops_agent）及路由逻辑；④更新对话时序图为 BrainRouter 双路版本；⑤修正 NaboBot Pool 描述（已废弃，现为 ops-agent ACP REST 集成）；⑥记录 case.assistant_type 演进路径（应从最新 conversation.assistant_type 恢复，case 字段待删除）。 |

---


