---
status: active
category: solution
audience: developer
last_updated: 2026-06-13
owner: team
---

# 平台内置硬编码治理审计

## 一、背景

HCI 智能排障平台的目标不是把某个具体排障案例写进代码，而是提供一套可运行、可插拔、可热生效的 Agent 能力平台。KBD、SOP、工具、技能和 Prompt 应由数据驱动；代码只保留稳定的执行内核、协议适配、安全边界和可观测性。

本次审查 PR 461 时发现，平台已有管理页面和数据库表，但多个运行时链路仍存在内置注册表、固定分支、启动期缓存和业务规则硬编码。其直接后果是：管理员在页面上新增或修改资源后，Agent 运行时并不一定使用新资源，甚至会继续走旧代码路径。

## 二、第一性原理

平台代码应回答“如何安全执行”，业务数据应回答“执行什么、何时执行、如何解释”。

| 层次 | 应放在代码中 | 应放在数据中 |
|---|---|---|
| 内核协议 | HTTP/SSE/Redis/DB 访问、鉴权、审计、trace_id、事务边界 | 不适用 |
| 安全底座 | 命令净化器、确认门禁、权限模型、沙箱边界、失败语义 | 风险规则、允许容器、工具启停、策略参数 |
| 能力定义 | 通用解释器、schema 校验器、资源加载器 | Tool/Skill/Prompt/SOP/KBD 定义、版本、状态 |
| 业务规则 | 不应写死具体 SOP 规则 | 告警解析规则、厂商寿命规则、变量派生规则、分类知识 |
| 运行状态 | 状态机实现 | execution/context/fact/audit 记录 |

判断某段代码是否属于不合理硬编码，可以问三个问题：

1. 运维人员是否希望不发版就修改它？
2. 它是否包含某个具体业务场景、SOP、工具名、告警类型或厂商规则？
3. 它是否已经在数据库管理页面存在对应资源，但运行时没有读取？

任一为“是”，就应优先下沉到数据驱动能力面。

## 三、硬编码问题清单

| 编号 | 模块 | 硬编码点 | 现状依据 | 风险 | 治理方向 | 优先级 |
|---|---|---|---|---|---|---|
| HC-01 | Skill | Python `_SKILL_REGISTRY` 作为 `skill_call` 唯一执行源 | `backend/agent-service/app/skills/registry.py`；`variable_pool/engine.py` 调用 `execute_skill()` | 技能管理页新增 Skill 不生效；`skill:hci-alert-parsing` 无法运行 | 引入数据库动态 Skill 运行时，Python 注册表仅保留内核 fallback 或迁移期兼容 | P0 |
| HC-02 | Skill | `disk_vendor_lifetime` 厂商寿命规则写在 Python | `backend/agent-service/app/skills/registry.py` | 业务知识更新需发版；违反 Skill 数据化设计 | 迁移为数据库 Skill + 结构化输出，或规则资源 | P0 |
| HC-03 | Env | `_resolve_env_variable` 写死 `node_ip/disk_sn/target/host` 解析 | `backend/conversation-service/app/routes/sop_execution.py` | 多告警场景锚定错误；业务解析继续膨胀 | env 只做确定性字段直取，复杂解析交给动态 Skill/规则 | P0 |
| HC-04 | Tool | `TOOL_REGISTRY` 仅启动时从 DB 加载 | `backend/agent-service/app/adapters/agents/htp/tool_registry.py` | 工具管理页修改后不热生效 | 增加版本感知/TTL/按次加载/刷新接口 | P0 |
| HC-05 | Tool | SCP 工具名到 Python 方法 dispatch 固定 | `backend/agent-service/app/adapters/clients/scp_client.py` | 新增 SCP 工具只有定义，没有执行能力 | 引入 `http_request` 类通用工具后端，由 DB 声明 method/path/query/body/output_path | P1 |
| HC-06 | Tool | `BridgeRelayExecutor` 特判 `get_failed_tasks` 拼命令 | `backend/agent-service/app/tools/acli/executor.py` | 命令模板双写；工具定义不是唯一事实源 | 全部前置查询迁回 `usage_template` + 参数模板，不在执行器按工具名分支 | P1 |
| HC-07 | Tool | 容器枚举写死 | `backend/agent-service/app/tools/acli/container_exec.py`、工具校验路由 | 新容器需改代码；管理页 schema 与执行器可能漂移 | `execution_boundary`/container catalog 数据化，执行器只验证动态 catalog | P1 |
| HC-08 | Tool | 风险规则正则写死 | `backend/agent-service/app/tools/acli/classifier.py` | 安全策略调整需发版 | `risk_rule` 数据表，支持优先级、正则、risk、scope、启停；代码保留 fail-closed 默认 | P1 |
| HC-09 | Tool | 语义校验依赖本地 acli catalog 文件 | `backend/agent-service/app/tools/acli/semantic_validator.py` | acli 能力更新不热生效 | acli catalog 数据化，支持采集、版本、环境维度 | P2 |
| HC-10 | SOP | SOP 导航工具集合和写操作工具集合写死 | `backend/agent-service/app/adapters/agents/htp/sop_tools.py` | 新 SOP 工具/风险策略需要改代码 | SOP 工具归入统一 tool registry，幂等规则由工具元数据或风险策略声明 | P1 |
| HC-11 | Prompt | Prompt 名称和占位符契约写在 Agent 代码 | `StrictPromptLoader.load_and_validate(..., prompt_name, expected_placeholders)` 调用点 | Prompt 内容可热改，但编排关系不可热改 | 建立 prompt slot/contract 表；代码按阶段读取 slot，不直接写死具体 name | P2 |
| HC-12 | Prompt | 单测 fallback 模板内置大量 Prompt 文本 | `backend/shared/utils/prompt_loader.py` | 测试常量与 DB seed 漂移 | 测试使用 fixture/seed 快照，避免生产逻辑 fallback | P2 |
| HC-13 | Triage | 分类 code 正则、候选数量、缓存 TTL 写死 | `backend/agent-service/app/adapters/agents/htp/triage_agent.py` | 分类体系变更受限；热更新延迟不可控 | 配置化分类约束和 cache policy | P2 |
| HC-14 | Hallucination | 默认工具清单写死 | `backend/agent-service/app/services/hallucination_detector.py` | 工具表为空或刷新失败时误判 | 幻觉检测只接受当前 registry 快照，缺失时 fail-loud | P2 |
| HC-15 | SOP 变量 | acquisition strategy 只支持固定字符串，缺少 provider 插件模型 | `backend/agent-service/app/memory/variable_pool/engine.py` | 新变量来源只能改代码 | 引入 `VariableProvider` 注册协议：env/tool/skill/derived/user_confirm/user_input | P0 |
| HC-16 | 脚本 | PR 内新增 `revert_*.sql` 回滚脚本处理未合并错误 | `scripts/dev/revert_is_sys_disk_skill.sql` | 未合并代码应直接修正，回滚脚本污染历史 | 删除回滚脚本，数据修复脚本只保留最终态，生产回滚另走迁移策略 | P0 |

## 四、允许保留在代码中的内核能力

不是所有固定逻辑都应消失。以下内容可以保留在代码中，但必须有清晰边界：

| 类型 | 可保留原因 | 约束 |
|---|---|---|
| 协议适配器 | 外部系统协议本身稳定，例如 Redis/SSE/HTTP 客户端 | 不包含具体业务工具名 |
| 安全 fail-closed | 数据缺失时必须保守拒绝 | 拒绝规则可配置，默认拒绝留在代码 |
| 通用解释器 | JSON Schema 校验、模板渲染、表达式求值器 | 解释器通用，规则来自 DB |
| 审计与可观测性 | trace_id、exec_id、审计落库、指标 | 所有动态资源调用都必须透传资源版本 |
| 数据访问仓储 | CRUD、事务、锁、缓存实现 | 不包含业务资源内容 |

## 五、治理原则

1. 单一事实源：每类资源只有一个权威定义表，代码不得重复维护同一份业务定义。
2. 运行时读取：管理页修改后，Agent 必须通过统一加载器读取最新发布版本，不能只在启动时加载。
3. 强校验：资源保存、发布、运行前都要校验引用完整性，缺失 tool/skill/prompt 时 fail-loud。
4. 可观测：每次 Agent 使用动态资源时记录 resource_type、resource_name、version、revision、trace_id、exec_id。
5. 可回滚：资源版本化，不用脚本回滚未合并 PR 的错误状态；生产回滚通过激活旧版本完成。
6. 微内核：平台内核只提供通用 provider、runner、validator、cache、audit；业务能力通过数据资源扩展。

## 六、第一个 PR 范围

第一个 PR 只做治理基线和明显错误清理，不实现完整动态运行时。由于 PR 461 尚未合并主线，本轮不通过 `revert_*.sql` 回滚错误改动，而是在新分支中直接不带入这些错误代码与脚本，确保主线只接收可审计的治理文档。

### 必做

- 删除未合并 PR 中的回滚脚本和错误数据修复脚本。
- 保留并更新 `SOP变量设计分析与数据流诊断.md`，把 `node_ip` 的长期方案纠正为动态 `hci-alert-parsing`。
- 新增本文档，明确硬编码清单、治理边界和后续 PR 拆分。
- 新增 `docs/solution/agent/五大动态资源统一运行时设计.md`，作为 PR-2 的核心主设计输入。
- 不继续新增 `acli_system_smartctl` 这类专用工具绕过通用执行器。

### 不做

- 不在本 PR 内实现动态 Skill 运行时。
- 不在本 PR 内改数据库 schema。
- 不在本 PR 内重写工具执行器。
- 不在本 PR 内修改前端交互。
- 不在本 PR 内提交旧 PR 的反向补丁；新分支从最新 `origin/main` 创建，净差异只允许三份文档。

## 七、验收标准

- 相对主线净差异只包含文档类变更。
- 文档明确列出 HC-01 到 HC-16 的硬编码点、风险、治理方向和优先级。
- 文档明确区分允许保留的内核逻辑与必须数据化的业务能力。
- 后续 PR 边界清晰：PR-2 实现统一动态资源运行时，PR-3 回到 SOP 变量方案优化。
