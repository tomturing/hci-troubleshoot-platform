---
status: active
category: solution
audience: developer
last_updated: 2026-04-21
version: v5.2.1
owner: team
---

# HCI 智能排障平台 - 知识库设计

> **文档边界说明**
> - **本文档范围**：知识库基础设施——数据存储 Schema、检索引擎、SOP/KBD 代入方法、知识注入机制。
> - **不在本文档**：AI Agent 的行为规范、LearningClaw/ProductionClaw 架构、对话上下文组装逻辑。
> - **相关文档**：[../ai-assistant/AI助手设计.md](../ai-assistant/AI助手设计.md) — AI 层如何消费知识库。

---

## 变更历史

| 日期 | 版本 | 变更内容 | 关联事件文档 |
|------|------|---------|------------|
| 2026-04-21 | v5.2.1 | §7.1 执行步骤重构为完整CLI使用手册：① CLI命令总览与子命令列表；② --resume/--failed-only/--override/--force 参数详解；③ 组合用法示例（5个典型场景）；④ 日志与进度查看方式；⑤ 状态流转与幂等行为说明；⑥ 导入后操作指引 | — |
| 2026-04-20 | v5.2 | Pipeline 日志持久化与进度追踪：① 新增 `progress.py` 模块（进度 JSON 追踪）；② `run.py` 双 Handler 日志配置（终端+文件）；③ `--resume`/`--failed-only` CLI 参数；④ `pipeline.py` 集成进度追踪；⑤ `fetcher.py`/`image_proc.py` 失败检测函数；详见 §7.1.1 | [2026-04-20-KBD-Pipeline日志与进度追踪改进.md](../../events/2026-04-20-KBD-Pipeline日志与进度追踪改进.md) |
| 2026-04-19 | v5.1 | 分类管理增强：① `category_repo.py` SQL SELECT 添加 `created_at` 字段修复 to_dict 返回 None；② 分类查询新增 `published_kbd_count`/`published_sop_count` 子查询统计；③ 意图识别 `classify.py` 过滤 `is_active=TRUE` 禁用分类；④ 新增 `GET /api/admin/kbd/{kbd_id}` 单条 KBD 详情接口；⑤ `sop_document` 新增 `(category_id, status)` 索引提升统计性能 | — |
| 2026-04-17 | v5.0 | 截图语义化重构：废弃双LLM路线（OCR照录+分析LLM），改为 Vision LLM 单次调用+文档上下文注入；新增 DESCRIPTION 字段（语义段落，供 RAG 召回）；TYPE 改由规则引擎本地判断；删除 analyzer.py / ocr.py；新增图片压缩预处理（>500KB 自动压缩避免超时）；KbdReviewView.vue 展示语义描述替换 KEY+TIPS；kimi-k2.5 作为 Vision 模型；详见 [图片识别.md](./图片识别.md) | — |
| 2026-04-17 | v4.10 | KBD Pipeline 新增 override + override_status 参数支持：① `--override` 强制覆盖已存在记录（默认幂等跳过）；② `--override-status` 状态过滤（默认 draft，支持 'all' 无限制或指定状态列表）；③ Importer 自动 port-forward 检测（kb-service ClusterIP 服务本地访问）；④ CLI 新增 `--force-fetch`（Stage 1 强制重新抓取）；⑤ API 返回 action 字段（created/skipped/overridden）；⑥ stats 统计改为 skipped/overridden（废弃 idempotent/updated） | [2026-04-17-KBD-Pipeline-配置修复及自动port-forward](../events/2026-04-17-KBD-Pipeline-配置修复及自动port-forward.md) |
| 2026-04-15 | v4.9 | SOP 发布函数 `approve_sop_document` 改为三段式事务：短事务查询 → 无事务 embedding 生成（释放 DB 连接，避免长时间占用超时）→ 短事务批量写入；外层 `try-except Exception` 将未捕获异常转为带可读 detail 的 HTTPException（修复大文档发布时前端只显示 "HTTP 500" 问题） | [2026-04-14-sop-publish-500-button-layout-fix](../events/2026-04-14-sop-publish-500-button-layout-fix.md) |
| 2026-04-13 | v4.8 | Vision stage 多后端 OCR 架构（EasyOCR/PaddleOCR + Vision LLM 兜底）：① dev 环境自动选 EasyOCR（WSL2 兼容，无 OneDNN 依赖），staging/prod 自动选 PaddleOCR（原生 Linux 高精度）；② 本地 OCR 失败/返回空时降级到 Vision LLM（qwen3.5-plus），带指数退避重试（最多3次，2s/4s/8s）；③ 分析 LLM（qwen3-max）同步加入重试机制；④ 环境通过 `kubectl get ns argocd -o jsonpath='{.metadata.labels.hci\.env\.role}'` 自动检测，失败时降级 `vision_only`；⑤ `config.py` 新增 `KBD_ENV`/`OCR_BACKEND`/`LLM_MAX_RETRIES`/`LLM_RETRY_BACKOFF` 及 `ocr_backend_effective` 属性；⑥ `ocr.py` 新增 `extract_text_easyocr()` 后端，共用坐标合并逻辑 | [2026-04-13-kbd-screenshot-ocr-redesign](../events/2026-04-13-kbd-screenshot-ocr-redesign.md) |
| 2026-04-12 | v4.7 | Vision prompt 升级（Plan A）：① 新增字段 0「截图背景颜色（黑色/白色/其他）」，强制规定输出格式（numbered bold fields + bullet）禁止 JSON/Markdown 标题；② `image_proc.py` 新增 `_normalize_vision_output()` 将意外 JSON 响应归一化为编号字段格式；③ 前端 `detectScreenshotType()` 改为多维度判断（bgColor + keywords），提升分类准确率（白色背景任务列表不再被误判为日志截图）；④ `parseScreenshotBlock()` 支持从 intro 行提取字段0 | — |
| 2026-04-12 | v4.6 | KBD 条目详情 UI 重构：① 确认分类改为分类基线 select（防止超出基线）；② 重写 Markdown 渲染器（修复 ol/ul 状态混乱 + 缩进子列表）；③ 截图说明识别为 5 类（告警/终端/任务/日志/其他）+ accordion 卡片样式；④ 详情弹窗支持内联编辑原文；补充 §7.1 完整调用链、流程图、时序图 | — |
| 2026-04-12 | v4.5 | 修复 classify stage 全链路 5 个 bug（见 §7.1 故障排除）；Helm template 新增 `kbService.llmBaseUrl`/`kbService.llmModel` 可覆盖配置；LLM 后端从集群内 openclaw proxy 切换为 ZhipuAI 直连（`glm-4-flash`）；完整 pipeline（case 34977）端到端打通 | — |
| 2026-04-12 | v4.4 | 修复 KBD pipeline 三项 bug：① `image_proc.py` 去除 `kbd_image` 表依赖，改为纯文件系统操作（写 `img_N.desc.txt`）；② `classifier.py`/`run.py`/`pipeline.py` 修正 `kbd_entry.case_id` → `support_id`；③ `classify.py` 修复 `path_labels` 重复 JSON 解析（JSONB 已自动解析）。引入 KBD pipeline 完整运行验证（案例 34977：fetch+vision+import 均成功） | — |
| 2026-04-11 | v4.3 | 重写 §7 知识导入方法：§7.1 KBD 补充 `--force` 参数、幂等行为对照表、分类 stage 只补空白条目说明；§7 前置操作统一补充 `--address 127.0.0.1` 和 `.env` 格式；版本号更新 | — |
| 2026-04-11 | v4.2 | 修复 sop_ingest.py `source_id` 幂等逻辑：同名文件内容更新时改为 upsert（更新内容+重建 chunks+重置 draft），而非静默跳过；更新 §7.2 幂等行为对照表 | — |
| 2026-04-11 | v4.1 | 修复三项 kb-service 问题：① `python-docx` 加入 requirements.txt（UI 上传 .docx 报 503 修复）；② `EMBEDDING_DIM` 512→1536（与 DB Vector(1536) 一致，修复发布后 0 向量）；③ `ZAI_API_KEY` 注入 hci-secrets；④ `sop_ingest.py` 幂等逻辑：hash 命中时若 `category_id` 不同则自动更新；重写 §7.2 SOP 导入方法（补充 K3s 步骤、category_id 更新行为、docx_hash 说明） | — |
| 2026-04-11 | v4.0 | 新增 SOP 管理 UI（SopManageView.vue）及对应 api-gateway 代理路由（`/api/v1/sop`）；KBD 管理新增编辑（`PATCH /api/v1/kbd/{id}`）和重新发布（`POST /api/v1/kbd/{id}/republish`）功能；后端新增 SOP 列表（`GET /api/admin/sop`）、SOP 状态更新（`PATCH /api/admin/sop/{id}`）、KBD 编辑（`PATCH /api/admin/kbd/{id}`）、KBD 重发布（`POST /api/admin/kbd/{id}/republish`）接口 | — |
| 2026-04-11 | v3.9 | Admin 「KBD审核」重命名为「KBD管理」；重写第七章导入操作指南（按实际运行步骤调整，删除不准确操作） | — |
| 2026-04-10 | v3.8 | 修复 import_from_yaml 与 category_baseline.yaml 格式不匹配：新增 _parse_baseline_yaml 四阶段解析（L1域节点+中间层+叶节点），level 从 path 长度推断，两阶段 upsert 建立 parent_id 树形关系 | [2026-04-10-category-yaml-import-format-fix](../events/2026-04-10-category-yaml-import-format-fix.md) |
| 2026-04-10 | v3.7 | 重构 api-gateway 为透明代理：移除 python-multipart 依赖，实现真正的请求透传 | [2026-04-10-api-gateway-transparent-proxy](../events/2026-04-10-api-gateway-transparent-proxy.md) |
| 2026-04-10 | v3.6 | 补充 api-gateway 依赖：添加 python-multipart 支持 multipart 表单解析（临时方案，已在 v3.7 重构移除） | — |
| 2026-04-10 | v3.5 | 补充 kb-service 依赖：添加 python-multipart 支持文件上传接口 | — |
| 2026-04-10 | v3.4 | 修复分类 YAML 导入接口：正确处理 multipart/form-data 文件上传 | [2026-04-09-kb-service-import-fix](../events/2026-04-09-kb-service-import-fix.md) |
| 2026-04-07 | v3.3 | 删除 classify.py 死代码 `GET /categories` 端点（解除与 categories.py 同路径冲突）；API 列表同步更正 | [Admin路由冲突与Token鉴权修复方案](../events/2026-04-07-Admin路由冲突与Token鉴权修复方案.md) |
| 2026-04-07 | v3.2 | KBD 审核后端实现：新增 `GET /api/admin/kbd/pending` 列表查询 + `PATCH /{id}/reject` 拒绝端点；api-gateway 新增 kbd_router 代理 | [Admin管理台API路由修复方案](../events/2026-04-07-Admin管理台API路由修复方案.md) |
| 2026-04-07 | v3.1 | 补充 SOP/KBD 导入方法、分类树导入方法 | — |
| 2026-04-07 | v3.0 | 重构为现行全量设计，归档 data-pipeline 和 PageIndex 方案 | [2026-03-05-DataPipeline-ETL设计方案.md](./events/2026-03-05-DataPipeline-ETL设计方案.md)、[2026-03-05-PageIndex对比分析.md](./events/2026-03-05-PageIndex对比分析.md) |
| 2026-04-02 | v2.0 | SOP 语义路由 → category_id 直查路由 | [2026-04-02-S0意图识别与分类基线重构方案.md](../events/2026-04-02-S0意图识别与分类基线重构方案.md) |
| 2026-03-23 | v1.5 | 单轨 RAG → 双轨三级 Fallback | — |
| 2026-03-05 | v1.0 | 初版 | — |

---

## 一、概述

### 1.1 系统目标

为 HCI 智能排障平台构建知识库检索增强生成（RAG）系统，实现：

1. **~7000 KBD 案例知识条目**的结构化存储与智能检索
2. **~10 篇 SOP 排障手册**的精确路由与步骤引导
3. **对话场景下的知识注入**，让 AI 助手基于真实案例和 SOP 生成精准答案

### 1.2 核心概念

| 概念 | 说明 |
|------|------|
| **KBD（Knowledge Base Document）** | 历史故障案例，陈述性知识（What-happened），从 `root_cause` 提取假设 |
| **SOP（Standard Operating Procedure）** | 排障手册，程序性知识（How-to），按决策树遍历执行 |
| **category_id** | 分类编码（如 `虚拟机-003`），S0 阶段确认，用于精确路由 |

### 1.3 双轨知识架构

```
用户描述故障（S0 意图识别 → category_id 确认）
        │
        ▼ category_id 已知
[第1轨：SOP 直查] sop_document WHERE category_id = X
        │
        ├─ 命中 ──→ 加载 sop_chunk → SOP 步骤引导 → ✅ 返回
        │
        └─ 未命中
                │
                ▼
        [第2轨：KBD 检索] kbd_entry WHERE category_id = X
                │           BM25(tsv) + 向量(embedding) → RRF → top5
                │
                ├─ 命中 ──→ 注入「历史案例参考」
                │
                └─ 未命中 → 人工兜底
```

---

## 二、数据存储设计

### 2.1 存储分层

| 数据层 | Source of Truth | 说明 |
|--------|----------------|------|
| kbd_entry | DB | KBD 知识条目，整条 embedding，无分块 |
| sop_document | DB | SOP 文档元信息 |
| sop_chunk | DB | SOP 分块，向量 + 全文索引 |
| kb_category | DB | 分类树 |
| sop_skills/ | 文件系统 | SOP 源文件（keywords_map.json + chapters/） |

### 2.2 数据库 Schema

#### kbd_entry — KBD 知识条目

```sql
CREATE TABLE kbd_entry (
    id                  BIGSERIAL PRIMARY KEY,
    support_id          VARCHAR(20) UNIQUE NOT NULL,    -- 深信服案例ID（幂等键）
    support_url         TEXT,                           -- 原始案例 URL
    title               TEXT NOT NULL,                  -- 案例标题
    content_md          TEXT,                           -- 结构化 Markdown
    metadata            JSONB DEFAULT '{}',             -- 补充元数据

    -- 分类字段
    category_id         VARCHAR(32),                    -- 人工确认分类
    ai_category_id      VARCHAR(32),                    -- AI 分类建议
    ai_category_conf    FLOAT,                          -- 分类置信度
    ai_category_reason  TEXT,                           -- 分类理由

    -- 检索字段（published 时生成）
    embedding           vector(1536),                   -- 语义向量
    tsv                 tsvector,                       -- BM25 全文索引

    -- 状态机
    status              VARCHAR(20) NOT NULL DEFAULT 'draft',  -- draft/published/archived/rejected
    reviewer_id         INTEGER,
    reviewed_at         TIMESTAMP WITH TIME ZONE,
    review_note         TEXT,
    published_at        TIMESTAMP WITH TIME ZONE,

    created_at          TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    updated_at          TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

CREATE INDEX idx_kbd_entry_status ON kbd_entry(status);
CREATE INDEX idx_kbd_entry_category ON kbd_entry(category_id);
CREATE INDEX idx_kbd_entry_embedding ON kbd_entry USING ivfflat (embedding vector_cosine_ops) WITH (lists = 100);
CREATE INDEX idx_kbd_entry_tsv ON kbd_entry USING GIN (tsv);
```

#### sop_document — SOP 文档

```sql
CREATE TABLE sop_document (
    id              SERIAL PRIMARY KEY,
    source_id       VARCHAR(100) UNIQUE,            -- 幂等键，如 sop-vm-start-failure
    category_id     VARCHAR(32),                    -- 关联 KB 分类
    title           VARCHAR(500) NOT NULL,          -- SOP 标题
    content_md      TEXT NOT NULL,                  -- 完整 SOP Markdown
    docx_hash       VARCHAR(64),                    -- 源文件哈希（幂等去重）
    status          VARCHAR(20) DEFAULT 'draft',    -- draft/published/archived
    reviewer_id     INTEGER,
    reviewed_at     TIMESTAMP WITH TIME ZONE,
    published_at    TIMESTAMP WITH TIME ZONE,
    created_at      TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    updated_at      TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

CREATE INDEX idx_sop_document_category ON sop_document(category_id);
```

#### sop_chunk — SOP 分块检索

```sql
CREATE TABLE sop_chunk (
    id              SERIAL PRIMARY KEY,
    document_id     INTEGER REFERENCES sop_document(id) ON DELETE CASCADE,
    chunk_index     SMALLINT NOT NULL,              -- 分块序号（0-based）
    chapter_title   VARCHAR(200),                   -- 章节标题
    content         TEXT NOT NULL,                  -- 分块内容
    embedding       vector(1536),                   -- 语义向量（审核时生成）
    tsv             tsvector,                       -- 全文检索向量
    created_at      TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

CREATE INDEX idx_sop_chunk_document ON sop_chunk(document_id);
CREATE INDEX idx_sop_chunk_embedding ON sop_chunk USING ivfflat (embedding vector_cosine_ops) WITH (lists = 100);
CREATE INDEX idx_sop_chunk_tsv ON sop_chunk USING GIN (tsv);
```

#### kb_category — 分类树

```sql
CREATE TABLE kb_category (
    id              SERIAL PRIMARY KEY,
    parent_id       INTEGER REFERENCES kb_category(id),
    code            VARCHAR(32) UNIQUE NOT NULL,    -- 分类编码，如 "虚拟机-003"
    name            VARCHAR(100) NOT NULL,          -- 分类名称
    level           SMALLINT NOT NULL,              -- 1=L1, 2=L2, 3=L3, 4=L4
    keywords        TEXT[],                         -- 关键字
    created_at      TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

CREATE INDEX idx_kb_category_parent ON kb_category(parent_id);
CREATE INDEX idx_kb_category_code ON kb_category(code);
```

---

## 三、SOP 代入方法

### 3.1 SOP 存储规范

| 存储单元 | 说明 |
|---------|------|
| `sop_document` | SOP 文档元信息 + 完整内容 |
| `sop_chunk` | 按章节拆分，支持向量 + 全文检索 |
| `sop_skills/` | 文件系统源文件（keywords_map.json + chapters/） |

### 3.2 SOP 路由机制

**两种路由方式：**

#### 方式一：category_id 直查（O(1)，S1+ 推荐）

```python
# S0 阶段已确认 category_id，S1+ 直接查 SOP
async def get_sop_by_category(category_id: str) -> SopDocument | None:
    async with session() as s:
        result = await s.execute(
            select(SopDocument).where(
                SopDocument.category_id == category_id,
                SopDocument.status == "published"
            )
        )
        return result.scalar_one_or_none()
```

#### 方式二：关键字精确匹配（S0 阶段）

```python
# 从 sop_skills/keywords_map.json 加载关键字索引
matcher = SopMatcher(sop_skills_dir="/app/sop_skills")
await matcher.load()

# 匹配用户问题中的关键字
result = matcher.match(user_query)
# 返回: SopMatchResult(skill_id, node_name, matched_keyword, content, file_path)
```

### 3.3 SOP 注入标签

```
【SOP 排障流程 | 来源: {sop_document.title}】
{章节内容}
```

---

## 四、KBD 代入方法

### 4.1 KBD 存储规范

- **存储单元**：`kbd_entry`（整条 embedding，无分块）
- **检索字段**：`embedding`（向量）+ `tsv`（BM25），RRF 融合
- **必须字段**：`title`、`content_md`、`category_id`、`embedding`、`tsv`

### 4.2 KBD 检索流程

```
POST /api/kb/search
{
  "query": "虚拟机开机失败",
  "top_n": 5,
  "category_l1": "虚拟机",        # 可选，分类过滤
  "category_l2": "开机失败"       # 可选
}
```

**检索流程：**

1. **BM25 检索**：jieba 分词 → tsquery → tsvector 全文匹配 → top-20
2. **向量检索**：embed(query) → pgvector 余弦相似度 → top-20
3. **RRF 融合**：score = Σ 1/(k + rank_i)，k=60
4. **返回 top-N**

### 4.3 KBD 注入标签

```
【历史案例参考 | 案例 #{support_id}】
{案例内容}
```

---

## 五、知识注入机制

### 5.1 四层 Prompt 组装

Conversation Service 在每轮对话时组装 messages[]：

```python
messages = [
    # Tier 1: 系统角色
    {"role": "system", "content": AGENTS_MD_CONTENT},

    # Tier 2: SOP 知识（可选，命中时注入）
    {"role": "system", "content": f"【SOP 排障流程】\n{sop_content}"},

    # Tier 3: KBD 知识（可选，检索结果）
    {"role": "system", "content": f"【历史案例参考】\n{kbd_chunks}"},

    # Tier 4: 工单上下文
    {"role": "system", "content": f"工单ID: {case_id}\n工程师: {engineer}"},

    # 历史对话（最近 20 条）
    {"role": "user", "content": "..."},
    {"role": "assistant", "content": "..."},

    # 当前消息
    {"role": "user", "content": current_message}
]
```

### 5.2 知识注入时序

```
S0 阶段（意图识别）:
  1. 用户描述故障
  2. 调用 POST /api/kb/classify → 确认 category_id
  3. 调用 POST /api/kb/sop/match → SOP 关键字匹配
  4. 不触发 KBD 检索（S0 专职分类）

S1+ 阶段（诊断执行）:
  1. 用 category_id 直查 SOP → 命中则注入 Tier 2
  2. 用 category_id 过滤 + query 检索 KBD → top-5 注入 Tier 3
  3. 组装完整 messages[] → 发给 LLM
```

---

## 六、KB Service API

### 6.1 检索接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/kb/search` | POST | 混合检索（SOP + KBD） |
| `/api/kb/sop/match` | POST | 仅 SOP 关键字匹配 |
| `/api/kb/classify` | POST | LLM 分类器，返回 category_id |
| `/api/kb/classify/intent` | POST | 对话意图识别（conversation-service 调用） |

> **注**：v3.3 删除了 classify.py 中的 `GET /categories` 端点（零调用者，与 categories.py 路由冲突）。分类树查询统一由 `/api/kb/categories` GET（categories.py）提供。

### 6.2 摄入接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/kb/kbd/ingest` | POST | 导入 KBD 知识条目（深信服案例） |
| `/api/sop/ingest` | POST | 导入 SOP 文档（自动分块） |
| `/api/kb/sop/import` | POST | 批量导入 SOP 节点（kb_sop_node 表） |
| `/api/kb/categories` | GET/POST | 分类树管理 |

### 6.3 管理接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/kb/documents` | GET | 文档列表（Admin） |
| `/api/kb/documents/{id}` | PATCH | 审核/发布/下线 |

---

## 七、知识导入方法

> **前置操作（K3s 环境统一步骤）**
>
> 1. **开启端口转发**（每次操作前执行，导入脚本直连 kb-service:8004）：
>    ```bash
>    kubectl port-forward svc/kb-service -n hci-dev 8004:8004 --address 127.0.0.1 &
>    ```
> 2. **脚本配置文件**（dev 环境已配置好，无需修改）：
>    ```
>    # scripts/kbd/.env
>    KB_SERVICE_URL=http://localhost:8004
>    INTERNAL_API_TOKEN=dev-internalapi-api-token-2026
>    ```
> 3. **在项目根目录下执行所有命令**。

### 7.1 KBD 导入方法

KBD 导入是完整的 ETL 流水线：抓取深信服案例 → 图片 OCR → AI 分类 → 写入 DB。

#### KBD Pipeline 整体流程图

```
┌─────────────────────────────── KBD ETL Pipeline ────────────────────────────────┐
│                                                                                  │
│  本地 CLI (scripts/kbd/run.py)                                                   │
│                                                                                  │
│  ┌────────┐   缓存文件    ┌────────┐  img_N.desc.txt  ┌────────┐                │
│  │ fetch  │ ──────────► │ vision │ ───────────────► │ import │                 │
│  │ stage  │  case.json  │ stage  │  图片 OCR 描述   │ stage  │                 │
│  └────────┘  attachments│        │                  └───┬────┘                 │
│  ↓ 深信服Portal API      └────────┘                     │                       │
│    (SANGFOR_COOKIE)      ↓ ZhipuAI API                  │ PATCH /api/kb/        │
│                           (glm-4v-flash)                │  kbd/import          │
│                                                         ↓                       │
│  ┌──────────────────────────────────────────────────────────────────────┐       │
│  │                        kb-service (K3s)                               │       │
│  │                                                                       │       │
│  │  POST /api/kb/classify              ←── ┌──────────┐                │       │
│  │  ↓                                      │ classify │                │       │
│  │  ZhipuAI API (glm-4-flash)              │  stage   │                │       │
│  │  ↓ LLM 分类建议 (top3 + confidence)     └──────────┘                │       │
│  │  ↓ 写 ai_category_id / ai_category_conf                              │       │
│  │                                                                       │       │
│  │  PostgreSQL: kbd_entry (status=draft)                                 │       │
│  └──────────────────────────────────────────────────────────────────────┘       │
│                                                                                  │
│  审核员 (Admin UI)                                                                │
│  ↓ 详情审核 + 分类确认（select 选分类基线）                                          │
│  ↓ PATCH /api/v1/kbd/{id}/approve                                                │
│  ↓ status = published，自动触发 embedding 生成                                    │
└──────────────────────────────────────────────────────────────────────────────────┘
```

#### classify stage 调用链

```
scripts/kbd/classifier.py
  │
  ├─ curl POST http://localhost:8004/api/kb/classify
  │         Authorization: Bearer {INTERNAL_API_TOKEN}
  │         {"title": "...", "problem_desc": "..."}
  │
  └─► kb-service (K3s Pod)
        │
        ├─ GET /api/kb/classify  (classify.py)
        │      ↓
        │      SELECT * FROM kb_category  (读取 ~198 个分类节点)
        │      ↓
        │      构建 CLASSIFY_PROMPT_TEMPLATE（含分类树）
        │      ↓
        │      AsyncOpenAI.chat.completions.create(
        │        base_url = ZAI_BASE_URL,      # https://open.bigmodel.cn/api/paas/v4
        │        api_key  = ZAI_API_KEY,       # 41708580b6...
        │        model    = ZAI_LLM_MODEL,     # glm-4-flash
        │      )
        │      ↓
        │      json.loads(LLM 响应)  →  top3 分类 + 置信度
        │      ↓
        │      PATCH kbd_entry SET ai_category_id, ai_category_conf
        │
        └─► 响应 {"category_id": "虚拟机-003", "confidence": 0.9, ...}
```

#### classify stage 时序图

```
脚本进程          kb-service         ZhipuAI API         PostgreSQL
   │                  │                   │                   │
   │  POST /classify  │                   │                   │
   │─────────────────►│                   │                   │
   │                  │  SELECT kb_category                   │
   │                  │──────────────────────────────────────►│
   │                  │◄──────────────────────────────────────│
   │                  │  198 分类节点                         │
   │                  │                   │                   │
   │                  │  chat.completions.create(glm-4-flash) │
   │                  │──────────────────►│                   │
   │                  │                   │                   │
   │                  │◄──────────────────│                   │
   │                  │  {top3, confidence, reason}           │
   │                  │                   │                   │
   │                  │  UPDATE kbd_entry SET ai_category_*   │
   │                  │──────────────────────────────────────►│
   │                  │◄──────────────────────────────────────│
   │                  │                   │                   │
   │◄─────────────────│                   │                   │
   │  200 OK          │                   │                   │
```

#### Admin UI 审核流程及调用链

```
审核员浏览器 (Admin UI)         api-gateway              kb-service          PostgreSQL
      │                             │                       │                    │
      │  GET /api/v1/kbd/pending    │                       │                    │
      │────────────────────────────►│                       │                    │
      │                             │  GET /api/admin/kbd/  │                    │
      │                             │     pending           │                    │
      │                             │──────────────────────►│                    │
      │                             │                       │ SELECT kbd_entry   │
      │                             │                       │──────────────────►│
      │                             │                       │◄──────────────────│
      │◄────────────────────────────│◄──────────────────────│                   │
      │  条目列表（附 AI 分类建议）  │                       │                   │
      │                             │                       │                   │
      │  [点击条目详情]              │                       │                   │
      │  — 查看结构化内容预览        │                       │                   │
      │  — 截图说明 accordion 卡片   │                       │                   │
      │  — 选择确认分类（select 基线）│                      │                   │
      │  — 可直接内联编辑原文        │                       │                   │
      │                             │                       │                   │
      │  PATCH /api/v1/kbd/{id}     │  (内联编辑保存)        │                   │
      │────────────────────────────►│──────────────────────►│                   │
      │                             │                       │ UPDATE content_md  │
      │◄────────────────────────────│◄──────────────────────│──────────────────►│
      │                             │                       │                   │
      │  PATCH /api/v1/kbd/{id}/approve  (审核通过)          │                   │
      │────────────────────────────►│──────────────────────►│                   │
      │                             │                       │ UPDATE status=published
      │                             │                       │ → 触发 embedding   │
      │◄────────────────────────────│◄──────────────────────│──────────────────►│
```

#### 前置配置

编辑 `scripts/kbd/.env`，补充以下必填项：

```bash
# 从浏览器 DevTools 复制 Cookie（登录 support.sangfor.com.cn 后获取）
SANGFOR_COOKIE=PHPSESSID=xxx; _pk_id.xxx=xxx; ...

# z.ai API Key（用于 Vision OCR + 分类）
ZAI_API_KEY=sk-...
```

案例 ID 来源：默认读取项目根目录下的 `案例生产详细数据24-26.xlsx`（第一列为案例ID）。

#### 执行步骤

##### CLI 命令总览

```bash
# 查看帮助信息
uv run python -m scripts.kbd.run --help

# 查看配置（检查环境变量、Cookie、API Key）
uv run python -m scripts.kbd.run config
```

**子命令列表**：

| 子命令 | 说明 | 关键参数 |
|--------|------|---------|
| `pipeline` | 运行完整流水线（推荐） | `--excel`, `--ids`, `--stages`, `--resume`, `--failed-only`, `--override` |
| `fetch` | Stage 1：抓取案例数据 | `--excel`, `--ids`, `--force`, `--resume`, `--failed-only` |
| `vision` | Stage 2：图片 OCR | `--excel`, `--ids`, `--failed-only` |
| `import` | Stage 3：写入 DB | `--excel`, `--ids`, `--override`, `--override-status` |
| `classify` | Stage 4：AI 分类 | `--excel`, `--ids` |
| `review-list` | 列出待审核案例 | `--limit` |
| `config` | 打印当前配置 | 无 |

##### 基本用法

**单案例测试**：
```bash
# 单案例完整流水线
uv run python -m scripts.kbd.run pipeline --ids 34977

# 单案例分步执行（便于定位问题）
uv run python -m scripts.kbd.run fetch --ids 34977
uv run python -m scripts.kbd.run vision --ids 34977
uv run python -m scripts.kbd.run import --ids 34977
uv run python -m scripts.kbd.run classify --ids 34977
```

**批量执行（Excel）**：
```bash
# 从 Excel 读取全量 ID（默认文件：案例生产详细数据24-26.xlsx）
uv run python -m scripts.kbd.run pipeline --excel

# 限制数量（测试用）
uv run python -m scripts.kbd.run pipeline --excel --limit 10

# 只跑指定 stages（跳过部分阶段）
uv run python -m scripts.kbd.run pipeline --excel --stages fetch,vision
# 或使用数字简写
uv run python -m scripts.kbd.run pipeline --excel --stages 1,2
```

**指定案例列表**：
```bash
# 逗号分隔 ID
uv run python -m scripts.kbd.run pipeline --ids 34977,36179,36166

# 从文件读取 ID（每行一个）
uv run python -m scripts.kbd.run pipeline --id-file /path/to/ids.txt
```

##### --resume 参数（断点续传）

**适用场景**：Pipeline 中途中断（Ctrl+C、网络错误、API 限流），需要从上次中断处继续执行。

```bash
# 自动加载最新 progress 文件继续
uv run python -m scripts.kbd.run pipeline --excel --resume

# 指定恢复特定 run_id
uv run python -m scripts.kbd.run pipeline --excel --resume --resume-run-id 20260420_143000

# 只恢复 fetch stage（其他 stage 同理）
uv run python -m scripts.kbd.run fetch --excel --resume
```

**Resume 行为说明**：
- 自动查找 `logs/progress_*.json` 最新文件
- 跳过已完成的案例（状态为 `done` 或 `skipped`）
- 沿用原有 `run_id`（日志追加到同一文件）
- 如果没有找到 progress 文件，则从头开始

##### --failed-only 参数（仅处理失败案例）

**适用场景**：批量运行后部分案例失败（API 限流、网络超时），需要重试失败案例。

**失败检测规则**：
- Fetch 失败：`cache/{support_id}/fetch.failed` 文件存在
- Vision 失败：`img_*.desc.failed` 文件存在，或 `desc.txt` 内容含"（无文字）"（疑似 API 限流）

```bash
# 仅处理 Vision 失败的案例（推荐用于"无文字"重试）
uv run python -m scripts.kbd.run vision --excel --failed-only --limit 500

# 仅处理抓取失败的案例
uv run python -m scripts.kbd.run fetch --excel --failed-only

# Pipeline 级别失败重试
uv run python -m scripts.kbd.run pipeline --excel --failed-only
```

**--failed-only 与 --resume 组合**：
```bash
# 从上次中断处继续，且仅处理失败的案例
uv run python -m scripts.kbd.run pipeline --excel --resume --failed-only
```

##### --override 参数（强制覆盖）

**适用场景**：Vision OCR 修复后重新入库、内容更新需要覆盖已存在记录。

```bash
# 覆盖已存在的 draft 记录（安全默认）
uv run python -m scripts.kbd.run import --ids 35694 --override

# 仅覆盖指定状态的记录
uv run python -m scripts.kbd.run import --ids 35694 --override --override-status draft

# 覆盖已发布数据（谨慎使用）
uv run python -m scripts.kbd.run import --ids 35694 --override --override-status published

# 无限制覆盖所有状态（危险操作）
uv run python -m scripts.kbd.run import --ids 35694 --override --override-status all
```

**override_status 值语义**：

| 值 | 含义 | 使用场景 |
|----|------|---------|
| 不传（默认） | 仅覆盖 `draft` 状态 | 安全默认，防止误覆盖已发布数据 |
| `all` 或 `*` | 无限制，覆盖所有状态 | **谨慎使用**，可能覆盖已发布的知识 |
| `draft,published` | 仅覆盖指定状态列表 | 精细控制，允许覆盖 draft 和 published |

**安全提示**：
- ⚠️ 默认 `draft` 过滤是安全设计：防止误操作覆盖已发布的知识
- ✅ 推荐做法：先在 Admin 界面删除旧记录，再重新导入（保留完整的审核记录）

##### --force/--force-fetch 参数（强制重新抓取）

**适用场景**：深信服案例内容已更新，需要重新抓取原始数据。

```bash
# 强制重新抓取（覆盖 cache 文件）
uv run python -m scripts.kbd.run fetch --ids 34977 --force

# Pipeline 级别强制抓取 + 覆盖入库
uv run python -m scripts.kbd.run pipeline --ids 34977 --force-fetch --override
```

**注意**：`--force-fetch` 只影响 Stage 1（fetch），不影响后续 stage。

##### 组合用法示例

**场景 1：首次批量导入**：
```bash
# 小批量测试（10 条）
uv run python -m scripts.kbd.run pipeline --excel --limit 10

# 检查日志和进度
cat scripts/kbd/logs/kbd_*.log
cat scripts/kbd/logs/progress_*.json

# 全量导入（建议分批）
uv run python -m scripts.kbd.run pipeline --excel
```

**场景 2：中断后继续**：
```bash
# Ctrl+C 中断后，从断点继续
uv run python -m scripts.kbd.run pipeline --excel --resume

# 查看上次运行结果
ls scripts/kbd/logs/
cat scripts/kbd/logs/progress_20260420_143000.json
```

**场景 3：API 限流导致大量"无文字"，重试**：
```bash
# 检查失败数量（查看 progress JSON）
grep "failed_ids" scripts/kbd/logs/progress_*.json

# 仅重试 Vision 失败案例
uv run python -m scripts.kbd.run vision --excel --failed-only --limit 500

# 或完整重跑失败案例（从 Vision stage 开始）
uv run python -m scripts.kbd.run pipeline --excel --failed-only --stages vision,import,classify
```

**场景 4：OCR 优化后重新入库**：
```bash
# 修改 Vision prompt 或切换 OCR 后端后重跑
uv run python -m scripts.kbd.run vision --ids 35694 --failed-only

# 覆盖已存在的 draft 记录
uv run python -m scripts.kbd.run import --ids 35694 --override
```

**场景 5：全量重新处理（危险）**：
```bash
# 强制重新抓取 + 覆盖所有状态（谨慎使用）
uv run python -m scripts.kbd.run pipeline --excel --force-fetch --override --override-status all
```

##### 日志与进度查看

**日志文件位置**：`scripts/kbd/logs/`

```bash
# 查看日志文件列表
ls -la scripts/kbd/logs/

# 查看最新日志
cat scripts/kbd/logs/kbd_20260420_143000.log

# 实时监控日志（运行时）
tail -f scripts/kbd/logs/kbd_*.log

# 搜索失败记录
grep "failed" scripts/kbd/logs/kbd_*.log
grep "ERROR" scripts/kbd/logs/kbd_*.log
```

**进度文件查看**：
```bash
# 查看进度 JSON
cat scripts/kbd/logs/progress_20260420_143000.json

# 统计完成数量（jq 工具）
jq '.stages.fetch.completed_ids | length' scripts/kbd/logs/progress_*.json
jq '.stages.vision.failed_ids | length' scripts/kbd/logs/progress_*.json

# 查看特定案例状态
jq '.cases["15414"]' scripts/kbd/logs/progress_*.json
```

##### 状态流转

```
draft ──(Admin 审核通过)──► published ──(自动触发 embedding)──► 可检索
      ──(Admin 驳回)──────► rejected
```

##### 幂等行为

| 场景 | 行为 |
|------|------|
| `support_id` 不存在 | 新建 `kbd_entry`（status=draft） |
| `support_id` 已存在 | 跳过写入，返回已有 `kbd_id` |
| 重跑 `classify` stage | 只补充 `ai_category_id IS NULL` 的条目，已有分类不覆盖 |
| `--override` | 强制覆盖已存在记录（需配合 `--override-status`） |

> **注意**：KBD 的内容来源是深信服案例系统，同一 `support_id` 的案例内容一般不变，因此 import 阶段默认幂等跳过（与 SOP 不同）。若确实需要更新内容（如 OCR 优化后重新入库），使用 `--override` 参数。

##### 导入后操作

在 Admin 界面「KBD管理」中审核。审核通过（published）后**自动触发 embedding 生成**，条目进入可检索状态。

#### Override 参数详解（v4.10 新增）

> **适用场景**：Vision OCR 修复后重新入库、内容更新需要覆盖已存在记录。

**CLI 参数**：

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `--override` | 强制覆盖已存在记录（否则幂等跳过） | `false`（幂等） |
| `--override-status` | 仅覆盖指定状态的记录（逗号分隔） | `draft`（安全默认） |
| `--force-fetch` | Stage 1 强制重新抓取（覆盖缓存） | `false` |

**override_status 值语义**：

| 值 | 含义 | 使用场景 |
|----|------|---------|
| 不传（默认） | 仅覆盖 `draft` 状态 | 安全默认，防止误覆盖已发布数据 |
| `all` 或 `*` | 无限制，覆盖所有状态 | **谨慎使用**，可能覆盖已发布的知识 |
| `draft,published` | 仅覆盖指定状态列表 | 精细控制，允许覆盖 draft 和 published |

**参数行为矩阵**：

| override | override_status | 记录状态 | 结果 |
|----------|-----------------|---------|------|
| `false` | - | 不存在 | ✅ created（新建） |
| `false` | - | 已存在 | ⏭️ skipped（幂等跳过） |
| `true` | 不传（默认 draft） | draft | ✅ overridden（覆盖成功） |
| `true` | 不传（默认 draft） | published | ⏭️ skipped（状态不匹配，保护已发布数据） |
| `true` | `all` | draft | ✅ overridden |
| `true` | `all` | published | ✅ overridden（**谨慎使用**） |
| `true` | `draft,published` | draft | ✅ overridden |
| `true` | `draft,published` | rejected | ⏭️ skipped（状态不在列表中） |

**安全提示**：

- ⚠️ **默认 `draft` 过滤是安全设计**：防止误操作覆盖已发布的知识（用户已审核通过的数据）
- ⚠️ **`--override-status all` 需谨慎**：会覆盖 `published` 状态的记录，可能影响正在使用的知识
- ✅ **推荐做法**：先在 Admin 界面删除旧记录，再重新导入（保留完整的审核记录）

**API 响应示例**：

```json
// 新建成功
{"success": true, "kbd_id": 123, "status": "draft", "action": "created"}

// 幂等跳过
{"success": true, "kbd_id": 123, "status": "draft", "action": "skipped", "message": "记录已存在，幂等跳过"}

// 覆盖成功
{"success": true, "kbd_id": 123, "status": "draft", "action": "overridden", "message": "记录已覆盖"}

// 状态不匹配跳过
{"success": true, "kbd_id": 123, "status": "published", "action": "skipped", "message": "记录状态 'published' 不在 override_status 范围内"}
```

**Stats 统计字段变更**（v4.10）：

| 旧字段（废弃） | 新字段 | 说明 |
|--------------|--------|------|
| `idempotent` | `skipped` | 幂等跳过（记录已存在） |
| `updated` | `overridden` | 覆盖成功 |

**Port-Forward 自动检测**（v4.10 新增）：

Import stage 自动检测 kb-service 是否可达：
- ✅ 已可达（本地 Docker 或已有 port-forward）→ 直接执行
- ⚠️ 不可达（k3s ClusterIP）→ 自动启动 `kubectl port-forward svc/kb-service -n hci-dev 8080:8080`
- ❌ 启动失败 → 返回 `stats["error"] = total`

> **手动 port-forward 方式仍支持**（备用）：
> ```bash
> kubectl port-forward svc/kb-service -n hci-dev 8080:8080 --address 127.0.0.1 &
> ```

#### 幂等行为

| 场景 | 行为 |
|------|------|
| `support_id` 不存在 | 新建 `kbd_entry`（status=draft） |
| `support_id` 已存在 | 跳过写入，返回已有 `kbd_id` |
| 重跑 `classify` stage | 只补充 `ai_category_id IS NULL` 的条目，已有分类不覆盖 |

> **注意**：KBD 的内容来源是深信服案例系统，同一 `support_id` 的案例内容一般不变，因此 import 阶段不提供 upsert（与 SOP 不同）。若确实需要更新内容（如 OCR 优化后重新入库），需先在 Admin 界面删除旧记录再重新导入。

#### 导入后操作

在 Admin 界面「KBD管理」中审核。审核通过（published）后**自动触发 embedding 生成**，条目进入可检索状态。

#### 状态流转

```
draft ──(Admin 审核通过)──► published ──(自动触发 embedding)──► 可检索
      ──(Admin 驳回)──────► rejected
```

#### classify stage — LLM 集成设计说明

##### 原始设计（v4.4 之前）

| 配置项 | 原始值 | 问题 |
|--------|--------|------|
| `ZAI_BASE_URL` | 硬编码 `http://openclaw:{port}`（Helm template） | 无法切换 LLM 后端 |
| `ZAI_API_KEY` | 始终读 `ZAI_API_KEY` 环境变量 | openclaw proxy 需要 `OPENCLAW_GATEWAY_TOKEN` |
| `LLM_MODEL` | `os.environ.get("ZAI_LLM_MODEL", "gpt-4o-mini")` | 默认值 `gpt-4o-mini` 不被任何后端支持 |
| `base_url` | 直接传递环境变量值 | openai SDK 要求 URL 含 `/v1`，openclaw 无此后缀 |
| `path_labels` 解析 | `json.loads(row.path_labels)` （无类型检查） | JSONB 字段已被 asyncpg 解析为 `list`，再次 `json.loads` 抛 `TypeError` |
| Prompt 模板 | 示例 JSON 中直接写 `{` `}` | Python `.format()` 把 `{}` 当占位符，抛 `KeyError` |
| `openai` 依赖 | 未在 `requirements.txt` 声明 | 容器启动时 `ModuleNotFoundError` |

**实际效果（LLM 调用链）**：
```
脚本 → kb-service /api/kb/classify
  → http://openclaw:18789/chat/completions  ← 404（缺 /v1）
  → http://openclaw:18789/v1/chat/completions （修 URL 后）
  → OPENCLAW_GATEWAY_TOKEN + model=openclaw  ← "API rate limit reached"
     （openclaw 代理内部使用 glm-5，费用高/配额受限）
```

##### 优化后设计（v4.5）

**核心改动一：classify.py 自动适配逻辑**

```python
# 根据 ZAI_BASE_URL 是否含 "openclaw" 自动切换参数
_raw_base_url = os.environ.get("ZAI_BASE_URL", "http://host.docker.internal:18790")
_is_openclaw_proxy = "openclaw" in _raw_base_url

ZAI_BASE_URL = (          # 补齐 /v1 后缀（openai SDK 要求）
    _raw_base_url.rstrip("/") + "/v1"
    if _is_openclaw_proxy and not _raw_base_url.rstrip("/").endswith("/v1")
    else _raw_base_url
)
ZAI_API_KEY = (           # 代理用 OPENCLAW_GATEWAY_TOKEN，直连用 ZAI_API_KEY
    os.environ.get("OPENCLAW_GATEWAY_TOKEN", "")
    if _is_openclaw_proxy
    else os.environ.get("ZAI_API_KEY", "")
)
LLM_MODEL = os.environ.get("ZAI_LLM_MODEL") or (
    "openclaw" if _is_openclaw_proxy          # 代理要求 model="openclaw"
    else os.environ.get("OPENCLAW_DEFAULT_MODEL", "glm-4-flash")
)
```

**核心改动二：Helm template 支持 LLM 后端可配置**

```yaml
# deploy/helm/hci-platform/templates/kb-service/deployment.yaml
- name: ZAI_BASE_URL
  value: {{ .Values.kbService.llmBaseUrl | default (printf "http://openclaw:%v" .Values.openclaw.port) | quote }}
- name: ZAI_LLM_MODEL
  value: {{ .Values.kbService.llmModel | default "" | quote }}
```

**核心改动三：Dev 环境切换为 ZhipuAI 直连**

```yaml
# hci-platform-env/environments/dev/values.yaml
kbService:
  llmBaseUrl: "https://open.bigmodel.cn/api/paas/v4"
  llmModel: "glm-4-flash"
```

**实际效果（修复后调用链）**：
```
脚本 → kb-service /api/kb/classify
  → https://open.bigmodel.cn/api/paas/v4/chat/completions
  → ZAI_API_KEY + model=glm-4-flash  ← 成功返回分类结果
```

**`path_labels` 修复**（类型保护）：
```python
# 修复前：直接 json.loads 会对 list 类型抛 TypeError
# 修复后：先检查类型
raw = row.path_labels
if isinstance(raw, list):
    path_labels = raw          # JSONB 已解析
elif isinstance(raw, str):
    path_labels = json.loads(raw)
else:
    path_labels = []
```

**`path_labels` 修复**（类型保护）：
```python
# 修复前：直接 json.loads 会对 list 类型抛 TypeError
# 修复后：先检查类型
raw = row.path_labels
if isinstance(raw, list):
    path_labels = raw          # JSONB 已解析
elif isinstance(raw, str):
    path_labels = json.loads(raw)
else:
    path_labels = []
```

**其它修复汇总**：

| Bug | 根因 | 修复 |
|-----|------|------|
| Prompt `KeyError: '\n  "top3"'` | `.format()` 字符串中裸 `{}` | 全部改为 `{{}}` |
| `ModuleNotFoundError: No module named 'openai'` | `requirements.txt` 缺 `openai` | 添加 `openai>=1.0.0` |
| openclaw 限流 | 代理内部用 `glm-5`，费用高/配额少 | 切换 ZhipuAI 直连 `glm-4-flash` |

---

#### Vision Stage 截图识别架构（v4.7/v4.8）

> v4.7 完成了前端多维度分类（bgColor + keywords）和 Vision prompt 格式约束。
> v4.8 将 OCR 主路径从 Vision LLM 改为本地 OCR，Vision LLM 降为兜底。

##### v4.7 — Vision prompt 格式与前端分类规则

Vision（glm-4v-flash/qwen3.5-plus）输出格式约束：

```
0. **截图背景颜色**：[只写：黑色 / 白色 / 其他]
1. **截图界面类型**：[一行描述]
2. **截图中所有可见的文字内容**：
- [原文照录每行]
3. **截图中的报错信息或异常状态**：
- [精确引用；无报错写：无]
4. **对故障排查有价值的技术细节**：
- [关键细节]
```

关键约束：`禁止使用 JSON / Markdown 标题（#） / 表格`，并提供完整示例迫使模型遵守格式。

**前端多维度分类规则**（`detectScreenshotType()`）：

| 优先级 | 类型 | 判断条件 |
|--------|------|---------|
| 1 | 告警截图 🔔 | isWhite && fullText 含「紧急\|普通\|历史告警数\|未处理」 |
| 2 | 任务截图 📋 | isWhite && fullText 含操作人/对象类型/行为/开始时间/结束时间 **或** 完成/失败/进行中 |
| 3 | 终端截图 💻 | isBlack && fullText 含 Shell 特征（$# sudo chmod /var/ /etc/ 0x hex）|
| 4 | 日志截图 📄 | isBlack && typeName 含「日志」 **或** fullText 含时间戳格式（`\d{4}-\d{2}-\d{2} \d{2}:\d{2}:\d{2}`）|
| 5 | 其他截图 🖼️ | 以上均不满足 |

**isBlack/isWhite** 来自字段 0（bgColorText）中的「黑」/「白」关键词（Pillow 独立采样，非 LLM 推断）。

**JSON 格式容错处理**：glm-4v-flash/qwen3.5-plus 偶发返回 JSON 代码块格式，`image_proc.py` 在写 `desc.txt` 前调用 `_normalize_vision_output()` 自动归一化为编号字段格式。

##### v4.8 — 多后端本地 OCR + Vision LLM 兜底

**问题背景**：Vision LLM API 偶发超时（90s），超时时整张图片无法识别，无兜底机制。PaddleOCR 在 WSL2 开发环境 `import paddle` 阶段因 OneDNN 内核兼容问题挂死，无法在 dev 使用。

**解决方案**：引入 EasyOCR 作为 dev 主路径（纯 PyTorch，无 OneDNN 依赖，WSL2 完全兼容），Vision LLM 降为次级兜底。

**OCR 后端决策矩阵**：

| 环境（hci.env.role） | 主路径 | 兜底 |
|---------------------|--------|------|
| dev | EasyOCR（gpu=False） | Vision LLM（qwen3.5-plus） |
| staging / prod | PaddleOCR | Vision LLM（qwen3.5-plus） |
| kubectl 失败 / 其他 | — | Vision LLM 仅（vision_only） |

**环境自动检测**：
```python
# config.py _detect_env_role()
kubectl get ns argocd -o jsonpath='{.metadata.labels.hci\.env\.role}'
# 返回 dev/staging/prod；kubectl 失败 → 降级 "dev"（本地安全默认）
```

**image_proc.py 执行流程**：
```python
async def _process_image(img_path) -> str:
    backend = settings.ocr_backend_effective   # easyocr / paddleocr / vision_only
    if backend in ("paddleocr", "easyocr"):
        extractor = extract_text if backend == "paddleocr" else extract_text_easyocr
        full_text = await asyncio.to_thread(extractor, img_path)
    if not full_text:                          # 本地 OCR 空/失败 → Vision LLM 兜底
        full_text = await _vision_ocr_fallback(...)  # 指数退避重试
    background = await asyncio.to_thread(_detect_background, img_path)  # Pillow
    result = await analyze_screenshot(background, full_text, ...)        # qwen3-max
    return _format_desc_v2(background, result, full_text)
```

**重试机制**（Vision LLM 兜底 + 分析 LLM 均适用）：
- 单次超时：`LLM_TIMEOUT=90s`
- 最大重试次数：`LLM_MAX_RETRIES=3`
- 退避等待：`LLM_RETRY_BACKOFF=2.0` → 实际 2s / 4s / 8s

**新增 config.py 字段**：

| 字段 | 默认值 | 说明 |
|------|--------|------|
| `KBD_ENV` | `""` | 手动覆盖环境名（空=自动检测） |
| `OCR_BACKEND` | `"auto"` | auto / paddleocr / easyocr / vision_only |
| `LLM_TIMEOUT` | `90.0` | 单次 LLM 请求超时（秒） |
| `LLM_MAX_RETRIES` | `3` | 最大重试次数 |
| `LLM_RETRY_BACKOFF` | `2.0` | 指数退避基数（秒） |
| `ocr_backend_effective` | 属性 | 根据 KBD_ENV/OCR_BACKEND 推导实际后端名称 |

**手动覆盖方式（`.env` 或环境变量）**：
```bash
OCR_BACKEND=easyocr     # 强制 EasyOCR
OCR_BACKEND=paddleocr   # 强制 PaddleOCR
OCR_BACKEND=vision_only # 强制纯 Vision LLM（无本地 OCR）
KBD_ENV=staging         # 手动指定环境，不执行 kubectl 检测
```

**desc.txt v2 格式**（v4.8 输出，由 analyzer.py 生成）：
```
BACKGROUND: 黑色
TYPE: 日志截图
FULL_TEXT:
- 行1
- 行2
KEY:
- 关键错误行
TIPS:
- 排障建议1
```

> v4.7 的 converter.py 数据流（字段0内嵌在 intro 行）在 v4.8 中已由 desc.txt v2 格式取代；`parseScreenshotBlock()` 前端保留 v1/v2 双解析器向下兼容。



---

### 7.1.1 KBD Pipeline 运行日志与进度追踪（v5.1 新增）

> **问题背景**：批量执行 KBD Pipeline（~19500 案例）后，发现 16032 张图片（82%）识别结果为"（无文字）"，疑似 API 限流导致。由于运行过程缺少持久化日志和进度追踪，无法快速定位问题根源，也无法从失败点继续执行。

#### 新增功能概览

| 功能 | 说明 | 实现文件 |
|------|------|---------|
| 日志文件持久化 | 终端 + 文件双输出，保存到 `logs/kbd_{run_id}.log` | `run.py` `_setup_logging()` |
| 进度 JSON 追踪 | 保存到 `logs/progress_{run_id}.json` | `progress.py`（新增模块） |
| `--resume` 参数 | 从上次中断处继续，自动跳过已完成案例 | `pipeline.py` + `run.py` CLI |
| `--failed-only` 参数 | 仅处理失败案例（有 `.failed` 标记或识别为"无文字"） | `fetcher.py` + `image_proc.py` |

#### 日志文件结构

**日志目录**：`scripts/kbd/logs/`

**日志文件命名**：`kbd_{run_id}.log`（run_id 格式：YYYYMMDD_HHMMSS）

**日志格式**：
```
HH:MM:SS [INFO] kbd.pipeline — 流水线启动 案例数=100 stages=['FETCH', 'VISION', 'IMPORT', 'CLASSIFY'] run_id=20260420_143000 resume=False
HH:MM:SS [INFO] kbd.pipeline ─── Stage 1: 数据抓取 ───
HH:MM:SS [INFO] kbd.fetcher [1/100] 处理案例 15414
...
```

**日志级别**：
- 终端输出：INFO 级别
- 文件输出：DEBUG 级别（更详细，包含每张图片处理过程）

#### 进度 JSON 结构

**文件命名**：`progress_{run_id}.json`

**JSON 结构**：
```json
{
  "run_id": "20260420_143000",
  "started_at": "2026-04-20T14:30:00",
  "finished_at": null,
  "total_ids": 100,
  "stages_run": ["fetch", "vision", "import", "classify"],
  "stages": {
    "fetch": {
      "completed_ids": ["15414", "15415"],
      "failed_ids": ["15416"],
      "skipped_ids": []
    },
    "vision": {
      "completed_ids": ["15414"],
      "failed_ids": ["15415"],
      "skipped_ids": ["15416"]
    }
  },
  "cases": {
    "15414": {
      "fetch": "done",
      "vision": "done",
      "import": "pending",
      "classify": "pending"
    },
    "15415": {
      "fetch": "done",
      "vision": "failed",
      "import": "pending",
      "classify": "pending"
    },
    "15416": {
      "fetch": "failed",
      "vision": "pending",
      "import": "pending",
      "classify": "pending"
    }
  }
}
```

**状态值语义**：
- `pending`：尚未处理
- `done`：处理成功
- `failed`：处理失败（有 `.failed` 标记）
- `skipped`：幂等跳过（已完成或 resume 模式）

#### CLI 新增参数

| 参数 | 适用命令 | 说明 |
|------|---------|------|
| `--resume` | `pipeline`, `fetch` | 从上次中断处继续，自动加载最新 `progress_*.json` |
| `--resume-run-id` | `pipeline`, `fetch` | 指定恢复的 run_id（不传则自动查找最新） |
| `--failed-only` | `pipeline`, `vision` | 仅处理失败案例（筛选 `.failed` 或"无文字"标记） |

**典型用法示例**：

```bash
# 场景 1：正常执行（自动生成 run_id 和日志）
uv run python -m scripts.kbd.run pipeline --excel --limit 100

# 场景 2：从上次中断处继续
uv run python -m scripts.kbd.run pipeline --excel --resume

# 场景 3：指定恢复特定 run_id
uv run python -m scripts.kbd.run pipeline --excel --resume --resume-run-id 20260420_143000

# 场景 4：仅处理失败的 Vision 案例（重试"无文字"识别）
uv run python -m scripts.kbd.run vision --excel --failed-only --limit 500

# 场景 5：仅处理抓取失败的案例
uv run python -m scripts.kbd.run fetch --excel --failed-only --limit 100

# 场景 6：查看运行日志和进度
cat scripts/kbd/logs/kbd_20260420_143000.log
cat scripts/kbd/logs/progress_20260420_143000.json
```

#### 失败检测逻辑

**抓取失败检测**（`fetcher.py`）：
```python
def _is_fetch_failed(support_id: str) -> bool:
    """检查是否有 fetch.failed 标记"""
    case_dir = _case_dir(support_id)
    return (case_dir / "fetch.failed").exists()

def get_failed_fetch_ids(case_ids: list[str]) -> list[str]:
    """从案例列表中筛选出抓取失败的案例"""
```

**Vision 失败检测**（`image_proc.py`）：
```python
def _has_failed_vision(case_dir: Path) -> bool:
    """
    检查是否有图片处理失败的标记：
    - .desc.failed 文件存在
    - 或 desc.txt 内容为"（无文字）"（疑似 API 限流）
    """
    if any(case_dir.glob("img_*.desc.failed")):
        return True
    for desc_file in case_dir.glob("img_*.desc.txt"):
        content = desc_file.read_text(encoding="utf-8")
        if "（无文字）" in content and "DESCRIPTION:" in content:
            return True
    return False

def get_failed_vision_ids(case_ids: list[str]) -> list[str]:
    """从案例列表中筛选出 Vision 处理失败的案例"""
```

#### Resume 执行流程

```
用户执行 --resume
      │
      ▼
find_latest_progress_file() → 查找 logs/progress_*.json 最新文件
      │
      ▼
load_progress(run_id) → 加载已有进度 JSON
      │
      ▼
get_completed_ids_for_stage(progress, "fetch") → 获取已完成案例
      │
      ▼
过滤 case_ids，跳过已完成 → 仅处理 pending/failed 案例
      │
      ▼
run_pipeline(filtered_ids, ...) → 执行剩余案例
      │
      ▼
save_progress(run_id, progress) → 每个 Stage 完成后更新进度
      │
      ▼
finish_progress(progress) → 标记 finished_at，保存最终进度
```

#### 进度追踪核心函数

**`progress.py` 模块**（新增）：

| 函数 | 说明 |
|------|------|
| `generate_run_id()` | 生成 YYYYMMDD_HHMMSS 格式 run_id |
| `get_progress_path(run_id)` | 返回进度文件路径 |
| `get_log_path(run_id)` | 返回日志文件路径 |
| `init_progress(run_id, case_ids, stages)` | 初始化进度结构并保存 |
| `save_progress(run_id, progress)` | 保存进度到 JSON 文件 |
| `load_progress(run_id)` | 加载已有进度文件 |
| `update_stage_status(progress, stage, support_id, status)` | 更新单个案例状态 |
| `finish_progress(progress)` | 标记进度完成并保存 |
| `find_latest_progress_file()` | 查找最新的进度文件 run_id |
| `get_completed_ids_for_stage(progress, stage)` | 获取某个 stage 已完成的案例 ID |

#### 实现注意事项

1. **仅使用 Python stdlib**：json、logging、pathlib、datetime，不新增外部依赖
2. **向后兼容**：现有 CLI 行为不变，新参数为可选
3. **幂等设计**：每个 Stage 完成后立即保存进度，支持随时中断和恢复
4. **日志双 Handler**：StreamHandler（终端 INFO）+ FileHandler（文件 DEBUG）

---

### 7.2 SOP 导入方法

SOP 来源是 `.docx` 格式的排障手册，脚本用 `python-docx` 解析章节结构并分块入库。

#### 前置操作（K3s 环境）

```bash
# 1. 端口转发 kb-service 到本地（导入脚本直连 kb-service，非 api-gateway）
kubectl port-forward svc/kb-service -n hci-dev 8004:8004 --address 127.0.0.1 &

# 2. 确认 INTERNAL_API_TOKEN（dev 环境已写入 scripts/kbd/.env，无需修改）
#    如需手动验证：
kubectl exec -n hci-dev deploy/kb-service -- env | grep INTERNAL_API_TOKEN
```

**`scripts/kbd/.env` 默认配置（无需修改）：**
```
KB_SERVICE_URL=http://localhost:8004
INTERNAL_API_TOKEN=dev-internalapi-api-token-2026
```

#### 执行步骤

```bash
# 在项目根目录下执行
cd /path/to/hci-troubleshoot-platform

# 单文件导入（指定对应分类 code）
uv run python -m scripts.kbd.import_sop \
  --file /path/to/vm-startup-failure.docx \
  --category-id "虚拟机-003"

# 批量导入一个目录下的所有 .docx
uv run python -m scripts.kbd.import_sop \
  --dir /path/to/sop_docs/ \
  --category-id "虚拟机-003"
```

> **注意**：`--category-id` 是分类基准中的 `id` 字段，例如 `虚拟机-003`。
> 导入前请先确认分类树已导入（见 7.3）。

#### 导入流程

```
.docx 文件（二进制）
    │
    ├─ compute_docx_hash()：SHA256(原始字节) → docx_hash
    │    └─ 同一文件重复调用 → hash 相同 → 幂等跳过入库
    │    └─ 修改文件内容后保存 → hash 变化 → 新建记录（旧记录保留）
    │
    ├─ parse_docx_to_markdown()：python-docx 读取 Heading 1/2/3
    │    ├─ 提取标题（第一个 Heading 1 或文件名）
    │    ├─ 生成完整 Markdown（full_markdown）
    │    └─ 按章节拆分成 chunks
    │
    └─ POST /api/sop/ingest → sop_document（draft）+ sop_chunk
```

#### category_id 行为说明

| 场景 | 行为 |
|------|------|
| 首次导入 | 新建 `sop_document`，写入 `category_id` |
| 相同文件名 + 相同内容（hash 相同） | 幂等跳过，返回已有 `document_id`，日志 `event=sop_ingest_duplicate_source` |
| **相同文件名 + 内容已修改（hash 不同）** | **upsert：更新内容、重建 chunks、状态重置为 draft（需重新审核）**，日志 `event=sop_ingest_updated` |
| 相同文件名，不同 `category_id` | 更新 `category_id`（仅在内容相同时；若内容也不同，则走 upsert 逻辑一并处理） |
| 不同文件名（不同 `source_id`） | 新建记录，与已有记录并存 |

**导入后**：SOP 默认 `draft` 状态，需在 Admin 界面「SOP管理」中审核发布，发布后：
1. 自动触发 embedding 生成（每个 chunk 生成向量写入 `sop_chunk.embedding`）
2. 进入可被 AI 检索的状态

---

### 7.3 分类树导入方法

分类树是所有知识的路由基础，必须首先导入。

#### 数据源

分类基准文件：`backend/kb-service/config/category_baseline.yaml`（198 条叶节点 + 自动推断 L1/中间层）

#### 执行步骤

```bash
# 预览（不写入，推荐先确认）
uv run python scripts/kbd/seed_categories.py --dry-run

# 正式导入（幂等，已存在的 code 会 upsert）
uv run python scripts/kbd/seed_categories.py
```

> **也可以通过 Admin 界面导入**：进入「分类基线」→ 点击「导入 YAML」→ 上传 `backend/kb-service/config/category_baseline.yaml`，两阶段操作（预览 → 确认导入）。

---

### 7.4 导入方式对比

| 数据类型 | 推荐方式 | 命令入口 |
|---------|---------|---------|
| 分类树 | Admin UI 或脚本 | `seed_categories.py` / Admin「导入YAML」 |
| KBD 案例 | 脚本（ETL 流水线） | `python -m scripts.kbd.run pipeline` |
| SOP 文档 | 脚本 | `python -m scripts.kbd.import_sop` |

---

## 八、技术选型

| 组件 | 选型 | 说明 |
|------|------|------|
| Embedding | z.ai API / bge-small-zh-v1.5 | 主力 z.ai，降级本地 |
| 向量存储 | pgvector | 复用现有 PostgreSQL |
| 向量索引 | IVFFlat (lists=100) | ~10000 向量规模适用 |
| BM25 | PostgreSQL tsvector + GIN | jieba 分词 |
| 中文分词 | jieba + HCI 自定义词典 | 支持领域术语 |

---

## 九、分类树结构

### 9.1 数据来源

分类基准文件：`backend/kb-service/config/category_baseline.yaml`

- **来源**：云计算-结构图-AI-全.mm
- **总计**：198 条叶节点（L2-L4）
- **L1 域节点**：5 个（虚拟机/网络/存储/硬件/平台）

### 9.2 分类编码规范

```
格式：<DOMAIN>-<SEQ>

示例：
  虚拟机-001  → 虚拟机创建失败
  虚拟机-003  → 虚拟机开机失败
  网络-015    → 分布式防火墙策略不生效
  存储-008    → 存储卷扩容失败
```

### 9.3 层级结构示例

```yaml
# L2 叶节点（直接属于 L1）
- id: 虚拟机-003
  domain: 虚拟机
  label: 虚拟机开机失败
  path:
    - 虚拟机
    - 虚拟机开机失败

# L3 叶节点（中间有分组）
- id: 虚拟机-001
  domain: 虚拟机
  label: 虚拟机创建失败
  path:
    - 虚拟机
    - 虚拟机创建
    - 虚拟机创建失败

# L4 叶节点（多层分组）
- id: 硬盘-001
  domain: 硬件
  label: 单盘故障排查
  path:
    - 硬件
    - 硬盘
    - 硬盘排障
    - 单盘故障排查
```

### 9.4 五大技术域

| L1 域 | 说明 | 典型分类 |
|-------|------|---------|
| 虚拟机 | 虚拟机生命周期管理 | 创建/删除/开机/关机/迁移/克隆 |
| 网络 | 物理网络 + 虚拟网络 | 网口/VLAN/分布式防火墙/负载均衡 |
| 存储 | 虚拟存储 + 外部存储 | 创建/扩容/快照/备份/恢复 |
| 硬件 | 物理硬件故障 | 风扇/电源/CPU/内存/硬盘 |
| 平台 | 集群管理/系统运维 | 授权/升级/监控/日志 |

---

## 相关文档

- [AI助手设计.md](../ai-assistant/AI助手设计.md) — AI 层如何消费知识库
- [接口设计.md](../接口设计.md) — KB Service REST API 规范
- [2026-04-02-S0意图识别与分类基线重构方案.md](../events/2026-04-02-S0意图识别与分类基线重构方案.md) — 分类决策
