---
status: active
category: verify
audience: developer
last_updated: 2026-08-06
owner: team
---

# KBD 数据管道稳定性与可观测性验证

## 验证范围

- Pipeline DAG 依赖闭包、VISION/CLASSIFY 分叉和 EXTRACT_SIGNALS 双硬依赖。
- Import 失败后的 `blocked_by_dependency` 状态。
- Vision 质量状态与图片级状态聚合。
- CLI Stage 5/6 入口和非零退出码。
- Signal Job 轮询及零 Proposal 的 `needs_review` 语义。

## 自动验证结果

执行 KBD Pipeline 单元测试：

```bash
uv run pytest tests/unit/kbd -q
```

结果：106 passed，131 skipped（Golden 源数据用例按环境条件跳过）。

执行 kb-service 全量测试：

```bash
uv run pytest backend/kb-service/tests -q
```

结果：359 passed；存在 1 条既有 `AsyncMock` 未 await RuntimeWarning，测试未失败，和本次改动无直接关系。

执行 Helm 验证：

```bash
helm lint deploy/helm/hci-platform
helm unittest deploy/helm/hci-platform -f 'tests/kb_service_health_dns_test.yaml'
```

结果：lint 通过；1 个 suite、3 个 assertion 通过。

另行执行 `python3 -m compileall -q data-pipeline/kbd backend/kb-service/app` 和 `git diff --check`，均通过。

## 尚未宣称通过的项目

- 真实 Provider 429/timeout 恢复和 20 KBD 长批次仍需在目标环境执行。
- Redis/多副本 Job 状态恢复尚未实现，不能把当前进程内 JobManager 宣称为生产持久化。
- 真实分类准确率、Vision 质量和 Signal Proposal 质量 A/B 尚未完成。

## 验收判定

代码级回归通过；生产验收必须补做 20 KBD 故障注入，并记录技术成功率、完成覆盖率、P50/P95、429/timeout、重试放大系数、低置信度率和 Signal 需复核率。
