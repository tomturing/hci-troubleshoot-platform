---
status: frozen
category: verify-event
audience: developer, operator
last_updated: 2026-08-07
owner: team
---

# CI 发布链路补偿闭环验证报告

## 1. 验证范围

验证 `.github/workflows/ci.yml` 新增的手动补偿发布入口及晋级前后环境仓库 tag 核验，不执行真实 staging 晋级，不修改外部环境仓库。

## 2. 自动化静态验证

| 检查 | 结果 | 证据 |
|---|---|---|
| `ci.yml`、`release-please.yml`、`env-repo-sync.yml` YAML 解析 | ✅ | Python 3 + PyYAML `safe_load` 全部通过 |
| Workflow 中 29 个 `run` 块 Bash 语法 | ✅ | 对每个 `run` 块执行 `bash -n` |
| Git 空白检查 | ✅ | `git diff --check` |
| 真实环境 values 同步脚本 | ✅ | 使用 `hci-platform-env/environments/staging/values.yaml` 临时副本和测试 tag 验证 |
| 逐服务 tag 读取与核验 | ✅ | `apiGateway`、`caseService`、`conversationService`、`schedulerService`、`kbService`、`customerUI`、`adminUI`、`terminalBridge`、`agentService`、`evalService`、`dbMigrate` 均可核验 |

## 3. 触发矩阵验证

| 事件 | ref | `promote_target` | 构建 | 晋级 | 预期 |
|---|---|---|---|---|---|
| `push` | `refs/heads/main` | — | 是 | dev → staging | ✅ |
| `workflow_dispatch` | `refs/heads/main` | `none` | 是 | 否 | ✅ |
| `workflow_dispatch` | `refs/heads/main` | `dev` | 是 | dev | ✅ |
| `workflow_dispatch` | `refs/heads/main` | `staging` | 是 | staging | ✅ |
| `workflow_dispatch` | `refs/heads/main` | `both` | 是 | dev → staging | ✅ |
| `workflow_dispatch` | `refs/heads/feature/demo` | `both` | 是 | Job 明确失败，不写环境仓 | ✅ |

## 4. 尚未执行的验证

以下项目需要 GitHub Hosted Runner 和真实凭据，不能由本地静态检查替代：

- 在 `main` 上手动选择 `promote_target=both`，确认镜像全部推送到 GHCR；
- 确认 `hci-platform-env` 两个 values 文件的远端提交和目标 tag；
- 确认 ArgoCD Application 从环境仓库 revision 变化到 `Synced/Healthy`；
- 核验 staging Pod 的 image digest、启动时间和容器源码；
- 以 KBD23821 执行“取值 → AI 提取 → delta 判断 → 保存”真实回归。

## 5. 验收结论

代码级补偿链路和安全边界验证通过。真实 staging 发布必须在合并后由具备 `ENV_REPO_PAT` 的 GitHub Actions 运行完成，并按第 4 节补充运行态证据后，才能宣称 PR698 已在 staging 生效。
