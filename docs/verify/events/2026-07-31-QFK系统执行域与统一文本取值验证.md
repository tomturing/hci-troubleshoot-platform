---
status: passed_code_level
category: verify
audience: developer, tester, expert
last_updated: 2026-07-31
owner: team
---

# QFK 系统执行域与统一文本取值验证

## 已完成的自动验证

```text
agent-service：170 passed
  - test_kbd_differential.py、test_qfk.py、test_qfk_extractor.py
  - test_semantic_validator.py、test_executor.py

kb-service：63 passed
  - test_signal_text_extract_schema.py、test_kbd_expert_maintenance.py、test_playbooks.py

Admin UI
  - vue-tsc -b 通过
  - vite production build 通过

Signal Schema Contract
  - 13 个契约文件自身合法
  - 合法 fixture 通过，4 个非法 fixture 正确拒绝
  - 内存生成与入库文件无漂移

静态检查
  - 改动 Python 文件 ruff check 通过
  - git diff --check 通过
```

已覆盖：aCLI 全局参数编译、旧 host 归一、Bridge 不接收 aCLI container、
`--cluster` boolean、54% 数值转换、两行 max 聚合、TextExtract Schema 复用、
`match.extract` 占位符 requires 推导，以及已有的截断输出 Fail Closed。

## 仍需验证

- Admin `vue-tsc` 与生产构建；
- Schema Contract 全量漂移检查；
- 在真实 HCI 上确认 `acli --container` 的版本兼容与完整命令；
- KBD40061 的真实 Agent 执行、Artifact/Evidence Replay；
- 122 条 Proposal 的正式专家复核和 Expert Gold 判定。

本报告仅证明代码级回归，不将 Mock 测试等同真实客户环境执行。
