---
status: completed
category: verify
audience: developer, tester, expert
last_updated: 2026-07-30
owner: team
---

# KBD 专家信号编辑与执行契约一致性验证

## 结论

代码级验证通过。KBD30880 所代表的删除场景不再产生悬空 `verification_contract`
引用；保存草稿与发布门禁已分层。尚未在本次代码变更中宣称真实 HCI Agent 现场执行
通过，也不将其记为 Expert Gold。

## 执行记录

```text
PYTHONPATH=backend:backend/kb-service .venv/bin/pytest \
  backend/kb-service/tests/test_kbd_expert_maintenance.py \
  backend/kb-service/tests/test_signal_text_extract_schema.py -q

52 passed in 1.37s
```

覆盖：

- `context sig_004` 删除后 Contract 同步移除，必要证据保持；
- 最后一条必要证据移除后工作稿仍可保存，并生成专家可操作问题；
- `context → must` 的角色移动不会重复或残留旧分组；
- 既有 maintenance 保存、详情 overlay、发布锁与 v2 文本/日志 Schema 回归。

## 待部署人工验证

按 [实施任务](../../task/agent/events/2026-07-30-KBD专家信号编辑与执行契约一致性任务.md)
的五步在 dev Admin UI 验收，重点检查删除弹窗、保存后的即时检查、维护稿不热发布和显式
发布切 active。该人工验证完成前，不以此文档代替 Agent E2E 或业务专家批准证据。
