---
status: completed
category: verify
audience: developer, tester, operator
last_updated: 2026-07-27
owner: team
---

# 运行时代码完整性修复验证

## 验证范围

验证工单 `Q2026072748256` 的修复不是停留在 Git 提交或镜像 tag，而是已在 `hci-dev` Pod 内实际生效；验证新的 API Server 防护能阻止同一类源码覆盖。

## 结果

| 检查 | 结果 | 证据 |
|---|---|---|
| PR #628 镜像部署 | 通过 | conversation/kb/agent 均运行 `20260727-1349-8f7a79a` |
| conversation-service 运行源码 | 通过 | `kind == "human_escalation"` 与 `human_escalation_acknowledged` 均存在 |
| kb-service 运行源码 | 通过 | `has_produces` 与 `has_match == has_produces` 均存在 |
| agent-service 运行源码 | 通过 | QFK produces/完整输出提取与 host→IP 归一化逻辑均存在 |
| 源码覆盖挂载 | 通过 | 三个 Deployment 的 volumes、volumeMounts 均为 0；9 个临时 ConfigMap 已删除 |
| Helm 模板 | 通过 | `helm lint deploy/helm/hci-platform-infra` 成功；API Server dry-run 接受策略对象 |
| 正向准入测试 | 通过 | 无源码卷挂载的 Deployment server-side dry-run 成功 |
| 负向准入测试 | 通过 | ConfigMap 挂载到 `/app/app/example.py` 被 API Server 以 `Forbidden` 拒绝 |

## 关键负向证据

```text
ValidatingAdmissionPolicy 'hci-platform-deny-runtime-source-overrides'
with binding 'hci-platform-deny-runtime-source-overrides' denied request
```

因此未来直接执行 `kubectl patch deployment` 尝试复现本事故时，资源不会被写入 Kubernetes；不会再出现“ArgoCD Healthy、新 image、旧 Python 文件运行”的同类状态。

## 未替代的验证

本次在修复后未复用旧会话重新点击人工升级卡片，以避免把已失败会话状态与新逻辑混淆。下一张新建工单应按发布指南执行 KBD 路由和人工升级确认回归：KBD `result_count` 不应因 `produces` 缺少 matcher 而为 0；`human_escalation` 不应请求 agent-service 的 `interactive-response`，更不会调用 ops-agent。

## 关联文档

- [部署修复与防护事件](../../deploy/events/2026-07-27-运行时代码完整性修复与防护.md)
- [发布后验证步骤](../../deploy/发布指南.md#52-健康检查要点)
