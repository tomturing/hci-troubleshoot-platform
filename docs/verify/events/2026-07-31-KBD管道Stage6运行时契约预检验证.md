---
status: passed_code_level
category: verify
audience: developer, tester, operator
last_updated: 2026-07-31
owner: team
---

# KBD 管道 Stage 6 运行时契约预检验证

## 验证目标

验证 KBD 的真实仓库入口无需人工设置 `PYTHONPATH` 即可导入 Stage 6 使用的
`backend/shared` 契约，并确保默认 pipeline 将这一依赖检查提前到所有生产阶段之前。

## 自动验证

| 验证项 | 方法 | 预期 |
|---|---|---|
| 推荐入口可发现 | `python -m data-pipeline.kbd.run --help` | 显示六阶段 CLI；无 `shared` 导入错误 |
| 无路径变量审计 | pytest 子进程移除 `PYTHONPATH`，执行 `audit-log-signals --file` | 返回 0 且报告 `PASS_LOG_CONTRACT` |
| 领域规则回归 | `tests/unit/kbd/test_log_signal_audit.py` | 原有 qfk_log Schema/Catalog 判定不变 |
| CLI 规则回归 | `tests/unit/kbd/test_run_cli.py` | Stage、输入互斥、fail-on-blocked 与新入口均通过 |

本轮实际结果：`test_run_cli.py` 与 `test_log_signal_audit.py` 共 **12 passed**；Ruff、
`git diff --check`、模块文档同步和文档命名检查均通过；从仓库根目录移除
`PYTHONPATH` 后，`uv run python -m data-pipeline.kbd.run --help` 成功。

全量 `tests/unit/kbd` 另有一条与本次代码无关的环境完整性失败：用户此前以
`--force-fetch` 刷新了被 `.gitignore` 排除的本地 `cache/40061/raw.json`，其 SHA-256
已从 Golden manifest 的不可变指纹变化。该测试正确拒绝将新 Portal 原文静默当作旧
Golden 源；本轮不更新 manifest，也不将该缓存变动带入代码提交。

## 人工回归

```bash
cd /aihci/hci-troubleshoot-platform
uv run python -m data-pipeline.kbd.run pipeline \
  --ids 40061 --override --override-status all --force-fetch
```

观察第一条 Stage 日志前不应出现 `ModuleNotFoundError: shared`；若 checkout 缺失共享目录，
命令必须在 fetch 前退出并说明应检查 `backend/shared`/执行 `uv sync`。成功跑到 Stage 6
只表示运行时装配正确，仍不证明 LLM Proposal 的日志语义正确或该 KBD 已成为 Expert Gold。
