---
status: completed
category: verify
audience: developer, tester, expert
last_updated: 2026-08-04
owner: team
---

# KBD 关键信号图片来源门禁收敛验证

## 验证目标

验证 KBD 图片输入仍严格按四个诊断字段过滤，同时 Candidate 图片来源检查不再将正文 evidence 强制逐字匹配截图 OCR。

## 自动验证结果

| 验证项 | 命令/范围 | 结果 |
|---|---|---|
| Vision Evidence 单元回归 | `PYTHONPATH=backend/kb-service:backend /aihci/hci-troubleshoot-platform/.venv/bin/python -m pytest backend/kb-service/tests/test_vision_evidence.py -q` | 16 passed |
| KB Service 全量单元回归 | `PYTHONPATH=backend/kb-service:backend /aihci/hci-troubleshoot-platform/.venv/bin/python -m pytest backend/kb-service/tests -q` | 336 passed，1 条既有 AsyncMock 未 await warning |
| Python 静态检查 | `ruff check backend/kb-service/app/routes/extract_signals.py backend/kb-service/tests/test_vision_evidence.py` | 通过 |
| 文档检查 | `check_docs_naming.py`、`check_module_doc_sync.py`、`git diff --check` | 通过 |
| KBD27736 只读门禁重放 | 当前数据库的 `sig_004` + 当前代码 | 进入 Matcher 门禁，以脱敏 `address xx.100.88` 拒绝；不再报图片 evidence 逐字追溯 |

## 关键断言

| 场景 | 预期 |
|---|---|
| solution/root_cause 图片和截图上下文 | 不进入 Signal Prompt |
| Candidate 引用未输入图片 | `run_failed`，原因是未进入诊断输入 |
| 正文 evidence + 实际输入的诊断图 ref | 可继续通过来源门禁 |
| KBD27736 型 `address xx.100.88` Matcher | 到达 Matcher 门禁并因脱敏占位文本被拒绝，不显示图片 evidence 逐字追溯错误 |
| 图片长度限制 | 仅完整且实际进入 Prompt 的图片区域可引用 |

## 事实边界

本验证证明输入边界和来源集合成员校验的确定性行为；不触发真实 LLM 重抽，不修改或发布任何 KBD revision。KBD27736 的业务级候选质量仍须专家显式重抽和复核。
