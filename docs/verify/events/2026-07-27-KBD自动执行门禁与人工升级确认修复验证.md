---
status: completed
category: verify
audience: developer
last_updated: 2026-07-27
owner: team
---

# KBD 自动执行门禁与人工升级确认修复验证

> 对应方案：[KBD自动执行门禁与人工升级确认修复方案](../../solution/events/2026-07-27-KBD自动执行门禁与人工升级确认修复方案.md)

## 现场复现证据

工单 `Q2026072769668` 的会话 `355fefc0-79c1-4478-8551-f3f4f7b6819c`：

1. S0 成功确认 `虚拟机-003`，S1 正确请求分类 playbooks。
2. 接口返回 KBD 27123，但 `executable=false`，问题列表为 `sig_002 缺少确定性 matcher`、`sig_003 缺少确定性 matcher`。
3. 两条信号均为合法产出变量模式；保存 v2 Schema 已允许，QFK 执行器也支持命令成功后写变量池并 PASS。
4. 用户确认人工升级卡时，日志显示向 ops-agent 的 `submit_response` 返回 404，conversation-service 对页面返回 503。

## 自动化验证

| 验证项 | 命令/用例 | 预期 |
|---|---|---|
| KBD 门禁单测 | `pytest backend/kb-service/tests/test_playbooks.py` | 产出变量模式可执行；缺失/二义配置不可执行 |
| 人工升级确认单测 | `pytest backend/conversation-service/tests/unit/test_human_escalation.py` | 本地 ack 不需 AgentClient，非法 ack 被拒绝 |
| 相关回归 | `pytest backend/conversation-service/tests/unit/test_tool_confirm_reliability.py` | 工具授权分支不受影响 |
| 静态检查 | `git diff --check`、文档命名与模块同步检查 | 无格式或文档治理问题 |

## 部署后现场验证

1. 使用同类“虚拟机-003”工单完成 S0 分类确认。
2. 查询分类 playbooks，确认 KBD 27123 的 `executable=true`，无“缺少确定性 matcher”错误。
3. 观察 Agent 开始执行 KBD 信号；若实际 `lsof`/文本提取失败，应显示真实证据失败或依赖阻塞，而非在 S1 错误转人工。
4. 人为触发 HTP `AgentEscalation` 后点击“我知道了”，页面应返回成功；会话历史新增确认消息；agent-service/ops-agent 不应出现对应 `submit_response` 请求。
5. 真实 ops-agent SOP/信息确认卡仍按 ACP 路径返回并续写。
