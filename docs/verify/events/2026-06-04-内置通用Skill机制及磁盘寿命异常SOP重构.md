# 规划文档自动归档、废弃代码下线与 Vibe Kanban 清理验收报告

本轮任务已全部顺利完成。我们重构了 Git Hooks 跨环境自动配置，清理了废弃的 Vibe Kanban 配置文件，将 API 网关的代理路由重命名为 `capabilities.py`，并彻底下线了后端的所有僵尸代码和过期模型。

---

## 1. 变更内容清单

### 1.1 多环境 Git Hooks 归档自动化
* **[NEW] [archive_artifacts.py](file:///mnt/d/aihci/hci-troubleshoot-platform/scripts/archive_artifacts.py)**：恢复文档归档的核心 Python 逻辑，支持对历史和新对话归档。
* **[NEW] [archive_artifacts.sh](file:///mnt/d/aihci/hci-troubleshoot-platform/scripts/archive_artifacts.sh)**：归档脚本的 Bash 包装器。
* **[NEW] [pre-commit](file:///mnt/d/aihci/hci-troubleshoot-platform/scripts/git-hooks/pre-commit)**：Git commit 前置钩子模板。
* **[MODIFY] [package.json](file:///mnt/d/aihci/hci-troubleshoot-platform/frontend/package.json)**：在 `scripts` 中增加 `"prepare"` 钩子，配置在运行 `pnpm install` 时，自动调用 `git config core.hooksPath` 指向 `scripts/git-hooks` 目录。由此，当其他开发者或 CI 系统运行安装脚本时，Git Hook 均会自动生效并绑定到项目目录中，解决了仅能在本地配置的问题。

### 1.2 废弃 Vibe Kanban (VK) 清理
基于第一性原理，完全清理了已不再使用的 Vibe Kanban 组件：
* **[DELETE] `.vk/` 目录**：删除了存放本地数据和临时报告的目录。
* **[DELETE] `scripts/.vk/` 目录**：删除了脚本包。
* **[DELETE] [vk-hooks.sh](file:///mnt/d/aihci/hci-troubleshoot-platform/scripts/ci/vk-hooks.sh)** / **[vk-mcp-client.py](file:///mnt/d/aihci/hci-troubleshoot-platform/scripts/ci/vk-mcp-client.py)**：删除了专属于 VK 的工作流钩子和 MCP Server 接口代码。
* **[MODIFY] [Makefile](file:///mnt/d/aihci/hci-troubleshoot-platform/Makefile)**：删除了 `vk`, `vk-stop`, `vk-restart` 目标，并去除了相关联的说明信息。
* **[MODIFY] [agent-quality-gate.sh](file:///mnt/d/aihci/hci-troubleshoot-platform/scripts/ci/agent-quality-gate.sh)**：去除了对 `vk-hooks.sh` 的加载，移除了成功/失败时对 `vk_on_cleanup` 的回调，同时将质检报告的输出路径从 `.vk/reports` 移至通用的 `build/reports`。

### 1.3 Gateway 路由重命名 (`tools_prompts` -> `capabilities`)
* **[NEW] [capabilities.py](file:///mnt/d/aihci/hci-troubleshoot-platform/backend/api-gateway/app/routes/capabilities.py)**：原 `tools_prompts.py` 重命名为 `capabilities.py`，其更能契合对 Tools, Prompts, Skills 代理的语义定义。
* **[DELETE] [tools_prompts.py](file:///mnt/d/aihci/hci-troubleshoot-platform/backend/api-gateway/app/routes/tools_prompts.py)**：彻底删除旧文件。
* **[MODIFY] [main.py](file:///mnt/d/aihci/hci-troubleshoot-platform/backend/api-gateway/app/main.py)**：更新了导入和 `router` 的注册指向。
* **[NEW] [test_capabilities.py](file:///mnt/d/aihci/hci-troubleshoot-platform/backend/api-gateway/tests/unit/test_capabilities.py)**：重构了对应的代理单元测试。
* **[DELETE] [test_tools_prompts.py](file:///mnt/d/aihci/hci-troubleshoot-platform/backend/api-gateway/tests/unit/test_tools_prompts.py)**：彻底删除旧测试文件。

### 1.4 后端僵尸代码与废弃模型下线
基于依赖分析，删除了以下未被引入或已过期的僵尸文件：
* **[DELETE] [kb_pusher.py](file:///mnt/d/aihci/hci-troubleshoot-platform/backend/case-service/app/services/kb_pusher.py)**：工单关闭的异步推送（已弃用）。
* **[DELETE] [admin_routes.py](file:///mnt/d/aihci/hci-troubleshoot-platform/backend/case-service/app/routes/admin_routes.py)**：未在 `main.py` 中挂载的质量评分统计 API。
* **[DELETE] [diagnostic_state.py](file:///mnt/d/aihci/hci-troubleshoot-platform/backend/conversation-service/app/models/diagnostic_state.py)**：旧的内存会话模型（其职责已由数据库持久化表 `diagnostic_item` 接管）。
* **[MODIFY] [__init__.py](file:///mnt/d/aihci/hci-troubleshoot-platform/backend/conversation-service/app/models/__init__.py)**：移除了废弃模型的导入与 `__all__` 导出。
* **[DELETE] [test_diagnostic_state.py](file:///mnt/d/aihci/hci-troubleshoot-platform/backend/conversation-service/tests/unit/test_diagnostic_state.py)** / **[test_diagnostic_stage_constants.py](file:///mnt/d/aihci/hci-troubleshoot-platform/backend/conversation-service/tests/unit/test_diagnostic_stage_constants.py)**：删除了与废弃的内存模型相关的测试用例。

---

## 2. 测试与验证结果

### 2.1 静态代码质量
运行 `make lint` 显示全部通过：
```
uv run ruff check backend/ tests/
All checks passed!
```

### 2.2 单元与集成测试
逐个运行了受改动影响的微服务测试集，全部通过：
* **`api-gateway`**：46 个用例全部通过（100% Passed），包括新的 `test_capabilities.py`。
* **`case-service`**：36 个用例全部通过（100% Passed），证实删除 `kb_pusher` 和 `admin_routes` 后系统无回归影响。
* **`conversation-service`**：155 个用例全部通过（100% Passed），证实删除内存状态机相关文件后各项逻辑完全正常。
