---
status: active
category: verify
audience: developer
last_updated: 2026-07-27
owner: team
---

# QFK 历史管道分步修复保存死锁验证

## 已完成验证

| 范围 | 命令 / 方法 | 结果 |
|------|---------------|------|
| 前端生产构建 | `corepack pnpm --dir frontend --filter @hci/admin build` | 通过；`vue-tsc -b` 和 Vite 构建成功 |
| 实际数据查证 | 查询 `kbd_entry.id=1` 的 `signals_json` | 确认 `signals[1]=sig_002` 与 `signals[2]=sig_003` 同时含管道，与 422 的 `signals[2]` 一致 |
| 服务日志查证 | `kb-service` 近 90 分钟日志 | 确认失败均为 `PATCH /api/admin/kbd/1` 返回 422，不是网络或数据库写入失败 |

## 部署后现场验证

1. 打开 KBD 27123 的第 2 条消费者信号，将 `lsof | grep {{VM}}` 点击“安全转换管道”。
2. 切换至第 3 条消费者信号，确认第 2 条显示“已暂存”。
3. 将第 3 条的旧 `ps auxf | grep` 改为人工确认后的不含管道基础命令。对该现场关键字判定用例，候选是 `ps auxf` + 现有 `ClwDRDBClient` 关键字；容器选择必须由审核人基于目标进程实际位置确认。
4. 点击保存，确认当前可以一次保存两条修改。
5. 再次查询 `kbd_entry.id=1`，确认所有 QFK `acquire.args.command` 不含 `|`，并使用真实工单运行验证产出/判定结果。

## 结论

本热修保持后端严格契约，解决的是审核过程中多条旧管道无法逐条修复的交互死锁。部署后现场验证通过前，不直接修改当前发布 KBD 的数据库记录。
