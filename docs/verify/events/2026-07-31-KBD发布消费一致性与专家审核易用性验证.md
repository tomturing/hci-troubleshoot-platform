---
status: passed_code_level
category: verify
audience: developer, tester, expert
last_updated: 2026-07-31
owner: team
---

# KBD 发布消费一致性与专家审核易用性验证

## 验证范围

本报告只证明代码、Schema 和 Admin 构建层通过，不把自动测试冒充真实 HCI Execution Replay 或 Expert Gold。

## 已验证不变量

| 不变量 | 结果 |
|---|---|
| Expert 发布写当前 `publish_validation` | 通过 |
| LLM 原 `generation_metadata.tool_contract_revision` 不覆盖 | 通过 |
| 当前发布盖章可解除“仅原生成 revision 旧”的 inventory 阻断 | 通过 |
| Agent compiler 接受当前盖章，同时继续编译信号 | 通过 |
| 旧 role/Contract 冲突按已有运行 Contract 回填 | 通过 |
| QKV 显式 END 与硬编码路径格式一致 | 通过 |
| task 高优先级锚点不被 alert 覆盖 | 通过 |
| threshold 从 df 读取 54 而非 1 | 通过 |
| Schema 生成文件无漂移 | 通过 |
| Admin TypeScript 与生产构建 | 通过 |

## 自动测试结果

```text
KB Service 全量测试
274 passed（1 条既有 RuntimeWarning）

Agent Service 全量测试
640 passed, 1 skipped（3 条既有 RuntimeWarning）

Signal Schema Contract
13 个契约文件合法
合法 fixture 通过
4 个非法 fixture 正确拒绝
生成漂移检查通过

Admin UI
vue-tsc -b 通过
vite build 通过
```

覆盖文件：

```text
backend/kb-service/tests/test_kbd_expert_maintenance.py
backend/kb-service/tests/test_playbooks.py
backend/agent-service/tests/unit/test_qkv.py
backend/agent-service/tests/unit/test_qfk.py
backend/agent-service/tests/unit/test_cdd_scheduler.py
backend/agent-service/tests/unit/test_kbd_differential.py
```

## 尚需部署后验证

1. KBD30880 maintenance 显式发布后的数据库主记录、最终 Expert Revision 和 Dynamic Resource Active 三方一致；
2. KB inventory `executable=true`，InvestigationAgent 未过滤；
3. KBD40061 的 alert/task 替代证据与 qfk_log 真实运行；
4. HCI 上 `acli system df /sf/log/` 的实际输出进入 matcher 后取 54；
5. qfk_log 按 END 定位、必要时解压并检索 `sfvt_vtpsh.log`；
6. Evidence/Execution Replay、真实使用审计和结论门控。

## 不得宣称

- 不得宣称 Embedding 失败问题已修复；
- 不得宣称 qfk_system 默认 host 或容器边界已完成；
- 不得宣称 126 KBD 已通过真实 Agent replay；
- 不得宣称 122 Proposal 已专家审核；
- 当前仍为 0/126 Expert Gold。
