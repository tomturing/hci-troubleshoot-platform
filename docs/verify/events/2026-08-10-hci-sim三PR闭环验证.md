---
status: active
category: verify
audience: qa, developer, sre, security
last_updated: 2026-08-11
owner: team
---

# hci-sim 三 PR 闭环验证矩阵

## PR1 退出证据

```bash
helm lint deploy/helm/hci-platform
helm unittest deploy/helm/hci-platform --strict
kubectl -n argocd get application hci-platform-dev
kubectl -n hci-dev get job,pod,secret,configmap
kubectl -n hci-sim-dev get deploy,svc,pod,secret,networkpolicy
```

必须保存：Argo revisions、migration 日志、四 schema/表计数、角色实际连接成功/失败输出、Bridge health/ready、SSH handshake、exec-result、trace/run ID 和无真实 HCI 访问证据。

### 已取得的数据库边界证据（2026-08-10 dev）

- migration 等价 Job 重跑幂等成功；`hci_sim` 存在，schema 表计数为 `control_plane=6`、`fixture=5`、`artifact=3`、`audit=1`；
- `hci_sim_runtime` 在目标表事务中完成 DML，CREATE TABLE 被拒绝；
- `hci_sim_migrator` DDL 成功；
- `hci_sim_runtime` 连接 `hci_troubleshoot` 被拒绝；平台 `hci_admin` 仍可连接；
- 以上只证明数据库迁移/权限边界，hci-sim Deployment 和 Bridge→Runtime E2E 仍为 pending。

### PR #718 自动门禁证据（2026-08-10）

当前 PR 末提交 `2da29de` 对应的 GitHub Actions run `31369315116`（CI）、`31369315128`（hci-sim-runtime）和 `31369315124`（数据库迁移）已全部通过：

- `docs-governance`、`hci-sim Go quality gate`、`hci_sim migration and permission boundary`、`published bundle and Helm gate`、`Helm 模板校验`、`Layer-2A 探针路径对齐检查` 均为 `SUCCESS`；
- `terminal bridge simulation-auth gate` 为 `SUCCESS`；变更范围之外的通用后端/安全/集成/镜像任务按路径正确 `SKIPPED`；
- `mergeStateStatus=CLEAN`。该证据只证明 PR #718 的代码/迁移/门禁，不等价于 hci-sim-dev 真实 Deployment 或产品级 E2E 已通过。

## PR2 退出证据

- API：KBD_ID build、进度、日志、TestRun create/status/cancel/retry/idempotency。
- DB：事务/CAS、outbox 重放、RLS/RBAC、stale 传播、Bundle digest/size/schema/signature 篡改拒绝。
- UI：构建前/中/后按钮状态、日志折叠/关闭、失败重试保留输入、创建工单自动填充 SSH 信息。
- E2E：27123、23821 和异构 KBD 各至少一条 published Bundle 经真实 Bridge/Agent 链路完成。

## PR3 退出证据

- 每个核心 KBD 绑定 revision、Bundle digest、Artifact provenance、variants、Oracle 和 owner。
- Differential、关键 mutation、20 次稳定性全部有 Evidence Manifest。
- 容量报告覆盖 1/10/50/100/200；100 场景零串线，200 场景显式 overload/降级。
- 故障演练覆盖 Lease revoke/expiry、Runtime/Bridge/Redis/DB 重启、网络隔离、secret scan 和 sim→real 阻断。

### 镜像发布与 GitOps 晋级证据

- CI run `31386141111` 的动态矩阵只包含 `hci-sim`，构建 job 成功，并发布
  `ghcr.io/tomturing/hci-troubleshoot-platform/hci-sim:20260810-1204-946af37`。
- buildx metadata 给出 OCI manifest digest
  `sha256:ff0732825a96db8858f03c8b6d574b57dd0dce1fe7c2e60153c6d7b5079cfd02`。
- `verify-hci-sim-gitops` 已加入 main-only 发布路径：从 GHCR 读取 digest、校验 Argo Application，发现漂移时不允许声明晋级成功；main ruleset 下的 digest 更新必须经过 PR。
- 当前提交尚未在 K3s 完成该 digest 的干净拉取/重建；因此后续 E2E 仍保持 `pending`，不得以旧 `dev-local` 运行结果替代。

## 证据判定规则

“代码通过”只能证明单元/渲染层；“部署健康”只能证明 Pod 可用；只有 API、数据库、UI、SSH、Bridge、Agent、Oracle 和清理证据共同存在，才可把对应阶段标为 `passed`。没有证据的项保持 `pending`，不允许用 synthetic 结果替代真实 Artifact 结果。

## PR3 已执行验证（2026-08-10）

| 检查 | 操作/证据 | 结论 |
|---|---|---|
| Helm 语法与渲染 | `helm lint deploy/helm/hci-sim`；分别以 `database.required=false` 和 `image.digest=sha256:abc` 渲染，确认 DB/控制面 Secret 引用与 `repository@digest` 输出 | passed |
| hci_sim Go 门禁 | `docker run --rm -v $WORKTREE/hci_sim:/src -w /src golang:1.20 go test ./...` | passed（2026-08-10，所有 Go 包通过） |
| 23821 digest | `go run ./cmd/hci-sim manifest-digest --manifest ../deploy/helm/hci-sim/files/kbd-23821-synthetic-fixture-manifest.json` 输出与 manifest digest 相同 | passed（`sha256:3577785d2f43ba621e16d8e126e1f8bac47c2f979e4b2087c18d460f7d5d738a`） |
| 23821 fixture 边界 | `deploy/helm/hci-sim/files/kbd-23821-synthetic-fixture-manifest.json` 含 `SYNTHETIC=true`、`support_id=23821` 和 facts boundary；未声明 approved real | passed（synthetic contract only） |
| Admin UI 路由/状态机 | `/simulation` 路由、KBD_ID 构建、进度/日志折叠、开始测试禁用条件、标题/描述持久化和失败重试保留输入；`corepack pnpm --filter @hci/admin build` | passed（未接入真实浏览器/外部控制面） |
| Gateway 代理 | `python3 -m compileall -q backend/api-gateway/app/routes/simulations.py backend/api-gateway/app/main.py backend/api-gateway/app/config.py` | passed；浏览器只访问 Gateway，Token 由服务间请求注入 |
| Gateway 单测环境 | `uv run --project backend/api-gateway pytest -q backend/api-gateway/tests/unit/test_gateway.py` | blocked（当前环境 CPython 3.14 与既有 `asyncpg==0.29.0` C 扩展不兼容；需按项目 CI 的 Python 版本重跑） |
| custom-ui 隔离 | PR3 基线提交 `089baf7` 删除仿真租约入口、`sim-ssh` 连接类型和 connection.json 解析 | passed |
| Runtime 控制契约 | 代码审查确认 build 绑定已加载 immutable fixture 并返回 environment context；TestRun API 拒绝非 `sim-ssh`、缺 TestRun ID 或 KBD/context 串线 | passed（未在无外部 DB/K3s 环境启动） |
| K3s 受管运行面 | PR #725 合入后已验证 Argo `Synced/Healthy`、immutable digest Pod 1/1、Gateway build/TestRun、K3s Bridge SSH 与 NetworkPolicy allow/deny | passed（证据见 PR #725 评论） |
| 持久化与 E 级产品证据 | Repository/CAS、Event/Result/outbox、Runner Result API 和 opt-in reconciler 已完成；真实 23821 Bundle、Agent context 重启现场、容量/差分/mutation/故障演练尚无原始证据 | partial / capability_gap |

### P1 Runtime 持久化接线验证（2026-08-10）

| 检查 | 操作/证据 | 结论 |
|---|---|---|
| 应用侧 UUID 与权限边界 | `RunRepository.Create` 使用 `uuid.New()`；迁移未要求 `pgcrypto`；Runtime 角色只执行 schema DML，不能 CREATE TABLE | passed（代码审查 + CI SQL 负向门禁） |
| hci_sim 连接启动 | `database.Open` 只接受数据库名 `hci_sim` 并 Ping；`HCI_SIM_DATABASE_REQUIRED=true` 时错误/空 URL 直接失败 | passed（单元测试 + 启动代码审查） |
| Build 持久化 | build API 在签发连接前写 Scenario/Run；`Idempotency-Key` 同摘要重放、异摘要 `409`；数据库写失败返回 `503` 且不返回 Lease | passed（代码路径 + Repository 集成门禁） |
| Lease/Attempt 持久化 | Lease 明文不入库，仅写 `sha256(JTI)`，并以 version/CAS 将 Run 推进到 `leased`；重复签发按同 JTI hash 幂等 | passed（Repository 集成测试；CI 配置已加入） |
| TestRun 显式绑定 | TestRun API 查询 Run，校验 support/KBD、Bundle digest、`sim-ssh` 和 TestRun ID 一致性；冲突返回 `409` | passed（代码路径；需 K3s 原始 HTTP/DB 日志补充） |
| environment context 持久化 | `control_plane.run.environment_context` 在 Build 事务中冻结 TestRun/KBD revision/Bundle/execution mode/virtual node/container；重放读取相同 JSONB | passed（迁移 + Repository 集成门禁；Agent Service 工单写库现场证据 pending） |
| Event | build/TestRun 和 SSH worker 将脱敏 digest 写入 `run_event`；同事务写 outbox；相同事件重放返回同一 seq | passed（Go/Repository PostgreSQL 集成测试） |
| Result | Result/Attempt/Run/outbox 在同一事务完成；相同结果幂等、不同结果拒绝；只允许受控对象 URI | passed（Repository PostgreSQL 集成测试；Runner Oracle 调用仍 pending） |
| outbox/reconciler | `FOR UPDATE SKIP LOCKED` claim、processed/pending 完成接口、Webhook 2xx ACK、无 sink 不假成功、processing lease 恢复和最大尝试次数已验证 | partial：K3s 常驻进程现场、死信告警和下游真实 ACK 证据 pending |
| Runner Result HTTP | `/v1/simulations/test-runs/{id}/result` 与 Gateway 代理接入 `RecordResult`，拒绝非 object URI/敏感 URI | passed（代码/编译门禁；真实 Runner Oracle 调用 pending） |
| 重启恢复 | 启动 reconciler 会恢复过期 processing；数据库 Run/Attempt/Event/Result 可重启后查询 | partial：尚无 K3s Pod 重启现场原始证据 |

### P1 Event/Result/outbox 验证（2026-08-10）

真实 PostgreSQL 15 临时数据库已执行：

```text
独立迁移：PASS（control_plane/fixture/artifact/audit 共 16 表）
Runtime DML：PASS
Runtime DDL：拒绝
Event append：PASS
Event duplicate replay：PASS（seq 不增长）
Result atomic finalize：PASS
Result duplicate replay：PASS
Conflicting Result：PASS（run_result_conflict）
Outbox claim/complete：PASS
Outbox no-sink no-false-success：PASS
Outbox webhook 2xx ACK：PASS
processing lease recovery：PASS
SSH positive exec digest event：PASS
SSH fixture_not_found digest event：PASS
```

事件断言同时检查 payload 只有 `sha256:` 加 64 位摘要；测试未把命令、stdout、stderr、Lease 或数据库密码写入 Event/Outbox。

### 最终收口工具验证

| 工具 | 默认行为 | 验证结果 |
|---|---|---|
| `scripts/hci-sim/capability-matrix.sh` | 未提供两个只读数据库 URL 时直接失败；缺 published Bundle 的 KBD 输出 `capability_gap` | `bash -n` 通过；未连接当前环境生产库，保持现场数据 pending |
| `scripts/hci-sim/stage-e-matrix.sh` | 未显式设置 `HCI_SIM_STAGE_E_ENABLE=1` 时退出码 2，不发起请求 | `bash -n` 通过；未获压力测试授权，保持 1/10/50/100/200 与 20-repeat pending |

真实 23821 approved Artifact/Bundle、126 KBD 全量运行结果和阶段 E 原始容量/故障/SLO 证据仍不存在，不能从 synthetic manifest 或脚本静态检查推导为 passed。

当前验证命令（使用项目固定 Go 容器，避免宿主机无 Go 造成误判）：

```bash
docker run --rm -v "$PWD/hci_sim:/src" -w /src golang:1.20 go test ./...
docker run --rm -v "$PWD/hci_sim:/src" -w /src golang:1.20 \
  go test ./internal/database ./internal/reconciler -count=1
```

第二条命令没有 `HCI_SIM_TEST_DATABASE_URL` 时必须明确 `SKIP`；CI 会先创建临时 PostgreSQL、执行独立迁移和 Runtime DML 授权，再运行同一测试。没有 CI/现场数据库原始日志时，不得把本地 Skip 表述为集成通过。

### 两步人工验收（当前实现契约）

1. 在 Admin UI 打开“仿真测试”，输入 KBD_ID，点击“环境构建”；必须看到构建进度和可展开日志，失败后重试且 KBD_ID/标题/描述仍保留。控制面返回的 connection 必须显示 `execution_mode=sim-ssh`、端口 2222、用户 `sim`、TestRun ID 和 Bundle digest。
2. 点击“开始测试”，补充标题和描述，点击“连接 SSH 并创建工单”；API 必须返回同一个 TestRun ID，且不得出现真实 HCI host/lease。若 K3s/Control Plane/Bundle 任一依赖未注入，页面必须显示明确错误并保持输入，不得降级到真实 HCI。

### 当前 Go/No-Go

- **Go（代码/渲染层）**：数据库 fail-closed、Helm Secret/digest/fixture 选择、Admin UI 入口和 sim context 校验。
- **No-Go（产品宣称）**：没有 K3s Deployment/Bridge→Runtime 原始日志、PostgreSQL Repository、approved real 23821 Bundle 和 E 级 Evidence Manifest 前，不得宣称“端到端已完成”或“126 个 KBD 可验证”。
