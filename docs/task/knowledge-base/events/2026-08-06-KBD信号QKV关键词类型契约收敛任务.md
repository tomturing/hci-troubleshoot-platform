---
status: active
category: task
audience: developer
last_updated: 2026-08-06
owner: team
---

# KBD 信号 QKV 关键词类型契约收敛任务

## 任务范围

针对 `qkv_task.acquire.args.keyword` 被 LLM 抽取为数组的问题，完成 Prompt、共享门禁、测试和文档闭环。实现不放宽 QKV 的单字符串执行契约。

## 任务清单

| 任务 | 文件/范围 | 状态 |
|------|-----------|------|
| T-KB-QKV-TYPE-1 | `database/seeds/02_system_prompts.sql`：增加类型矩阵，去除裸 keyword 数组表述 | ✅ 完成 |
| T-KB-QKV-TYPE-2 | `database/data-migrations/023_align_qkv_keyword_type_contract.sql`：存量 Prompt 2.3 幂等归一与断言 | ✅ 完成 |
| T-KB-QKV-TYPE-3 | `backend/shared/schemas/acquirer_args.py`：保留 string 门禁并增加 `QKV_KEYWORD_MUST_BE_STRING` | ✅ 完成 |
| T-KB-QKV-TYPE-4 | `backend/kb-service/tests/test_acquire_args_contract.py`：QKV 标量/数组/错误码回归 | ✅ 完成 |
| T-KB-QKV-TYPE-5 | `backend/kb-service/tests/test_system_prompt_contracts.py`：Prompt 字段边界回归 | ✅ 完成 |
| T-KB-QKV-DOC | 方案、知识库现行文档、README、验证避坑同步 | ✅ 完成 |

## 验收命令

```bash
cd /aihci/.worktrees/acquire-keyword-diagnosis
PYTHONPATH=backend uv run pytest backend/kb-service/tests/test_acquire_args_contract.py backend/kb-service/tests/test_system_prompt_contracts.py -q
```

## 后续观测

部署后检查 `system_prompt` 的实际版本是否为 2.3，并统计 `QKV_KEYWORD_MUST_BE_STRING`。如果仅剩历史单元素数组，再评估增加显式开关控制的 unwrap 兼容层；多元素数组继续 fail closed。
