---
status: active
category: task
audience: developer
last_updated: 2026-08-06
owner: team
---

# KBD 数据管道稳定性与可观测性任务

## 实施项

- [x] 将 VISION、CLASSIFY 依赖改为共同依赖 IMPORT。
- [x] 两阶段并行调度；EXTRACT_SIGNALS 使用两者成功结果交集作为硬门禁。
- [x] 分类改用结构化 KBD 字段，不再从 `content_md` 反向解析。
- [x] 案例级 Vision 结果与图片级统计分离，并记录状态不一致。
- [x] 扩展进度状态：warning、blocked_by_dependency、skipped_existing、not_applicable。
- [x] 分类/识图/信号抽取 API 客户端增加 429、Retry-After、传输错误与 jitter 退避。
- [x] kb-service 增加共享 LLM 并发舱壁和统一业务重试。
- [x] 新增交互式 `cli`、`--json` 和人类可读 Pipeline 总结。
- [x] 增加 JSONL 排障日志并抑制默认 httpcore 噪声。
- [ ] Redis/数据库持久化 Vision/Signal Job 状态（后续部署专项，需先完成多副本契约验证）。

## 变更文件

见方案事件文档中的影响范围；实现不修改 KBD 管理页面导入功能。

## 完成定义

代码、单测、事件文档、现行文档变更历史和 README 第一屏全部进入同一 PR；验证报告明确区分自动化通过项与尚未进行的真实 Provider/20 KBD 故障注入项。
