---
status: completed
category: task
audience: developer, tester, operator
last_updated: 2026-08-03
owner: team
---

# KBD 关键信号抽取反馈闭环与版本治理任务

## 目标

修复 KBD30880 暴露的专家修改统计与关键信号抽取回归，并落实“当前状态、不可变血缘、筛选数据集”分层版本治理。

## 实施任务

### Revision 与评估配对

- [x] 为 `kbd_revision` 增加 `baseline_proposal_revision_id`；
- [x] Atlas migration 沿 parent 链回填历史 Expert 的最近 Proposal；
- [x] 新 Expert 创建时冻结 Proposal baseline；
- [x] 相同 payload、不同 baseline 不跨基线复用 revision；
- [x] 历史缺失字段时保留 parent 链兼容解析；
- [x] evaluation export 只选择 approved/published Expert；
- [x] evaluation export 按真实 Proposal 计算 `diff_from_proposal`；
- [x] 保留完整历史，不增加物理删除逻辑。

### 专家页面统计

- [x] 新增信号级专家修改摘要；
- [x] 新增、删除、修改按稳定 signal ID 计数；
- [x] 同一信号多字段变化只计一条；
- [x] 无当前 Expert 时返回 0；发布后的当前 approved/published Expert 仍按其 Proposal 统计，新重抽后旧 Expert 不跨基线复用；
- [x] 移除 `history[0].diff_from_parent.length` 的错误产品语义；
- [x] 文案改为“专家修改：N 条关键信号”；
- [x] 重抽后刷新 detail、revision 和 validation。

### 抽取质量

- [x] 自动抽取缺失/10/30 秒 timeout 归一为 120 秒；
- [x] 拒绝无人消费的 QFK producer；
- [x] 自引用不视为下游消费；
- [x] 保留 rejected candidate 与明确原因；
- [x] `/sf/cfg` 配置读取改为当前 `command + command_args` 契约；
- [x] 新增 Prompt data migration 019，将已部署 Prompt 升至 v1.8；
- [x] 同步全新环境 seed Prompt；
- [x] 明确任务 producer、多图 refs、参考截图、配置 matcher 和默认 timeout 规则；
- [x] 不增加 KBD30880 特判。

### 测试与治理

- [x] revision summary、baseline 解析和评估候选单测；
- [x] timeout、dead producer、配置文件命令模型回归；
- [x] Prompt seed/data migration 占位符契约测试；
- [x] Admin UI TypeScript/生产构建；
- [x] 相关 KBD 生命周期与维护测试；
- [ ] 合并部署后在 dev 重抽 KBD30880，验证真实 LLM 输出为 1 qkv_task + 2 qfk_system；
- [ ] 后续独立实现 retention class、数据集锁定与安全 GC，不在本 PR 删除历史。

## 发布后验收查询

1. 查询 KBD30880 ��前 `latest_proposal_revision_id` 与 `working_revision_id`；
2. 专家未编辑时确认摘要 `changed_signal_count=0`；
3. 重新抽取并检查 rejected candidates 中不存在落库的 dead producer；
4. 核对三条目标信号的 tool、requires/produces、match 和 timeout；
5. 保存一次 Expert working，确认 `baseline_proposal_revision_id` 指向本轮 Proposal；
6. 核对 evaluation export 不包含 working revision；
7. 发布后检查 runtime revision 与 usage audit 仍可定位。

## 回滚

- 应用回滚不删除已创建 revision；
- Prompt 通过激活前一受控版本回滚，不改写历史 Proposal；
- 新列为可空，旧应用可忽略；
- 发现抽取质量异常时保留 Bad Case，新建前向 Proposal 修复，不覆写历史。
