---
status: completed
category: task
audience: developer, tester, operator
last_updated: 2026-08-04
owner: team
---

# KBD 关键信号只读边界任务

## 目标

让 KBD 正常只读 Signal 继续保存，并将处置/变更候选以原文和可读错误交给专家处理；
不引入 `sed` 专项识别或通用 Shell 风险引擎。

## 实施任务

### 领域门禁

- [x] 新增 KBD 专用只读判定与整文档校验；
- [x] 复用平台现有明确写动作词表；
- [x] `phase=solution` 在 QFK `match XOR produces` 之前拒绝；
- [x] rejected candidate 保留完整原文与专家处理建议；
- [x] 正常只读 Signal 保持通过；
- [x] 不增加 `sed -i/--in-place/-n` 专项逻辑。

### 保存与运行时

- [x] KBD 专家工作稿保存接入只读门禁；
- [x] 审核发布、维护发布、重新发布接入只读门禁；
- [x] Admin 审核问题映射保留稳定 Signal ID；
- [x] Agent 复用共享判定阻断历史 solution/明确写动作；
- [x] SOP 通用 Schema 和保存路径保持不变。

### Prompt 与迁移

- [x] seed Prompt 升至 v1.9；
- [x] 删除“写操作可作为 solution Signal”的冲突规则；
- [x] 新增 data migration 020 前向更新已部署 Prompt；
- [x] migration 不修改 KBD 正文、Signal、Revision 或数据库结构；
- [x] 增加 seed/migration 占位符与语义契约测试。

### 验证

- [x] 自动抽取处置候选拒绝顺序与审计原文测试；
- [x] 正常只读 `cat` Matcher 通过测试；
- [x] 专家维护保存拒绝测试；
- [x] Agent 历史信号纵深防御测试；
- [x] Prompt seed/data migration 契约测试；
- [ ] 合并部署后用已修正文的案例重新抽取并由专家确认结果。

## 发布后检查

1. 在 dev 确认 migration 020 后 Prompt 版本为 1.9 且存在“补充规则 24”；
2. 对已修正“缺少”的案例重新抽取；
3. 正常 `cat` 配置检查应进入 Signal，处置候选不得进入 `signals`；
4. 如仍出现处置候选，检查 `rejected_candidates` 是否包含完整原文和可读原因；
5. 专家修正正文或把动作放回解决方案后再次抽取，不直接编辑候选为可执行动作。
