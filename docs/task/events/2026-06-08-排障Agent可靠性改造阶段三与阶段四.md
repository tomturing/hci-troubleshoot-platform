# 阶段三与阶段四任务清单

## 阶段三：结构化推理与反幻觉 (T3)
- [x] T3-1: SOP 决策树滑动窗口优化与前置条件拼接
  - [x] 修改 `app/tools/sop/nav.py` 在 `children_summary` 中包含 `prerequisites`
  - [x] 修改 `investigation_agent.py` 格式化 `branches_list` 时拼接前置条件
- [x] T3-2: 关键节点输出 Schema 化与降级策略
  - [x] 定义 `backend/shared/models/reliability.py` 模型
  - [x] 在 `__init__.py` 中导出结构化模型
  - [x] 改造 `ReactEngine.execute` 支持 `response_schema` 并反序列化
  - [x] 实现 Schema 校验失败时的降级拦截（阻止 risk_level >= 2 执行）
  - [x] 在 `InvestigationAgent` 中根据阶段注入 Schema
- [x] T3-3: CoT 思考过程外显与前端折叠展示
  - [x] 在提示词中注入 `<reasoning>` 标签要求
  - [x] 前端 `MessageBubble.vue` 支持 `<reasoning>` 自动解析与折叠展示
- [x] T3-4: 轻量版 `HallucinationDetector` 规则引擎
  - [x] 创建 `app/services/hallucination_detector.py`
  - [x] 实现 `phantom_tool_reference` 检测规则
  - [x] 实现 `overconfident_claim` 检测规则
  - [x] 实现 `ungrounded_number` 数值交叉核对
  - [x] 在 `ReactEngine` 与 `InvestigationAgent` 中集成检测、报告及 Re-run 重试
- [x] T3-5: 验证节点强制绑定（校验优先闭环）
  - [x] 在 `ReactEngine` 中自动检测未验证的写操作，拦截过早 Closure 并引导 LLM

## 阶段四：评测闭环 (T4)
- [x] T4-1: 黄金工单评测集与 Replay Runner
  - [x] 创建 `evaluation/golden_tickets.json` 并插入 30 条用例
  - [x] 实现 `evaluation/replay_runner.py` 评测框架
- [x] T4-2: 核心可靠性指标接入 Prometheus
  - [x] 实现 `backend/agent-service/app/services/metrics.py`
- [x] T4-3: Fact Store 迁移到 PostgreSQL 持久化与 Redis 缓存
  - [x] 编写 PostgreSQL migration sql 脚本
  - [x] 在 `shared/models/fact.py` 中定义 SQLAlchemy ORM
  - [x] 修改 `FactStore` 适配 PostgreSQL + Redis 双轨读写
- [x] T4-4: CI 回归评测门禁脚本
  - [x] 实现 `scripts/ci/agent-quality-gate.sh`
- [x] 验证与回归
  - [x] 编写单元测试 `test_reliability_phase3.py`
  - [x] 编写单元测试 `test_reliability_phase4.py`
  - [x] 运行全量单元测试并确保通过
