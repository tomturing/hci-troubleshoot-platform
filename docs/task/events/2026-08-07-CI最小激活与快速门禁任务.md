---
status: active
category: task
audience: developer
date: 2026-08-07
author: Codex
trigger: CI 最小激活与快速门禁方案
---

# CI 最小激活与快速门禁任务

## 1. 已实施任务

- [x] 在 `hci-sim-go.yml` 增加一次性变更探测 job；
- [x] 将 runtime、Bridge、Manifest/Helm gate 改为按输出条件启动；
- [x] 为 hci-sim workflow 增加分支级并发取消与 job 超时；
- [x] 删除 `build-terminal-bridge.yml` 的 PR 触发；
- [x] 将 `docs-governance` 与 CI 变更探测合并，保持 required context 名称；
- [x] 将 Signal Schema Contract 并入 Lint，共用一次 `uv sync`；
- [x] 收窄平台 Helm 过滤器，hci-sim chart 由 hci-sim-runtime 保护；
- [x] 删除 Python 后端测试对 Helm、探针和 Lint 的无效等待，恢复并行关键路径；
- [x] 为自动生成的 exe PR 同时加入 `[skip docs]` 与 `[skip doc-sync]`；
- [x] 对只含 CI 生成 exe 的前端变更跳过 pnpm 门禁。

## 2. 任务—证据映射

| 任务 | 代码位置 | 验证方式 |
|---|---|---|
| hci-sim 路径分流 | `.github/workflows/hci-sim-go.yml` | workflow YAML 解析 + GitHub PR check 矩阵 |
| required context 兼容 | `.github/workflows/ci.yml` | ruleset API 与 PR check 名称对照 |
| 重复依赖安装消除 | `ci.yml` lint/schema | job 步骤审计；同 job 仅一次 `uv sync` |
| 无效 needs 移除 | `ci.yml` unit/integration/security | DAG 审查与模拟条件测试 |
| exe PR 缩减 | `build-terminal-bridge.yml` | 事件触发审查；main push 手工 smoke |

## 3. 后续任务（独立 PR）

- [ ] 采集 20 个 PR 的 job 排队/执行/关键路径数据，确认 P50/P95；
- [ ] 为 main push 建立 Dockerfile、共享库、锁文件到 service 的影响矩阵；
- [ ] 用动态矩阵仅构建受影响镜像，并对共享基础变更保留全量 fallback；
- [ ] 将第三方 action 固定到经审计的 commit SHA；
- [ ] 对数据库 workflow 增加 Atlas CLI 与 PostgreSQL 镜像 digest 固定；
- [ ] 将非阻断文档评论改为单个汇总评论，减少 API 调用和噪音。

## 4. 回滚步骤

若 GitHub required check 因 job skipped 被错误判定为缺失，可在同一 PR 中暂时恢复原 job-level 条件；无需回滚业务代码。若发现 hci-sim 某路径漏触发，先将该路径加入 `changes` filter，再重跑 PR。
