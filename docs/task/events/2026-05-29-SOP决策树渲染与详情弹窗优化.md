# SOP 决策树前端渲染与弹窗改造追踪任务

- `[x]` 新建递归树节点组件 `SopTreeNode.vue`
  - `[x]` 编写组件的 `props` 与 `emits` 结构
  - `[x]` 设计前置条件（prerequisite_items）优雅渲染（filter 与 priority 彩色徽章）
  - `[x]` 编写变量声明表格（variables）的高美感渲染
  - `[x]` 设计叶子节点诊断详情（acli 命令终端黑色背景框加一键复制，分析步骤与可能原因）
  - `[x]` 设计叶子节点修复方案（快速恢复与彻底解决）
  - `[x]` 实现子节点向下的递归调用，依靠同级缩进防止宽度挤压
- `[x]` 重构 `SopManageView.vue` 中的详情查看弹窗
  - `[x]` 导入 `SopTreeNode` 组件
  - `[x]` 添加响应式状态 `treeLoading`、`treeData` 以及 `expandedKeys` (Set)
  - `[x]` 完善接口请求方法 `loadTreeData(docId)` 从 `/api/v1/sop/{id}/tree` 抓取数据
  - `[x]` 修改 `openViewDialog` 让其打开时加载数据，且默认全部展开
  - `[x]` 编写全部展开 `expandAll()` 和全部折叠 `collapseAll()` 的核心逻辑
  - `[x]` 改造 `el-dialog` 模板布局为双栏，启用 `draggable` 与全屏切换模式
  - `[x]` 精雕细琢 CSS 样式，增加 hover 微动画与现代过渡效果
- `[x]` 验证与回归测试
  - `[x]` 检查 Element Plus 与 Vue 组件编译无报错
  - `[x]` 验证展开/折叠、全屏、拖拽等交互响应情况
  - `[x]` 回归并确认识别无内容挤压情况
