---
status: active
category: task
audience: developer, tester, operator
last_updated: 2026-07-28
owner: team
---

# KBD 关键信号结果展示与 ps 输出提取方案任务

> 对应方案：[KBD关键信号结果展示与ps输出提取方案](../../solution/events/2026-07-28-KBD关键信号结果展示与ps输出提取方案.md)

## 任务清单

- [x] 查询 `Q2026072804914` 的 conversation、trace、第三步工具结果和 bridge 原始/筛选字节数。
- [x] 查询 `Q2026072803376` 的 conversation、trace、第三步工具结果和稳定错误码。
- [x] 对比 `pid=,args=` 与 `cmd=` 的物理输出，区分偶然 PASS 与正确采集。
- [x] 复核 KBD 27123 当前第三步仍保留 `include=["{{PID}}"]` 的错误组合。
- [x] 明确第三步业务语义为“PID 对应进程存在且命令行仍关联 VM 镜像”，不引入特定进程关键字。
- [x] 为 `StepResult` 增加逐 signal 的 `produced_variables`。
- [x] 在工具事件和 diagnostic_item 中保留产出变量及既有审计字段。
- [x] 重构关键信号报告为检查说明、状态、检查结果和提取信息。
- [x] 隐去主报告中的 exec/evaluation ID、参数 JSON 和 raw output。
- [x] 增加稳定错误码用户态翻译和 BLOCKED 缺失变量展示。
- [x] 增加 VM/HOST/END/PID/CMD 中文标签、单行化和展示截断。
- [x] 修改 golden test 使用 `ps -p {{PID}} -o cmd=`、`include=["{{VM}}"]` 和 `CMD`。
- [x] 增加旧 PID include 导致 `QFK_NO_MATCH` 的反例测试。
- [x] 增加 stderr 噪音、内部 ID 不进入报告的回归测试。
- [x] 更新需求、方案、任务、验证事件及现行全量文档。
- [ ] 由 KBD 审核人员将环境中 KBD 27123 第三步的 include 改为 `{{VM}}`、requires 补齐 `VM` 并发布新 revision。
- [ ] 合并部署后创建新工单，确认三步通过且页面使用新报告样式。

## 文件级实施范围

| 文件 | 任务 |
|---|---|
| `backend/agent-service/app/adapters/agents/htp/kbd_differential.py` | 捕获产出变量、用户态 evidence formatter |
| `backend/agent-service/tests/unit/test_kbd_differential.py` | 三步 golden、报告成功/失败/阻断回归 |
| `backend/agent-service/tests/unit/test_qfk_extractor.py` | `cmd=` 正反例提取测试 |
| `docs/solution/knowledge-base/KBD证据诊断与CDD闭环设计.md` | 更新当前报告分层和第三步业务语义 |
| `docs/task/agent/Agent可靠性改造任务清单.md` | 登记完成项与待人工发布项 |
| `docs/verify/测试指南.md` | 更新 golden chain 和验收断言 |

## 回滚边界

- 报告格式可随 agent-service 镜像回滚；审计记录字段为向后兼容新增字段。
- 本次不自动修改数据库中的已发布 KBD revision，代码回滚不会造成知识数据漂移。
- 若报告格式回滚，原始工具事件和审计数据仍在，不影响 CDD 结论门禁。
