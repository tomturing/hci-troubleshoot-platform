# 任务清单：SSE 空闲保活治理

- [x] T1 配置项：`conversation-service` 新增 `SSE_HEARTBEAT_ENABLED` / `SSE_HEARTBEAT_INTERVAL_SEC`（默认开启 / 20s）。
- [x] T2 后端包装器：实现 `sse_keepalive` 异步生成器（队列 + 心跳任务 + 源结束/取消清理）。
- [x] T3 接入两条 SSE 流：`POST /api/conversations/{id}/message` 与 `GET /resume-stream`。
- [x] T4 指标/日志：`SSE_KEEPALIVE_TOTAL` 计数 + `sse_keepalive_start` 结构化日志。
- [x] T5 前端解析：两个 SSE 解析循环显式跳过 `:` 注释行。
- [x] T6 前端体验：网络错误区分 + `sseNetworkError` 状态 + `ChatWindow` 可重试横幅 + `retryLastMessage()`。
- [x] T7 测试：`backend/conversation-service/tests/unit/test_sse_keepalive.py` 5 项单测。
- [x] T8 文档：requirement / solution / task 事件文档 + 配置契约登记（`SSE_HEARTBEAT_ENABLED` / `SSE_HEARTBEAT_INTERVAL_SEC`）。
- [ ] 后续（独立议题）：LLM 读超时配置化收敛、单轮诊断墙钟上限治理。
