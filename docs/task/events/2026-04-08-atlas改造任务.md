---
status: active
category: task
audience: developer
last_updated: 2026-04-08
owner: team
---

# 任务：Atlas 声明式数据库发布改造

> 对应方案文档：[2026-04-08-atlas声明式schema方案](../../solution/events/2026-04-08-atlas声明式schema方案.md)  
> 对应需求文档：[2026-04-08-atlas数据库改造需求](../../requirement/events/2026-04-08-atlas数据库改造需求.md)

---

## 完成标准

**所有任务完成，PR 合并后**：

1. PR 中不存在 `scripts/ci/sync-db-migrations.sh`
2. PR 中不存在 `deploy/helm/.../db-migrations-configmap.yaml`
3. `ci.yml` 中不存在 `check-db-migrations-sync` job
4. `ci.yml` 中或单独 workflow 存在 `db-migration-test` job
5. `deploy/helm/.../db-migrate-job.yaml` 使用 Atlas 镜像（`ghcr.io/ariga/atlas:latest` 基础的自定义镜像）
6. `atlas.hcl` 存在于项目根目录
7. `database/desired_schema.sql` 存在且包含 17 张业务表定义
8. `database/atlas-migrations/20260408000000_baseline.sql` 存在
9. `Dockerfile.migrations` 存在且可构建

---

## 任务清单

### T1：Atlas 工具链配置

- [x] 创建 `atlas.hcl`（根目录，env: local / ci / prod）
- [x] 创建 `database/desired_schema.sql`（从 `schema_design.sql` 清理 `schema_migrations` 表后的期望状态）
- [x] 创建 `database/atlas-migrations/` 目录
- [x] 创建 `database/atlas-migrations/20260408000000_baseline.sql`（完整建表 SQL，供全新 DB 使用）
- [x] 生成 `database/atlas-migrations/atlas.sum`（运行 `atlas migrate hash` 生成）

### T2：迁移镜像化

- [x] 创建 `Dockerfile.migrations`（FROM ghcr.io/ariga/atlas:latest + COPY atlas-migrations）
- [x] 在 `ci.yml` 的 build-and-push 矩阵中添加 `db-migrate` 服务
- [x] 更新 Helm values 中的 `dbMigrate.image` 配置

### T3：Helm Job 更新

- [x] 更新 `deploy/helm/hci-platform/templates/hooks/db-migrate-job.yaml`
  - 删除 ConfigMap volume mount
  - 改用 Atlas 自定义镜像
  - 命令改为 `atlas migrate apply`
  - 添加 `--baseline` 参数支持（通过 Helm values 控制）
- [x] 更新 `deploy/helm/hci-platform/values.yaml`（删除 dbmate 相关注释，更新 image 配置）

### T4：删除旧方案

- [x] 删除 `deploy/helm/hci-platform/templates/hooks/db-migrations-configmap.yaml`
- [x] 删除 `deploy/helm/hci-platform/templates/hooks/db-migrations-configmap.yaml.bak`
- [x] 删除 `scripts/ci/sync-db-migrations.sh`
- [x] 删除 `scripts/ci/check-db-migrations-sync.sh`

### T5：CI 验证流程

- [x] 删除 `ci.yml` 中的 `check-db-migrations-sync` job
- [x] 创建 `.github/workflows/db-migration-test.yml`（PR 阶段 DB 全量验证）
  - postgres service container
  - `atlas migrate lint`（危险操作检测）
  - `atlas migrate apply`（全量执行验证）
  - 幂等性验证（第二次执行 0 变更）

### T6：文档同步

- [x] 更新 `docs/solution/数据库设计.md`（v6.2.4：迁移工具切换）
- [x] 更新 `docs/task/数据库任务.md`（添加 Atlas 改造任务）
- [x] 更新 `docs/deploy/发布指南.md`（数据库迁移章节）
- [x] 创建本文件

### T7：环境仓库变更（PR 合并后）

> **注意**：此任务在本 PR 合并后，在 hci-platform-env 中单独完成。

- [ ] 更新 dev values.yaml：添加 `dbMigrate.atlasBaseline: "20260408000000"`
- [ ] 更新 staging values.yaml：添加 `dbMigrate.atlasBaseline: "20260408000000"`
- [ ] 验证 dev 环境 Atlas 首次运行成功
- [ ] 验证 staging 环境 Atlas 首次运行成功
- [ ] prod 环境：审批后添加 baseline 配置并验证

---

## 执行记录

| 任务 | 执行者 | 完成时间 | 备注 |
|------|--------|---------|------|
| T1-T6 | AI Agent | 2026-04-08 | PR #xxx |
| T7 | DevOps | 待执行 | 环境仓库单独 PR |
