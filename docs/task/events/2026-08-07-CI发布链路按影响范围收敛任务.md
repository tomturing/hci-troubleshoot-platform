---
status: active
category: task
audience: developer
last_updated: 2026-08-07
owner: team
---

# CI 发布链路按影响范围收敛任务

| 编号 | 任务 | 状态 | 验证 |
|---|---|---|---|
| CI-MIN-01 | 新增镜像影响闭包解析器和动态 matrix 输出 | ✅ 完成 | Python 合成场景测试 |
| CI-MIN-02 | 将 db-migrate 变更判断前移至 changes 输出 | ✅ 完成 | database-only 计划验证 |
| CI-MIN-03 | 让环境仓库只同步本次实际构建服务 | ✅ 完成 | 空服务列表 shell 验证 |
| CI-MIN-04 | 将文档命名/模块同步合并到 docs-governance runner | ✅ 完成 | 两个独立 workflow 删除、YAML 检查 |
| CI-MIN-05 | 为构建、发布、同步 job 增加超时和同组并发保护 | ✅ 完成 | workflow 静态检查 |
| CI-MIN-06 | 固定第三方 Actions commit SHA | ✅ 完成 | workflow 中无 `@v*`/`@master` |
| CI-MIN-07 | 增加手动 CI 时延 P50/P95 报告 | ✅ 完成 | 脚本样例输入验证 |
| CI-MIN-08 | GitHub runner 上执行完整 workflow/actionlint 与 main 发布回归 | ⏳ 待 PR CI | 合并前必做 |

## 回滚

若动态计划影响判断异常，临时从 `workflow_dispatch` 运行完整构建；若需快速回滚代码，可恢复固定 matrix，保留测试和文档治理合并逻辑。
