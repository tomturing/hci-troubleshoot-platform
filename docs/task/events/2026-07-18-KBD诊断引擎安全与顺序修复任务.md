---
status: active
category: task
audience: developer
last_updated: 2026-07-18
owner: team
---

# KBD 诊断引擎 安全与顺序修复 — 任务清单

> 镜像方案事件：[2026-07-18-KBD诊断引擎安全与顺序修复分析.md](../solution/events/2026-07-18-KBD诊断引擎安全与顺序修复分析.md)
> 本文档跟踪该方案的**落地执行任务**与完成状态。

## 任务清单

| # | 任务 | 文件 | 状态 |
|---|------|------|------|
| T1 | 补全 `acli system` 写操作 risk=2/3 规则，修复 kill 免确认的安全倒挂 | `backend/agent-service/app/tools/acli/classifier.py` | ✅ 已完成 |
| T2 | 抽取输入剥离 `root_cause`/`solution`；新增写操作词表与 `_is_write_op_signal`/`_write_op_risk`；`_validate_signal` 标注 `phase`/`require_human_confirm`/`risk` | `backend/kb-service/app/routes/extract_signals.py` | ✅ 已完成 |
| T3 | 新增 `KBD.get_signal(tool_name)` 供执行层取原始信号做门禁 | `backend/agent-service/app/adapters/agents/htp/kbd_model.py` | ✅ 已完成 |
| T4 | 候选确定性排序（`-similarity, id`）；`_signal_requires_human` 门禁（贪心 + 确认分支均拦截写操作并 `yield AgentInteractiveRequest`）；报告只返 `root_cause`/`solution` 原文 + 证据 + 标题超链接 | `backend/agent-service/app/adapters/agents/htp/kbd_differential.py` | ✅ 已完成 |
| T5 | 可选：清理 prompt 模板中"根因/解决方案"输入行（补一条 DB 迁移 SQL） | `database/data-migrations/` | ⏳ 待定（代码已传空字符串等效达成） |
| T6 | 确认分支改为按 KBD 内容顺序遍历全部 `signals`，前端生产者纳入证据（修复"跳过 qkv_task"的设计缺口） | `backend/agent-service/app/adapters/agents/htp/kbd_differential.py` 确认分支 | ✅ 已完成 |

## 验收（对齐方案事件第五节）

- [x] 写操作信号带 `require_human_confirm`/`phase="solution"`/`risk≥2`
- [x] 单候选诊断写操作只记录 + 推送人工授权，不自动执行
- [x] `acli system kill -9`/`reboot` 判 risk≥2；`ps auxf`/`lsof` 保持 risk=1
- [x] 相同输入多次诊断结论可复现
- [x] 报告仅含原文 + 证据 + 可点击标题超链接

## 待办（关闭循环前）

- [ ] 若 `solution/AI助手/AI助手设计.md` 描述诊断引擎执行顺序/报告格式，追加变更历史
- [ ] 同步 `docs/README.md` 第一屏（如有相关里程碑变化）
