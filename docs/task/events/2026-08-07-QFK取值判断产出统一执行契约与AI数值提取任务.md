---
status: active
category: task
audience: developer, tester
last_updated: 2026-08-07
owner: team
---

# QFK 取值、判断与产出统一执行契约实施任务

对应方案：[QFK 取值判断产出统一执行契约与 AI 数值提取方案](../../solution/events/2026-08-07-QFK取值判断产出统一执行契约与AI数值提取方案.md)

## 任务目标

在公共 QFK 模块完成“候选行取值 → 类型化中间结果 → Matcher 判断/变量产出”的一致实现，修复 KBD23821 暴露的顺序缺陷，同时保持 Catalog 能力边界和旧非数值 Matcher 兼容。

## 实施清单

| 编号 | 范围 | 交付物 | 状态 |
|---|---|---|---|
| QFK-01 | AI extractor | 支持 `number`/`array<number>`、原始字面量逐字回查、统一候选预算 | 已完成 |
| QFK-02 | Matcher/Engine | 数值 Matcher AI 取值先行；非数值 Matcher 保持命中后证据提取；delta 首末差值 | 已完成 |
| QFK-03 | Schema/Catalog | AI numeric 通道有条件放行；无 AI 时保留 source 能力错误；产出名唯一 | 已完成 |
| QFK-04 | Produce | AI/普通取值全部成功后原子写入变量池，复用同一 Extract/AI 实现 | 已完成（既有链路复核） |
| QFK-05 | Admin | 统一 ValueExtract；区分 Matcher 数值提示与 Produce 类型化产出提示 | 已完成 |
| QFK-06 | 回归验证 | Agent、KB Schema、Admin 构建/类型检查、ruff、文档同步、PR #698 CI | 进行中 |

## 验证记录

- Agent QFK/AI 定向测试：覆盖 numeric AI-before-judgement、KBD23821 delta 相等/不等、threshold、trend、grounding。
- KB Schema：覆盖 qfk_log 无 AI 拒绝、有 AI numeric 放行、重复产出名拒绝。
- 前端：执行 `vue-tsc` 与生产构建；Vitest 若 CI 安装完整开发依赖则执行组件回归。
- 提交前运行 `scripts/ci/check_module_doc_sync.py`，并确认 PR #698 所有检查状态。

## 完成定义

代码、文档、测试和 PR #698 同步提交；所有失败路径均可从 Signal ID、字段路径、source/parser、提取类型和错误码定位；不引入 KBD23821 或单文件特例。
