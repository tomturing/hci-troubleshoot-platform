---
status: active
category: task
audience: developer
last_updated: 2026-04-17
owner: team
---

# AI 助手选择器交互优化任务

## 关联方案
[../solution/events/2026-04-17-助手切换优化方案.md](../solution/events/2026-04-17-助手切换优化方案.md)

## 任务清单

### T1: 后端 Schema 扩展 — MessageCreate 新增 assistant_type 字段
- **描述**：在 `backend/shared/models/schemas.py` 的 `MessageCreate` 类中添加可选字段 `assistant_type: str | None = None`
- **文件变更**：
  - `backend/shared/models/schemas.py` — 新增字段
- **验收标准**：
  - Schema 定义正确，字段可选
  - Pydantic 验证通过
- **依赖**：无
- **预计耗时**：15 分钟
- **状态**：待开始

### T2: 后端路由改造 — send_message 传递 assistant_type 参数
- **描述**：在 `backend/conversation-service/app/routes/conversations.py` 的 `send_message` 路由中，从 `MessageCreate` 提取 `assistant_type` 并传递给 service
- **文件变更**：
  - `backend/conversation-service/app/routes/conversations.py` — 修改 send_message 函数
- **验收标准**：
  - 参数正确提取并传递
  - 单元测试通过
- **依赖**：T1（Schema 扩展）
- **预计耗时**：20 分钟
- **状态**：待开始

### T3: 前端类型定义 — MessageCreate 类型新增 assistant_type
- **描述**：在 `frontend/shared/src/types.ts` 的 `MessageCreate` 类型中添加可选字段 `assistant_type?: string`
- **文件变更**：
  - `frontend/shared/src/types.ts` — 新增字段
- **验收标准**：
  - TypeScript 类型正确
  - 前端构建通过
- **依赖**：无（可与 T1 并行）
- **预计耗时**：10 分钟
- **状态**：待开始

### T4: 前端 Store 改造 — sendMessage 携带 assistant_type
- **描述**：在 `frontend/customer/src/stores/chat.ts` 的 `streamAIResponse` 或相关函数中，发送消息时携带 `selectedAssistant.value`
- **文件变更**：
  - `frontend/customer/src/stores/chat.ts` — 修改 sendMessage/streamAIResponse
- **验收标准**：
  - 参数正确携带
  - 请求体格式正确
- **依赖**：T3（类型定义）
- **预计耗时**：20 分钟
- **状态**：待开始

### T5: 前端 UI 改造 — ChatWindow.vue 助手选择器移位
- **描述**：将助手选择器从工单创建对话框移至对话界面顶部（消息区域上方）
- **文件变更**：
  - `frontend/customer/src/components/ChatWindow.vue` — UI 结构调整
- **验收标准**：
  - 助手选择器在对话界面顶部显示
  - 工单创建对话框移除助手选择器
  - UI 样式正确
- **依赖**：T4（Store 改造）
- **预计耗时**：30 分钟
- **状态**：待开始

### T6: 文档更新 — 同步相关设计文档
- **描述**：更新 `docs/solution/custom-ui/客户端设计.md`、`docs/solution/agent/AI助手设计.md`、`docs/solution/接口设计.md`
- **文件变更**：
  - `docs/solution/custom-ui/客户端设计.md` — 更新 §7 助手选择器章节
  - `docs/solution/agent/AI助手设计.md` — 更新交互流程说明
  - `docs/solution/接口设计.md` — 更新 MessageCreate schema
- **验收标准**：
  - 文档同步更新
  - 变更历史追加
- **依赖**：T1~T5（功能完成）
- **预计耗时**：30 分钟
- **状态**：待开始

## 任务依赖图

```
T1 ──→ T2 ──→ T4 ──→ T5 ──→ T6
T3 ──────────↗
```

## 执行顺序建议

1. **第一批（可并行）**：T1、T3（后端 Schema + 前端类型定义）
2. **第二批**：T2（后端路由）
3. **第三批**：T4（前端 Store）
4. **第四批**：T5（前端 UI）
5. **第五批**：T6（文档更新）

## 文档更新计划

按照文档管理规范，以下文档需要在任务完成后同步更新：
- [x] `docs/requirement/events/2026-04-17-助手切换优化.md` — 已创建
- [x] `docs/solution/events/2026-04-17-助手切换优化方案.md` — 已创建
- [ ] `docs/solution/custom-ui/客户端设计.md` — 更新变更历史
- [ ] `docs/solution/agent/AI助手设计.md` — 更新交互流程
- [ ] `docs/solution/接口设计.md` — 更新 schema
- [ ] `README.md` 第一屏 — 更新功能说明

## 测试计划

### 单元测试
- 后端：`MessageCreate` schema 验证测试
- 后端：`send_message` 路由参数传递测试
- 前端：类型定义编译测试

### 集成测试
- 端到端测试：切换助手 → 发送消息 → 新助手响应
- API 测试：携带 `assistant_type` 参数的消息发送

### 人工测试
- 创建工单 → 对话中切换助手 → 验证新助手响应
- 验证对话历史保留
- 验证 UI 显示正确

---

*文档版本: v1.0 | 创建: 2026-04-17 | 状态: 待确认*