---
status: active
category: task
audience: developer
last_updated: 2026-08-20
owner: team
---

# KBD 生产者信号产出变量别名实施任务

关联方案：[2026-08-20-KBD生产者信号产出变量别名设计方案](../../solution/events/2026-08-20-KBD生产者信号产出变量别名设计方案.md)

## 任务清单

### 前端

- [x] `produceVariables.ts`：`ProduceVariableOption` 新增 `alias?: string`，`parseProduceVariableDraftsFromSchema` / `extractProduceVariablesFromSchema` 透传 alias；新增 `effectiveKey(produce)` 工具函数
- [x] `KbdReviewView.vue`：产出变量编辑行新增 alias 输入框（占位符动态跟随 name，自动大写规范化）；清空 name 时同步清空 alias；阅览/预览态芯片显示 alias
- [x] `produceVariables.spec.ts`：补充 alias 相关单元测试

### 后端

- [x] `extract_signals.py`：alias 格式校验（`^[A-Z][A-Z0-9_]*$`）；同一信号 produces 内 effective_key 唯一性校验；变量池合法名集合扩展
- [x] `safe_pipeline_converter.py`：新增 `_effective_produce_name()` 辅助函数，替换相关 `name` 引用
- [x] `hci_sim_resolver.py`：`_derive_sample_output` 使用 effective_key 生成 fixture

### 文档

- [x] `docs/solution/events/` 方案事件文档归档（本文档）
- [x] `docs/task/events/` 任务事件文档归档（本文档）
- [x] `docs/admin-ui/kbd-produce-variable-catalog.md` 更新 alias 使用说明
