---
status: active
category: task
audience: developer
last_updated: 2026-04-16
owner: platform
---

# 可观测性全面修复任务

## 关联方案
[docs/solution/events/2026-04-16-可观测性全面修复方案.md](../../../solution/events/2026-04-16-可观测性全面修复方案.md)

## 任务清单

### T1: Tempo + Loki 持久化卷（#3 #8）
- **描述**：docker-compose-obs.yml 添加 `tempo_data` 和 `loki_data` named volume；tempo-config.yaml 存储路径从 `/tmp/tempo/blocks` 改为 `/var/tempo/blocks`
- **文件变更**：`docker-compose-obs.yml`、`tempo-config.yaml`
- **验收标准**：`docker compose restart tempo loki` 后历史数据仍在
- **依赖**：无
- **状态**：待开始

### T2: Promtail JSON Pipeline（#4）
- **描述**：promtail-config.yaml 添加 pipeline_stages 解析 JSON 日志，提取 `trace_id`/`level`/`service` 为 Loki 标签
- **文件变更**：`promtail-config.yaml`
- **验收标准**：Loki 查询支持 `{trace_id="xxx"}` 过滤
- **依赖**：无
- **状态**：待开始

### T3: datasources.yml 配置纠错（#9 #10）
- **描述**：serviceMap.datasourceUid 从 `loki` 改为 `prometheus`；tempo-config.yaml 添加 remote_write 到 Prometheus；prometheus.yml 开启 remote_write receiver
- **文件变更**：`datasources.yml`、`tempo-config.yaml`、`docker-compose-obs.yml`（Prometheus 启动参数）
- **验收标准**：Grafana Tempo Service Map 页面正常渲染
- **依赖**：无
- **状态**：待开始

### T4: 告警规则指标名修复 + scheduler-service 指标上报（#1）
- **描述**：alert_rules.yml 中 `hci_warm_pool_size` → `hci_pod_pool_idle`/`hci_pod_pool_active`；pod_pool.py 导入并上报 Gauge；prometheus.yml 添加 kb-service scrape job
- **文件变更**：`alert_rules.yml`、`pod_pool.py`、`prometheus.yml`
- **验收标准**：Prometheus `/api/v1/query?query=hci_pod_pool_idle` 有数据
- **依赖**：无
- **状态**：待开始

### T5: HTTP 请求指标中间件（#2）
- **描述**：shared/utils/metrics.py 添加 `HTTP_REQUEST_DURATION` Histogram 和 `HTTP_REQUESTS_TOTAL` Counter；各服务 main.py 添加 HTTPMetricsMiddleware
- **文件变更**：`shared/utils/metrics.py`、`backend/*/app/main.py`（多个服务）
- **验收标准**：`/metrics` 端点出现 `http_request_duration_seconds_bucket`
- **依赖**：无
- **状态**：待开始

### T6: X-Trace-Id 与 OTel 对齐（#5）
- **描述**：api-gateway/app/main.py 中 `TraceIDMiddleware` 改为从 OTel Span Context 读取真实 trace_id
- **文件变更**：`api-gateway/app/main.py`
- **验收标准**：响应头 `X-Trace-Id` 值可在 Tempo 直接搜索到
- **依赖**：T5（共用 main.py 文件）
- **状态**：待开始

### T7: KB 检索指标上报（#6）
- **描述**：kb-service 向量检索核心路径添加 `KB_SEARCH_DURATION_SECONDS` 计时上报
- **文件变更**：`kb-service/app/routes/` 或 services 目录
- **验收标准**：`/metrics` 出现 `hci_kb_search_seconds_bucket`
- **依赖**：无
- **状态**：待开始

### T8: StructuredLogger.exception() 方法（#13）
- **描述**：shared/utils/logger.py 添加 `exception()` 方法，记录完整 traceback
- **文件变更**：`shared/utils/logger.py`
- **验收标准**：`logger.exception()` 输出包含 `traceback` 字段
- **依赖**：无
- **状态**：待开始

### T9: Grafana 安全加固（#11）
- **描述**：docker-compose-obs.yml 中 Grafana admin 密码改用环境变量；关闭或限制匿名访问
- **文件变更**：`docker-compose-obs.yml`，新增 `env/obs.env.example`
- **验收标准**：未登录无法访问 Grafana 探索页面
- **依赖**：无
- **状态**：待开始

### T10: Grafana Dashboard 代码化（#14）
- **描述**：添加 grafana/provisioning/dashboards/ 目录，提供 HCI 概览大盘 JSON
- **文件变更**：新增 `dashboards.yml`、`hci-overview.json`
- **验收标准**：Grafana 启动后自动加载 HCI Overview 大盘
- **依赖**：无
- **状态**：待开始

### T11: otel.py 注释澄清 + prometheus.yml node-exporter 注释（#7 #12）
- **描述**：补充 LoggingInstrumentor 说明注释；prometheus.yml 添加注释形式的 node-exporter/kube-state-metrics 配置
- **文件变更**：`otel.py`、`prometheus.yml`
- **依赖**：无
- **状态**：待开始

### T12: 文档同步 + Lint 检查
- **描述**：更新 `docs/solution/可观测性设计.md` 变更历史；运行 ruff check 确保零错误
- **文件变更**：`docs/solution/可观测性设计.md`
- **依赖**：T1~T11 全部完成
- **状态**：待开始

## 执行顺序

```
T1 T2 T3 T4 T5 T7 T8 T9 T10 T11（可并行）
       ↓
     T6（依赖 T5 main.py 改动）
       ↓
     T12（文档 + lint）
```

## 测试计划
- 本地：`uv run ruff check backend/` 零错误
- 本地：各服务 `GET /metrics` 验证指标存在
- 人工：重启 Tempo/Loki 容器验证持久化
