# 任务列表：修复 SOP 变量提交失效问题

- [x] 后端修改：更新 `conversations.py` 路由与 `InteractiveResponseBody` 接收 `metadata`
- [x] 后端修改：更新 `conversation_service.py` 逻辑，支持 `variable_input`/`variable_confirm` 直接落库变量池并恢复状态
- [x] 前端修改：更新 `MessageBubble.vue` handleInteractiveFreeText/handleInteractiveOption，将 `kind` 和 `metadata` 随 POST 提交，并在成功后执行 `sendMessage` 唤醒 Agent
- [x] 前端修改：更新 `InteractiveRequestCard.vue` 支持发送完整 Payload
- [x] 验证：运行 `make quality-gate` 确保编译 and 全量单元测试通过
- [x] 文档与提交：更新 `AGENTS.md`，提交代码并推送到分支，确保符合项目提交规范
