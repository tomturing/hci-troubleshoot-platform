# 任务列表：工具调用可视化、变量交互重构与原始数据环境注入

- [x] 后端修改：`agent-service` 工具调用生命周期事件广播
  - [x] 修改 `agent_port.py`
  - [x] 修改 `react_engine.py` 内部 `_execute_tool_call` 方法，在工具执行前后分别 yield `tool_call` 与 `tool_result` 事件，并支持传递统一 the `exec_id`
  - [x] 修改 `BridgeRelayExecutor.execute` 签名，接收并支持 `exec_id` 参数
  - [x] 修改 `CompositeToolExecutor.execute` 与 `SopToolExecutor.execute` 签名，支持 `**kwargs` 透传 `exec_id`
  - [x] 修改 `triage_agent.py` 的 `_build_s0_prompt` 支持 `json.dumps` 格式化原始环境数据
- [x] 后端修改：`conversation-service` 链路转发与原始环境上下文获取
  - [x] 修改 `agent_client.py` 增加 `get_raw_environments` 方法
  - [x] 修改 `conversation_service.py` 路由中 `stage_update` 的转发逻辑，将 `tool_call` 与 `tool_result` 转换为独立 SSE 事件发送给前端
  - [x] 修改 `conversation_service.py` 的环境数据获取，在 `settings.USE_RAW_ENVIRONMENT_CONTEXT` 开启时，获取原始环境数据，并添加 `is_raw=True` 属性
  - [x] 修改 `sop_execution.py` 的 `_resolve_env_variable`，对 is_raw 模式下的 `env_info`、`alert_logs` 和 `task_logs` 提供向下兼容字段映射
- [x] 前端修改：`customer` UI 与 Store 功能开发
  - [x] 修改 `chat.ts` 监听处理 `tool_call` 和 `tool_result` SSE 事件，动态更新 `messages`，并在 "Auto-Run" 开启时根据等级自动执行 `pending` 工具
  - [x] 修改 `MessageBubble.vue` 渲染 `"tool_call"` 类型的专属 Collapsible Card（展示入参、状态及执行终端回显）
  - [x] 重构 `MessageBubble.vue` 中的 `variable_input` 变量填写表单卡片，支持实时正则匹配与必填格式校验
  - [x] 重构 `MessageBubble.vue` 中的 `variable_confirm` 变量确认卡片，展示为双栏形式，支持一键确认推荐值与修改后提交
- [x] 验证与测试
  - [x] 运行 `make lint` 和单元测试 `tests/unit/test_sop_execution_env_injection.py`
  - [x] 运行 `make test` 确保整体用例无退化
- [x] 交付：更新 `AGENTS.md` 规范说明与归档，提交 PR 并遵循提交日志格式规范
