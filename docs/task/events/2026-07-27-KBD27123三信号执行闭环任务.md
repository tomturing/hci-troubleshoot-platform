---
status: active
category: task
audience: developer, tester, operator
last_updated: 2026-07-27
owner: team
---

# KBD 27123 三信号执行闭环任务

> 对应方案：[KBD27123三信号执行闭环方案](../../solution/events/2026-07-27-KBD27123三信号执行闭环方案.md)

## 任务清单

- [x] 查询工单、会话、环境 task_logs、diagnostic_item、tool_result。
- [x] 用 Loki 串联 QKV、node list、lsof 和 exec-result 证据。
- [x] 区分 KBD 配置问题、Agent 问题、UI 契约问题和 terminal_bridge 传输问题。
- [x] 移除 KBD QKV 对预取上下文的静默替代。
- [x] 修复 `启动虚拟机失败` 的 QKV 关键词/失败状态规范化。
- [x] 新增 `output_filters` 内部协议和 Pydantic 白名单校验。
- [x] terminal_bridge 实现本地逐行筛选、256 KiB 上限和 Go 单测。
- [x] customer-ui 实现旧 bridge 兼容筛选、O(n) 分块缓存和输出上限。
- [x] 增加跨 chunk、无换行末行、共享 256 KiB 预算和非法筛选规格的回归测试。
- [x] terminal_bridge 串行化 stdout/stderr/bridge_log WebSocket 写入。
- [x] 统一工具卡片 `args/result/status`，保留滚动发布兼容别名。
- [x] SSE 中断时将未完成工具卡片收敛为失败。
- [x] conversation-service 按 exec_id 持久化所有 Agent 工具生命周期。
- [x] resume 续写流同步持久化并渲染工具生命周期。
- [x] 保存和快照层增加 QKV 产出契约门禁。
- [x] 通过管理 API 修正 KBD 27123 并发布 revision 17。
- [x] 增加三信号生产配置变量链自动测试。
- [x] 更新现行设计、接口、任务、测试指南和避坑文档。
- [ ] PR 合并后部署 agent/conversation/kb/customer-ui 和新 terminal_bridge 下载件。
- [ ] 使用全新工单完成现场回归并补充最终 exec_id/截图。

## 文件级实施范围

| 模块 | 主要文件 | 修改目标 |
|---|---|---|
| Agent | `kbd_differential.py` | 显式 QKV、edge filter、事件契约 |
| QFK | `qfk/engine.py` | 向执行通道传递 output_filters |
| 执行器 | `acli/executor.py` | 协议透传 |
| Conversation | `agent_exec.py` | 筛选规格白名单和 SSE 字段 |
| Conversation | `conversation_service.py` | exec_id 工具生命周期 upsert |
| UI | `chat.ts` / `MessageBubble.vue` | 状态归一化、兼容筛选、中断收敛 |
| Bridge | `terminal_bridge/main.go` | 输出源边缘筛选 |
| KB | `signal_schema.py` / `playbooks.py` | 保存/运行双门禁 |

## 回滚边界

- 服务代码可按镜像 tag 回滚；KBD revision 17 保持合法，可继续兼容旧代码。
- 新 UI 对旧 bridge 向后兼容；新 bridge 对旧 UI 会忽略 UI 未发送的 `output_filters`。
- 若边缘筛选异常，必须返回明确失败，不允许回退为静默使用截断输出。
