---
status: active
category: task
audience: developer
last_updated: 2026-04-09
owner: team
---

# 任务：desired_schema.sql 质量修复（Batch 1 + 2）

> 对应方案文档：[2026-04-09-schema质量修复方案](../../solution/events/2026-04-09-schema质量修复方案.md)
> 对应需求文档：[2026-04-09-schema质量修复需求](../../requirement/events/2026-04-09-schema质量修复需求.md)
> PR：#126

---

## 完成标准

**所有任务完成、PR 合并后**：

1. `desired_schema.sql` 中 `generate_case_id()` 包含 `pg_advisory_xact_lock` 调用
2. `desired_schema.sql` 中存在 `idx_kb_category_embedding`、`idx_kbd_entry_embedding`、`idx_sop_chunk_embedding` 三个 IVFFlat 索引
3. `idx_message_content_search` 使用 `'simple'` 而非 `'english'`
4. `diagnostic_item` 存在 `uq_diagnostic_item_conv_type_seq` 唯一约束
5. `session` 存在 `fk_session_user_id REFERENCES "user" ON DELETE CASCADE`
6. `assistant_evaluation`、`conversation`、`diagnostic_item`、`tool_result`、`tool_definition` 共 7 个 CHECK 约束存在
7. `kbd_entry` 和 `sop_document` 的 `updated_at` 触发器存在
8. `idx_user_client_id` 冗余索引已删除
9. `idx_user_trace_id`、`idx_environment_trace_id`、`idx_eval_trace_id` 低价值索引已删除
10. `docs/solution/数据库设计.md` 版本已更新至 v6.5

---

## 任务清单

### T1：修复 generate_case_id() 并发安全（D-001）

- [x] 在函数中添加 `PERFORM pg_advisory_xact_lock(hashtext('generate_case_id:' || v_today));`
- [x] 将 `COUNT(*) + 1` 改为 `COALESCE(MAX(CAST(...)), 0) + 1`
- [x] 验证：`grep -n 'pg_advisory_xact_lock' database/desired_schema.sql`

### T2：添加向量索引（D-002）

- [x] 为 `kb_category.embedding` 添加 IVFFlat 索引（lists=100，cosine_ops）
- [x] 为 `kbd_entry.embedding` 添加 IVFFlat 索引
- [x] 为 `sop_chunk.embedding` 添加 IVFFlat 索引
- [x] 验证：`grep -n 'ivfflat' database/desired_schema.sql`

### T3：修复全文检索分词（D-003）

- [x] 将 `idx_message_content_search` 中 `'english'` 改为 `'simple'`
- [x] 验证：`grep -n 'simple' database/desired_schema.sql`

### T4：添加 diagnostic_item 唯一约束（D-004）

- [x] 添加 `CONSTRAINT uq_diagnostic_item_conv_type_seq UNIQUE (conversation_id, "type", seq)`
- [x] 验证：`grep -n 'uq_diagnostic_item' database/desired_schema.sql`

### T5：添加 session.user_id 外键（D-005）

- [x] 添加 `CONSTRAINT fk_session_user_id FOREIGN KEY (user_id) REFERENCES "user" (user_id) ON DELETE CASCADE`
- [x] 验证：`grep -n 'fk_session_user_id' database/desired_schema.sql`
- [x] 文档记录迁移前置清理 SQL（已在方案文档中记录）

### T6：添加 CHECK 约束（D-006）

- [x] `assistant_evaluation.score`：`[1,5]`
- [x] `assistant_evaluation.composite_score`：`[0,100]`
- [x] `conversation.diagnostic_stage`：`IN ('S0','S1','S2','S3','S4','S5','S6')`
- [x] `diagnostic_item.probability`：`[0.0, 1.0]`
- [x] `diagnostic_item.stage`：`IN ('S2','S3','S4','S5')`
- [x] `tool_result.risk_level`：`[1,3]`（IS NULL OR）
- [x] `tool_definition.risk_level`：`[1,3]`（NOT NULL 字段，无需 NULL-safe）
- [x] 验证：`grep -c 'chk_' database/desired_schema.sql`

### T7：补全 updated_at 触发器（P2-2）

- [x] 为 `kbd_entry` 添加 `update_kbd_entry_updated_at` 触发器
- [x] 为 `sop_document` 添加 `update_sop_document_updated_at` 触发器
- [x] 验证：`grep -n 'update_kbd_entry\|update_sop_document' database/desired_schema.sql`

### T8：移除冗余索引（P1-1 + O-001）

- [x] 删除 `idx_user_client_id`（UNIQUE 约束已自动创建索引）
- [x] 删除 `idx_user_trace_id`（低价值裸索引）
- [x] 删除 `idx_environment_trace_id`（低价值裸索引）
- [x] 删除 `idx_eval_trace_id`（低价值裸索引）
- [x] 验证：`grep -n 'idx_user_client_id\|idx_user_trace_id\|idx_environment_trace_id\|idx_eval_trace_id' database/desired_schema.sql`

### T9：文档同步

- [x] 更新 `docs/solution/数据库设计.md`（v6.4 → v6.5，追加变更历史）
- [x] 创建需求事件文档 `docs/requirement/events/2026-04-09-schema质量修复需求.md`
- [x] 创建方案事件文档 `docs/solution/events/2026-04-09-schema质量修复方案.md`
- [x] 创建本任务文档
- [ ] 更新 `docs/requirement/需求说明.md`（变更历史追加）
- [ ] 更新 `docs/task/数据库任务.md`（变更历史追加 + 任务清单追加）

### T10：Atlas 迁移文件（外部依赖）

- [ ] 在有 PostgreSQL 连接的环境中执行 `atlas migrate diff --env local db-schema-v6.5-fixes`
- [ ] 审查生成的迁移文件（确认无破坏性变更）
- [ ] 提交迁移文件

> ⚠️ T10 依赖 PostgreSQL 容器，本地无法运行，CI 或部署环境执行。

---

## 已驳回任务

| 建议 | 来源 | 驳回原因 |
|------|------|---------|
| case_id 换 UUID 主键 | database-schema-review P0-1 | 业务可读工单号，迁移代价/收益比极差 |
| diagnostic_item 拆成 4 张表 | database-schema-review P0-4 | JSONB 多态合理，拆表增加复杂度无收益 |
| 创建 trace_log 中心化表 | database-schema-review P1-2 | 过度工程，删除冗余索引更简洁 |
| 软删除机制 | database-schema-review P1-3 | 产品决策，非技术缺陷 |
| 表分区 | database-schema-review P2-3 | 过早优化，当前数据量不需要 |
