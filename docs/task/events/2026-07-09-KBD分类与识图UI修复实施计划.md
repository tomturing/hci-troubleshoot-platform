---
status: completed
category: task
audience: developer
last_updated: 2026-07-09
owner: team
---

# KBD 分类与识图 UI 修复实施计划

## 关联方案
[方案设计文档](../../solution/events/2026-07-09-KBD分类与识图功能及UI布局修复设计方案.md)

## 任务清单

### T1: 修复后端 LLM API 的环境变量配置统一化与数据管道清理
- **文件变更**：
  - [classify.py](file:///mnt/d/aihci/hci-troubleshoot-platform/backend/kb-service/app/routes/classify.py)
  - [vision_processor.py](file:///mnt/d/aihci/hci-troubleshoot-platform/backend/kb-service/app/services/vision_processor.py)
  - [docker-compose.yml](file:///mnt/d/aihci/hci-troubleshoot-platform/deploy/docker/docker-compose.yml)
  - [.env](file:///mnt/d/aihci/hci-troubleshoot-platform/.env)
  - `data-pipeline/kbd/config.py`
  - `data-pipeline/kbd/run.py`
  - `data-pipeline/kbd/image_proc.py`
  - `data-pipeline/kbd/.env` / `.env.example`
- **内容**：
  1. 将后端路由及 Vision 处理器中读取环境变量的名称修正为标准的 `LLM_BASE_URL` 和 `LLM_API_KEY`。
  2. 在 `docker-compose.yml` 文件的 `kb-service` 环境变量字典中增加对 `LLM_BASE_URL` 和 `LLM_API_KEY` 的定义与透传。
  3. 在本地 `.env` 文件中补齐 `LLM_BASE_URL` 和 `LLM_API_KEY` 变量，保持与开发/生产统一的契约一致。
  4. 彻底删除 `data-pipeline/kbd` 内部无用的 LLM 直接调用字段（含配置类与 `.env` 中相关配置），在 `image_proc.py` 中重构补全 `_find_images` 函数以确保数据处理管道完全兼容并顺畅执行。
- **状态**：已完成

### T2: 重构前端交互及 confirm 确认逻辑
- **文件变更**：
  - [KbdReviewView.vue](file:///mnt/d/aihci/hci-troubleshoot-platform/frontend/admin/src/views/KbdReviewView.vue)
- **内容**：
  1. 引入 `Refresh` 图标依赖。
  2. 使用 Element Plus 的 `ElMessageBox.confirm` 替换浏览器的原生 `confirm` 提示，且支持展示完整标题。
- **状态**：已完成

### T3: 前端布局重组与重分类/重识图刷新对接
- **文件变更**：
  - [KbdReviewView.vue](file:///mnt/d/aihci/hci-troubleshoot-platform/frontend/admin/src/views/KbdReviewView.vue)
- **内容**：
  1. 从列表页操作列中彻底移除“重新分类”和“重新识图”按钮，调整操作列 `width` 为 `220`。
  2. 在详情弹窗内的“AI 建议分类”右侧添加 `Refresh` 图标按钮。
  3. 在详情弹窗的截图说明 card header 及图片列表 header 右侧添加 `Refresh` 识图按钮（需加 `.stop` 阻止折叠面板折叠事件冒泡）。
  4. 完善操作回调，成功执行后同时更新 main 列表行和 detail 弹窗缓存，使数据和内容预览区能即时更新。
- **状态**：已完成

### T4: 编译打包与验证
- **内容**：
  1. 在 `frontend/admin` 路径执行 `npm run build`，验证 TypeScript 及打包无报错。
- **状态**：已完成
