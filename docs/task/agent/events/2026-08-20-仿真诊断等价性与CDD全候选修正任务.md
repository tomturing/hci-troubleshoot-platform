---
status: active
category: task
audience: developer
last_updated: 2026-08-20
owner: team
---

# 仿真诊断等价性与 CDD 全候选修正任务

## 目标

纠正 PR #857 将 TestRun 目标 KBD 泄漏为诊断答案的问题，恢复真实与仿真共用分类完整候选、CDD 和 Conclusion Gate 的原则。

## 本次 hotfix

- [x] 删除 `support_id + resource_revision` 单 KBD 候选过滤。
- [x] 删除 `SIM_KBD_AUTHORITY_CONTEXT_INVALID` 和 `SIM_KBD_AUTHORITY_CANDIDATE_MISMATCH` S1 阻断。
- [x] 删除仿真模式清空 SOP 恢复路由的特殊分支。
- [x] 删除 `agent_sim_kbd_authority_binding_total` 错误指标。
- [x] 增加 `kbd_candidate_scope_resolved` 结构化日志和 `candidate_scope` 阶段元数据。
- [x] 增加 real/sim candidate IDs 完全一致的单元回归，覆盖 revision 不一致场景。
- [x] 保留并校正“无向量 top_k、无两篇早停、无最高频采集器筛选”的现行算法文档。
- [x] 删除 PR #857 三份错误事件归档，替换为修正方案、任务和问题复盘。
- [x] 将 V-014 反转为“仿真场景标签不得充当候选答案”的全局避坑规则。

## Bundle 后续工作

- [ ] Bundle Compiler 冻结目标场景及同分类全部可执行 KBD revision/signals digest。
- [ ] 生成或人工审核分类完整 acquisition RouteKey 集合。
- [ ] 为非目标候选提供可审查 observation，使其由 Matcher 自然得到反证。
- [ ] TestRun 创建前校验 category snapshot freshness，过期 Bundle 不用于当前 Agent/KBD 验证。
- [ ] 重建并发布 KBD27123 场景 Bundle，覆盖 KBD30880 的 `cat /sf/cfg/gpu_info.ini` acquisition。
- [ ] 运行 Q2026082002685 等价回归，验收 `27123=SUPPORTED`、`30880=REJECTED`、总体 `DEFINITIVE`。

这些任务不能通过 Agent 单 KBD过滤、`fixture_not_found -> CONTRADICTED` 或放宽 Conclusion Gate 替代。

## hotfix 验收

1. real/sim 都将分类内 27123、30880 交给 CDD。
2. TestRun target/revision 仅保留在仿真采集上下文中，不参与候选生成。
3. 代码库、指标和文档不存在 `SIM_KBD_AUTHORITY_CANDIDATE_MISMATCH`。
4. Python 单元测试、CDD 调度/归约测试、ruff 和文档门禁全部通过。
