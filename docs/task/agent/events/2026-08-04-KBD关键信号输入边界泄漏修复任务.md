---
status: completed
category: task
audience: developer, tester, expert
last_updated: 2026-08-04
owner: team
---

# KBD 关键信号输入边界泄漏修复任务

关联方案：[KBD 关键信号输入边界泄漏修复方案](../../../solution/agent/events/2026-08-04-KBD关键信号输入边界泄漏修复方案.md)。

## 已完成

- [x] 审计 KBD27736 的直接字段、`images_json`、当前 Prompt revision、generation metadata 和全部 rejected candidates。
- [x] 确认 `root_cause`/`solution` 直接模板变量置空，但 solution 截图 `img:3` 的 OCR 与 `context_before` 进入了 Signal Prompt。
- [x] 在 `_format_image_evidence()` 加诊断章节白名单，未知/根因/方案章节 fail closed。
- [x] 从 Signal Prompt 删除所有截图 `context_before/context_after` 与 legacy desc，仅保留图片原子观察事实和质量状态。
- [x] 建立“本轮实际图片输入”索引，拒绝越界 `source_refs`、非诊断 `source_section` 和不能逐字回溯的图片 evidence。
- [x] 新增 root_cause/solution 图片隔离、上下文隔离、越界 source ref 拒绝回归测试。
- [x] 保持 `write_signal` 后置安全门禁，不迁移、不重抽、不发布既有 KBD。

## 验证

```bash
PYTHONPATH="backend/kb-service:backend" \
  /aihci/hci-troubleshoot-platform/.venv/bin/python -m pytest \
  backend/kb-service/tests/test_vision_evidence.py \
  backend/kb-service/tests/test_signal_generation.py -q
```

预期结果：62 项通过。测试覆盖以下反例：

1. solution 截图含 `rm`/`restart` 文本时，格式化 Prompt 不含该文本；
2. 正常 `steps_text` 截图仍保留 OCR/observed facts 与稳定 region source ref；
3. Candidate 引用被排除的 solution 图片或将方案文字伪标为诊断图 evidence 时，进入
   `rejected_candidates`，不成为有效 Signal。

## 后续任务

- [ ] 以 maintenance working 方式由专家触发 KBD27736 重抽，确认新 Proposal 仅包含诊断证据；不得
  以本代码改动自动覆盖当前工作稿或发布版本。
- [ ] 对有 `images_json` 的历史 Proposal 做只读审计，统计 solution/root_cause 图片曾被引用的范围，
  将重抽优先级交由专家批次计划决定。
- [ ] 若未来确需将截图周边诊断文本送入模型，先设计可证明“同一诊断章节、逐段可定位”的上下文
  Contract，再另行评审；不得恢复无边界的 `context_before/context_after`。
