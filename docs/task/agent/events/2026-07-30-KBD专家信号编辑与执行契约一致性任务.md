---
status: completed
category: task
audience: developer, tester
last_updated: 2026-07-30
owner: team
---

# KBD 专家信号编辑与执行契约一致性任务

## 对应方案

[KBD 专家信号编辑与执行契约一致性方案](../../../solution/agent/events/2026-07-30-KBD专家信号编辑与执行契约一致性方案.md)

## 实施清单

| ID | 任务 | 交付物 | 状态 |
|---|---|---|---|
| KSE-01 | 收敛角色事实来源 | `verification_contract.py`：按 `signals[].id/role` 重建 policy、兼容历史 role、保留 Contract 元数据 | ✅ |
| KSE-02 | 分层校验 | `validate_draft_signals_json()` 允许无必要证据草稿；`validate_publishable_signals_json()` 保持严格发布门禁 | ✅ |
| KSE-03 | 覆盖所有写路径 | maintenance PATCH、普通 draft PATCH、LLM extract 均调用同一归一化 | ✅ |
| KSE-04 | 专家可处理反馈 | 校验接口翻译必要证据/ID 问题；不向主路径暴露 `verification_contract.*` 字段路径 | ✅ |
| KSE-05 | 前端保存闭环 | 删除/复制/排序/角色编辑保存前即时投影，后端 canonical 回写后立即检查 | ✅ |
| KSE-06 | 删除影响说明 | 显示角色、门槛收敛、草稿/发布边界和“不删除正文/截图” | ✅ |
| KSE-07 | KBD30880 回归 | 覆盖 `sig_004` context 删除、无 must 草稿、角色移动 | ✅ |
| KSE-08 | 文档归档 | 方案、任务、验证事件及现行文档/冷启动入口同步 | ✅ |

## 不在本轮范围

- 不把解释性“正常配置示例”自动迁移到图片或正文；专家按个案删除错误的可执行 Signal；
- 不更改 Agent Handler、工具 Registry 或数据库表；
- 不声称完成现场 Agent replay 或 Expert Gold；
- 不新增第二审核工作台、双审和审批型治理流程。

## 人工验收步骤（部署后）

1. 打开 `support_id=30880`，创建维护工作稿；
2. 删除“参照正常 GPU 主机的配置文件格式”；确认提示说明它是上下文证据且原文/截图不删除；
3. 保存，确认没有 `verification_contract.context` 原始错误，点击“检查当前内容”结果可发布；
4. 将最后一条必要证据改为增强证据并保存，确认工作稿保存成功、检查提示如何修复；
5. 改回必要证据，确认检查通过；选择发布维护版或放弃维护稿，确认前者才改变 Agent active。
