---
status: in_progress
category: task
audience: developer, tester, release
last_updated: 2026-08-06
owner: team
---

# C3 两步人工验收闭环任务

## 已完成

- [x] 新增 `hci-sim bootstrap --kbd-id`，调用 C1 capability 并 fail-closed。
- [x] 为 `27736/34164` 建立最小 synthetic qkv_task 白名单，不支持变量化 qfk_system。
- [x] 生成 Manifest v2、语义 Bundle digest、短时 htp2 Lease 和 0600 connection 文件。
- [x] 新增 dev 一键启动脚本，自动构建镜像、启动单副本 Runtime、检查 `/readyz`。
- [x] Custom UI 增加“仿真租约”表单并透传 `execution_mode/test_run_id`。

## 本提交验证任务

- [ ] Go `gofmt/test/race/vet/build`。
- [ ] Custom UI typecheck/build 和 terminal_bridge Go 测试。
- [ ] 案例 A/B 正常连接、推荐命令命中精确 route；案例 C 在 bootstrap 前被阻断。
- [ ] 校验 Lease 不写日志、不持久化到 localStorage，停止容器后无残留运行时。

## 后续任务（C3 之后）

1. 把 bootstrap 白名单替换为持久化 Signal Contract Compiler，并建立专家/安全审批和真实 Artifact Registry 适配器。
2. 实现 PostgreSQL TestRun/Scenario API、幂等键、CAS 状态机、in-flight 恢复和 Redis/共享 Lease Store。
3. 将单副本 dev 脚本升级为 Helm Controller 按 Bundle digest 部署隔离环境，接入 Custom UI 的受认证 TestRun 创建入口。
4. 对真实 HCI 做正向/负向差分、mutation、20 次稳定性和容量阶梯验证；完成后才可宣称 realistic/E2E。
