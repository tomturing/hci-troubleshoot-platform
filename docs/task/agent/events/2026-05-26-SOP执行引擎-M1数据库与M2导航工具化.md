---
status: active
category: task
audience: developer
last_updated: 2026-05-26
owner: team
priority: P3
milestone: M1-M2
---

# SOP 执行引擎：M1 数据库 + M2 导航工具化

> 关联方案：[agent设计.md §12.6–§12.7.2](../../../solution/agent/agent设计.md)  
> **里程碑说明**：
> - M1（数据库层）：无代码依赖，可与 P0/P1/P2 并行启动
> - M2（导航工具化）：依赖 M1 完成 + P2 重构（T-AGT-10/11/12）完成

---

## 里程碑 M1：数据库层

### T-AGT-18：创建 `sop_execution` 表

#### 背景与问题

```
【背景】
SOP 导航工具化方案（§12.6 推荐方案③）需要持久化执行状态，
支持中断恢复和并发多工单。

【当前状态】
sop_execution 表不存在，需通过 dbmate 迁移创建。
命名依据：execution 与业界编排系统对齐（Airflow dag_run、
GitHub Actions workflow_run、Temporal workflow_execution）
```

#### 任务目标

1. 创建 dbmate 迁移文件，建表 `sop_execution`
2. 包含 UNIQUE 约束、CHECK 约束、2 个索引
3. 迁移可逆（包含 `migrate:down` 部分）

#### 涉及文件

| 文件路径 | 操作 | 说明 |
|---------|------|------|
| `database/migrations/{timestamp}_create_sop_execution.sql` | 新建 | dbmate 迁移文件 |

#### 依赖关系

- **前置任务**：无（可并行启动）
- **后续任务**：T-AGT-20、T-AGT-21、T-AGT-24

#### 实现步骤

**Step 1：创建迁移文件**

```bash
cd database
dbmate new create_sop_execution
```

**Step 2：编写迁移内容**

迁移文件 `migrate:up` 内容（来自 §12.7.2 权威规格）：

```sql
-- migrate:up

-- 表: sop_execution  [模块: conversation-service]
-- 说明: SOP 执行记录表 — 记录一次 SOP 决策树的执行过程
-- 设计:
--   * 1:1 对应 conversation（UNIQUE constraint），一次对话执行一个 SOP
--   * execution_log 只记录节点导航事件，不重复 tool_result 的工具调用数据
--   * completed_steps 防止中断恢复后写操作节点被重复执行
CREATE TABLE IF NOT EXISTS sop_execution (
    id                    UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    conversation_id       UUID NOT NULL,
    sop_document_id       INTEGER NOT NULL,
    current_node_id       VARCHAR(64) NOT NULL,
    status                VARCHAR(16) NOT NULL DEFAULT 'active',
        -- active | completed | interrupted | aborted
    context_variables     JSONB NOT NULL DEFAULT '{}',
        -- 运行时变量池：{var_name: {value, source, resolved_at, resolved_by_tool}}
    completed_steps       JSONB NOT NULL DEFAULT '[]',
        -- 已完成 node_id 数组，防止恢复后写操作节点被重复执行
    pending_variable_name VARCHAR(64) DEFAULT NULL,
        -- 当前阻塞的待填变量名（NULL = 无阻塞）
        -- VARCHAR 而非 JSONB 数组：sop_request_variable() 串行等待，每次只等一个
    execution_log         JSONB NOT NULL DEFAULT '[]',
        -- 节点导航事件序列（不含工具调用细节，那是 tool_result 表的职责）
    trace_id              VARCHAR(64),
    created_at            TIMESTAMPTZ NOT NULL DEFAULT now(),
    updated_at            TIMESTAMPTZ NOT NULL DEFAULT now(),
    CONSTRAINT fk_sop_execution_conversation
        FOREIGN KEY (conversation_id) REFERENCES conversation(conversation_id) ON DELETE CASCADE,
    CONSTRAINT fk_sop_execution_sop_document
        FOREIGN KEY (sop_document_id) REFERENCES sop_document(id),
    CONSTRAINT uq_sop_execution_conversation
        UNIQUE (conversation_id),
    CONSTRAINT chk_sop_execution_status
        CHECK (status IN ('active', 'completed', 'interrupted', 'aborted'))
);

CREATE INDEX IF NOT EXISTS idx_sop_execution_sop_document_id
    ON sop_execution (sop_document_id);
CREATE INDEX IF NOT EXISTS idx_sop_execution_active
    ON sop_execution (status) WHERE status = 'active';

-- migrate:down
DROP TABLE IF EXISTS sop_execution;
```

**Step 3：在本地数据库执行迁移验证**

```bash
cd database
dbmate up
psql $DATABASE_URL -c "\d sop_execution"
```

**Step 4：对应 SQLAlchemy 模型**

在 `conversation-service` 中创建 `SopExecution` ORM 模型：

```python
# backend/conversation-service/app/models/sop_execution.py
class SopExecution(Base):
    __tablename__ = "sop_execution"

    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid4)
    conversation_id = Column(UUID(as_uuid=True), ForeignKey("conversation.conversation_id", ondelete="CASCADE"), nullable=False, unique=True)
    sop_document_id = Column(Integer, ForeignKey("sop_document.id"), nullable=False)
    current_node_id = Column(String(64), nullable=False)
    status = Column(String(16), nullable=False, default="active")
    context_variables = Column(JSONB, nullable=False, default=dict)
    completed_steps = Column(JSONB, nullable=False, default=list)
    pending_variable_name = Column(String(64), nullable=True)
    execution_log = Column(JSONB, nullable=False, default=list)
    trace_id = Column(String(64), nullable=True)
    created_at = Column(TIMESTAMPTZ, nullable=False, default=func.now())
    updated_at = Column(TIMESTAMPTZ, nullable=False, default=func.now(), onupdate=func.now())
```

#### 验收标准

| # | 验收项 | 通过标准 |
|---|--------|---------|
| 1 | 迁移成功 | `dbmate up` 无错误 |
| 2 | 表结构正确 | `\d sop_execution` 显示所有字段、约束、索引 |
| 3 | UNIQUE 约束 | 插入相同 `conversation_id` 两次报 UniqueViolation |
| 4 | CHECK 约束 | `status='invalid'` 插入报 CheckViolation |
| 5 | 可回滚 | `dbmate down` 成功删除表 |

---

### T-AGT-19：补全 `diagnostic_item` INSERT 路径（BUG-06 真正修复）

#### 背景与问题

```
【背景】
设计文档 §12.7.2 明确记载：
diagnostic_item 表目前处于半成品状态——表和模型已建，
archive（批量 UPDATE）路径已写，但各阶段 INSERT 路径从未实现，
BUG-06 尚未真正修复。

【BUG-06 原始问题】
原来在 conversation 表以 JSONB blob 存 hypotheses/react_state，
导致并发竞态、无法独立查询、无独立时间戳。
解法是 diagnostic_item 独立表，但 INSERT 路径缺失。
```

#### 任务目标

1. 在 S2/S3/S4/S5 各阶段的诊断流程中，补全 `diagnostic_item` 的 INSERT 路径
2. 验证：诊断完成后 `diagnostic_item` 表有完整记录

#### 涉及文件

| 文件路径 | 操作 | 说明 |
|---------|------|------|
| `backend/agent-service/app/adapters/agents/htp/investigation_agent.py` | 修改 | 各阶段 INSERT diagnostic_item |
| `backend/conversation-service/app/repositories/diagnostic_item_repository.py`（如存在） | 修改/新建 | INSERT 方法 |

#### 依赖关系

- **前置任务**：P2 重构（T-AGT-11）
- **后续任务**：无

#### 验收标准

| # | 验收项 | 通过标准 |
|---|--------|---------|
| 1 | INSERT 存在 | S2/S3/S4/S5 阶段执行后，`diagnostic_item` 表有对应记录 |
| 2 | 无重复 INSERT | 相同诊断步骤只插入一次 |
| 3 | archive 路径兼容 | 现有 archive（批量 UPDATE）逻辑正常工作 |

---

## 里程碑 M2：导航工具化核心引擎

### T-AGT-20：新增 `get_sop_node` 工具

#### 背景与问题

```
【背景】
§12.6 推荐方案③（导航工具化）：
在 TOOL_REGISTRY 注册 get_sop_node(node_id) 工具，
返回节点文字 + 子节点列表，供 LLM 按需获取 SOP 节点内容。
```

#### 任务目标

1. `tool_registry.py` 注册 `get_sop_node` 工具
2. 工具实现：调用 `kb_client.get_sop_tree(document_id)` 获取树，
   根据 `node_id` 提取节点内容 + 子节点列表

#### 涉及文件

| 文件路径 | 操作 | 说明 |
|---------|------|------|
| `backend/agent-service/app/adapters/agents/htp/tool_registry.py` | 修改 | 注册 `get_sop_node` |
| `backend/agent-service/app/adapters/agents/htp/sop_tools.py` | 新建 | SOP 工具实现 |

#### 依赖关系

- **前置任务**：T-AGT-18（sop_execution 表）
- **后续任务**：T-AGT-22

#### 工具定义规格

```python
# 工具签名
async def get_sop_node(
    node_id: str,           # 节点 ID，如 "n-1"、"n-3-2"
    *,
    sop_document_id: int,   # 由 SOP 命中时注入（非 LLM 传入）
    kb_client: KBClient,
) -> dict:
    """
    获取 SOP 决策树节点内容。
    返回：{
      "node_id": "n-3",
      "type": "diagnosis",          // diagnosis | solution | branch
      "title": "存储 I/O 故障诊断",
      "content": "检查磁盘错误率...",
      "commands": ["acli host.disk.stat"],
      "children": [
        {"node_id": "n-3-1", "title": "磁盘错误率 < 5%"},
        {"node_id": "n-3-2", "title": "磁盘错误率 > 5%"}
      ]
    }
    """
```

#### 验收标准

| # | 验收项 | 通过标准 |
|---|--------|---------|
| 1 | 工具注册 | `ToolRegistry.get_tool("get_sop_node")` 不抛出异常 |
| 2 | 节点内容正确 | 传入已知 `node_id`，返回正确的节点文字和子节点列表 |
| 3 | 不存在节点 | 传入不存在的 `node_id` 返回清晰错误消息（非 500） |

---

### T-AGT-21：新增 `sop_advance` 工具

#### 背景与问题

```
【背景】
§12.6 推荐方案③：
sop_advance(node_id, reasoning) 工具：
LLM 调用此工具记录推进路径，移动到指定子节点，写 execution_log。
```

#### 任务目标

1. 注册 `sop_advance` 工具
2. 工具实现：
   - 验证 `node_id` 是当前节点的合法子节点
   - 更新 `sop_execution.current_node_id`
   - 追加 `execution_log` 条目（type=node_entered）
   - 追加 `completed_steps`（前一节点标记完成）
   - 若叶节点（solution）则更新 `status=completed`

#### 涉及文件

| 文件路径 | 操作 | 说明 |
|---------|------|------|
| `backend/agent-service/app/adapters/agents/htp/sop_tools.py` | 修改 | 新增 `sop_advance` |
| `backend/agent-service/app/adapters/agents/htp/tool_registry.py` | 修改 | 注册 `sop_advance` |
| `backend/conversation-service/app/repositories/sop_execution_repository.py` | 新建 | `advance()` 方法 |

#### 依赖关系

- **前置任务**：T-AGT-18、T-AGT-20
- **后续任务**：T-AGT-22、T-AGT-27

#### 工具定义规格

```python
async def sop_advance(
    node_id: str,           # 目标子节点 ID
    reasoning: str,         # LLM 推进理由（写入 execution_log）
    *,
    conversation_id: str,   # 由上下文注入
    sop_execution_repo: SopExecutionRepository,
    sop_document_id: int,
    kb_client: KBClient,
) -> dict:
    """
    推进 SOP 到指定子节点，记录推理路径。
    返回：{
      "ok": true,
      "current_node_id": "n-3-2",
      "node_type": "diagnosis",      // 到达的节点类型
      "message": "已推进到：磁盘错误率 > 5% 分支"
    }
    """
```

#### execution_log 条目格式

```json
{"type": "node_entered", "node_id": "n-3-2", "entered_at": "...", "reasoning": "磁盘错误率 > 5%，进入严重 I/O 故障分支"}
```

#### 验收标准

| # | 验收项 | 通过标准 |
|---|--------|---------|
| 1 | 节点推进 | 调用后 `sop_execution.current_node_id` 更新 |
| 2 | execution_log 写入 | `execution_log` 追加 `node_entered` 条目 |
| 3 | 非法子节点 | 传入非当前节点的子节点时返回错误 |
| 4 | 叶节点处理 | 到达 solution 节点时 `status=completed` |
| 5 | completed_steps | 前一节点 ID 写入 `completed_steps` |

---

### T-AGT-22：ReactEngine 动态注入 SOP 工具

#### 背景与问题

```
【背景】
当前 SOP 模式和工具调用模式是两条独立路径（双轨），
导致 SOP 模式下无法使用工具，工具调用模式下无法利用 SOP 结构。
§12.6 推荐：SOP 命中后直接走 ReactEngine，消除双轨分叉。
```

#### 任务目标

1. SOP 命中时，动态向 `ToolRegistry` 注入 `get_sop_node`、`sop_advance` 两个工具（仅当前 conversation 有效）
2. system prompt 只注入：SOP 标题 + 根节点摘要 + "请从根节点开始"
3. 删除 `_process_sop_mode()` 独立路径，统一走 ReactEngine 主循环
4. SOP 和诊断工具在同一轮循环中可交替调用

#### 涉及文件

| 文件路径 | 操作 | 说明 |
|---------|------|------|
| `backend/agent-service/app/adapters/agents/htp/react_engine.py` | 修改 | 支持动态工具注入 |
| `backend/agent-service/app/adapters/agents/htp/investigation_agent.py` | 修改 | SOP 命中时注入工具，删除 `_process_sop_mode()` |
| `backend/agent-service/app/adapters/agents/htp/tool_registry.py` | 修改 | 支持 session 级工具注册 |

#### 依赖关系

- **前置任务**：T-AGT-20、T-AGT-21、T-AGT-11（InvestigationAgent 完善）
- **后续任务**：T-AGT-23（中断恢复）

#### 实现步骤

**Step 1：ReactEngine 支持动态工具注入**

```python
class ReactEngine:
    def __init__(self, tool_registry: ToolRegistry, ...):
        self._tool_registry = tool_registry

    async def execute(self, context, extra_tools: list[Tool] | None = None):
        # extra_tools 仅本次 execute() 调用期间有效
        active_tools = self._tool_registry.all_tools() + (extra_tools or [])
        ...
```

**Step 2：InvestigationAgent SOP 命中分支**

```python
# SOP 命中后：
sop_tools = [
    SopGetNodeTool(sop_document_id=doc_id, kb_client=self._kb_client),
    SopAdvanceTool(conversation_id=conversation_id, sop_execution_repo=self._sop_repo),
]
# 注入 SOP 工具，统一走 ReactEngine
async for event in self._react_engine.execute(context, extra_tools=sop_tools):
    yield event
```

**Step 3：删除 `_process_sop_mode()`**

确认功能由 ReactEngine 承接后，删除该独立路径。

#### 验收标准

| # | 验收项 | 通过标准 | 状态 |
|---|--------|---------|------|
| 1 | 统一循环 | SOP 命中后 LLM 可在同一轮中调用诊断工具和 SOP 导航工具 | ✅ 已实现 |
| 2 | 节点推进 | LLM 能调用 `sop_advance`，数据库 `current_node_id` 更新 | ✅ 已实现 |
| 3 | 非 SOP 不影响 | 无 SOP 命中时，ReactEngine 行为与之前完全一致 | ✅ 已实现 |
| 4 | 无 `_process_sop_mode` | 代码中不再有该方法 | ⚠️ 改为保留 `_process_sop_mode` 主方法 + `_process_sop_mode_legacy` 降级路径 |
| 5 | 端对端测试 | 完整 SOP 执行流程：根节点 → 诊断 → 子节点 → solution | ⏳ 待验证 |

**T-AGT-22 实现说明**：
- `_process_sop_mode()` 已重构为 ReactEngine + SopToolExecutor 路径
- `_process_sop_mode_legacy()` 作为降级路径保留（当 conversation_service_url 未配置时）
- `conversation-service` 新增 `/create` 接口创建 SopExecution 记录
- `SopToolExecutor` 统一执行诊断工具和 SOP 导航工具，注入必要上下文

---

### T-AGT-23：中断恢复流程

#### 背景与问题

```
【背景】
用户关闭页面后重新打开同一工单，SOP 执行应从上次位置恢复，
而非从头开始。sop_execution 表记录了恢复所需的全部状态。
```

#### 任务目标

1. `conversation-service` 加载会话历史时检测 `sop_execution.status=active`
2. 构建恢复版 system_prompt（含已完成步骤摘要 + 当前节点位置）
3. `completed_steps` 防止写操作节点被重复执行

#### 涉及文件

| 文件路径 | 操作 | 说明 |
|---------|------|------|
| `backend/conversation-service/app/services/conversation_service.py` | 修改 | 检测并构建恢复 prompt |
| `backend/conversation-service/app/repositories/sop_execution_repository.py` | 修改 | `get_active_by_conversation()` 方法 |

#### 依赖关系

- **前置任务**：T-AGT-22（统一 ReactEngine）
- **后续任务**：无

#### 恢复 system_prompt 格式

```
[身份+方法论]（固定段）

[SOP 恢复说明]
正在执行 SOP：《VM 启动失败排障》
已完成步骤 3 步，当前位置：存储 I/O 故障 → 磁盘检查
已知变量：vm_name=prod-vm-001, disk_id=disk-004

[当前节点窗口]（current_node_id 对应节点 + 子节点）
```

#### 验收标准

| # | 验收项 | 通过标准 | 状态 |
|---|--------|---------|------|
| 1 | 断线重连 | 关闭页面后重新打开，SOP 从 `current_node_id` 处继续 | ✅ 已实现 |
| 2 | 幂等性 | 已在 `completed_steps` 的节点不重新执行写操作 | ✅ 已实现 |
| 3 | 无恢复时无影响 | `sop_execution` 为空或 `status!=active` 时，流程不变 | ✅ 已实现 |

**T-AGT-23 实现说明**：
- `conversation-service` 在 `send_message_stream_only()` 中检测 `sop_execution.status=active`
- 构建恢复上下文 `sop_resume_context`（含 `current_node_id`、`completed_steps`、`context_variables`）
- 通过 `AgentClient.stream()` → `AgentRouter.process()` → `InvestigationAgent.process()` 传递恢复上下文
- `_process_sop_mode()` 检测 `sop_resume_context`，恢复场景下：
  - 跳过创建 `SopExecution`（使用已有记录）
  - 构建恢复版 `system_prompt`（含已完成步骤摘要 + 当前节点位置）
  - `SopToolExecutor` 传递 `completed_steps`，`sop_advance()` 执行幂等性检查
