---
status: active
category: task
audience: developer, qa, sre
last_updated: 2026-08-10
owner: team
---

# hci-sim 三 PR 闭环任务基线

## PR1：基础运行面（当前 PR #717）

- [x] 修复 ArgoCD PreSync 依赖顺序并补 Helm 回归测试。
- [ ] 合入后同步 dev，完成数据库/schema/table/role 验收。
- [ ] 将 hci-sim chart 纳入 dev GitOps，创建 `hci-sim-dev` namespace 和受管 Deployment/Service/Secret/NetworkPolicy。
- [ ] Runtime 使用独立数据库配置并 fail closed；禁止回退平台 `DATABASE_URL`。
- [ ] 完成 terminal_bridge → hci-sim SSH/exec-result/trace E2E。
- [ ] 更新 A/B 真实验证报告和部署避坑条目。

> 数据库迁移脚本和 Runtime URL fail-closed 边界已完成代码与现场等价 Job 验证；因为 PR2 尚未合入，正式 Argo Hook 仍需在合入后重新执行并保存 Application revision/Job 原始日志。

> 交付状态（2026-08-10）：PR #718 的 CI 已全绿并可合入。合入后不再创建数据库/CI 单点 PR；剩余全部能力（受管 hci-sim Deployment、持久控制面、TestRun/Runner、23821、Admin UI 和 E 级证据）统一放入最后一个纵向 PR #3。

## PR2：控制面和产品入口

- [ ] PostgreSQL Repository、CAS、Outbox、reconciler 和权限测试。
- [ ] Fixture Compiler/Artifact Gate/Bundle Registry API 和状态/日志查询。
- [ ] TestRun、Attempt、Lease、Runner、Agent environment context 显式绑定。
- [ ] 23821 approved Artifact、immutable Bundle 和 Runtime compatibility。
- [ ] Admin UI“仿真测试”页面；customer-ui 删除“仿真租约”；构建日志和按钮状态闭环。
- [ ] 只保留两步用户验收路径并补前后端契约测试。

## PR3：产品级验证和运营

- [ ] 27123、23821、至少一个异构 KBD 的认证包。
- [ ] positive/negative/near-miss/unknown/timeout/permission、differential、mutation、20-repeat。
- [ ] 1/10/50/100/200 容量梯度，100 场景零串线，200 场景受控降级。
- [ ] PR affected suite、nightly、周期校准、Evidence Manifest、dashboard、告警、SLO、waiver、runbook。
- [ ] 更新 A～E 需求/设计/任务/验证文档为实际证据状态，完成 Go/No-Go 签署。

## 合并门禁

PR1 未通过真实 Bridge→Runtime，PR2 不得宣称 TestRun 可用；PR2 未产出三个 published Bundle，PR3 不得进行产品级容量外推。任何未完成项必须保留 `pending`/`capability_gap`，不得以勾选替代证据。
