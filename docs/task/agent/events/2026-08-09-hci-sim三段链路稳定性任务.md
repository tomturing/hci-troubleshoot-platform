---
status: in_progress
category: task
audience: developer, tester, release
last_updated: 2026-08-09
owner: team
---

# hci-sim 三段链路稳定性任务

## 实施任务

- [x] 修复 acceptance 脚本固定 `27736` capability 探针，改为请求当前 KBD。
- [x] 同 KBD 旧实例处理、SSH/HTTP 端口预检和 `readyz` fail-closed。
- [x] 默认端口冲突时自动选择隔离端口，并将最终端口写入 connection.json。
- [x] dev host key 持久化；bootstrap/connection 增加 Lease 时间边界。
- [x] 脚本停止打印完整 connection JSON 和敏感 Lease。
- [x] 状态 A 增加“仿真租约连接（dev）”直接入口。
- [x] 连接弹窗支持粘贴 connection.json，校验 Lease 类型/有效期并自动填充推荐命令。
- [x] 仿真租约切换自动填充 hci-sim 默认 host/端口/用户名；失败重试保留本轮表单输入。
- [x] 增加 `hci-sim-smoke` 通用 connection JSON WebSocket 验收模式。
- [x] Linux 构建并启动 terminal_bridge，Customer UI runtime-config 临时指向 Linux Bridge，完成真实链路验证。
- [x] 补充 Windows 桌面 Bridge 与 Linux Bridge 的拓扑边界和配置说明。
- [x] 修复 acceptance 脚本对 `rg` 的运行时依赖，使用 Bash glob 识别旧容器。
- [x] 增加 Customer UI Helm 显式 `customerUI.terminalBridgeUrl` 配置及模板回归测试。
- [ ] 将 Linux Bridge enabled/runtime URL 写入受管 dev 环境仓库并由 Argo 同步，消除手工 Pod patch。
- [ ] 将 acceptance 脚本和 Linux Bridge smoke 接入可选的 dev workflow；PR 默认不启动真实环境。

## 不在本任务范围

- 不把 synthetic fixture 扩展为真实 Artifact。
- 不启用生产环境 terminal_bridge Pod。
- 不改变 Windows desktop Bridge 的普通密码/密钥认证行为。
