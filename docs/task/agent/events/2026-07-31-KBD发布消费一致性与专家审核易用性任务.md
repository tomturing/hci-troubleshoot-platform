---
status: completed
category: task
audience: developer, tester
last_updated: 2026-07-31
owner: team
---

# KBD 发布消费一致性与专家审核易用性任务

## 对应方案

[KBD 发布消费一致性与专家审核易用性方案](../../../solution/agent/events/2026-07-31-KBD发布消费一致性与专家审核易用性方案.md)

## 实施清单

| ID | 任务 | 状态 |
|---|---|---|
| KRC-01 | Signal v2 增加独立 `publish_validation`，Schema 生成器与漂移门禁同步 | ✅ |
| KRC-02 | 初次发布、重发布、maintenance 发布统一使用当前契约校验并盖章 | ✅ |
| KRC-03 | KB inventory 与 Agent compiler 优先消费发布盖章，保留原 generation revision | ✅ |
| KRC-04 | 发布前增加 diagnostic 变量 DAG、未声明输入和循环依赖门禁 | ✅ |
| KRC-05 | 旧 role/Contract 冲突在专家读取边界按已有运行 Contract 规范化 | ✅ |
| KRC-06 | QKV 显式 produces 的 END 统一转 HCI 绝对时间 | ✅ |
| KRC-07 | task/alert/dialog 同名锚点使用确定性优先级，不依赖执行顺序 | ✅ |
| KRC-08 | 增强证据 `minimum_should` 提供专家编辑与保存入口 | ✅ |
| KRC-09 | Evidence 默认折叠；role/matcher/期望结果补中文括号说明 | ✅ |
| KRC-10 | qfk_log 主字段收敛，日志族/路径/parser/.gz 进入高级设置 | ✅ |
| KRC-11 | threshold 前端保存按类型校验；标准 df 输出读取 Use% | ✅ |
| KRC-12 | 方案、任务、验证与冷启动入口归档 | ✅ |

## 部署后人工验收

1. KBD30880 创建 maintenance 并显式发布，确认 `signals_json.publish_validation` 存在；
2. 查询分类 playbooks，确认 KBD30880 不再只因原 Proposal revision 旧而 `executable=false`；
3. 确认 Proposal/旧 Expert Revision 的 `generation_metadata.tool_contract_revision` 未被覆盖；
4. 打开 KBD40061，确认旧 sig_002/sig_003 仍按旧 Contract 显示 should，而不是全 must；
5. 将 qkv_alert、qkv_task 都设为增强证据，规则设为至少 1 条并保存；
6. 检查 qfk_log 主界面只显示常用字段，高级定位和来源证据默认关闭；
7. 用标准 df 输出验证 54 与 80 比较，不能取到表头 1；
8. 在真实 Agent 会话验证 task 有结果时 `HOST/END` 来源优先于 alert。

## 未完成边界

- Embedding 失败行为未修改；
- qfk_system host/容器边界未修改；
- 真实客户环境 replay 未完成；
- 122 条 Proposal 未正式审核；
- 0/126 Expert Gold；
- Admin SSO、reason_code、评估导出和 Champion/Challenger 未完成。
