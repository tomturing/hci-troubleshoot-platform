---
status: completed_code_level
category: task
audience: developer, tester
last_updated: 2026-07-31
owner: team
---

# QFK 系统执行域与统一文本取值任务

## 对应方案

[QFK 系统执行域与统一文本取值方案](../../../solution/agent/events/2026-07-31-QFK系统执行域与统一文本取值方案.md)

## 实施清单

| ID | 工作项 | 状态 |
|---|---|---|
| QST-01 | 将 qfk_system `container` 收敛为 aCLI `--container` 枚举，新增 `cluster`、`formatter`、`command_args` | ✅ |
| QST-02 | SystemHandler 编译 aCLI 全局参数；Engine/Executor 不再把该字段传给 Bridge | ✅ |
| QST-03 | 旧 `container=host` 读取归一为省略 `--container`，不改变 HOST-OS 语义 | ✅ |
| QST-04 | 修正 aCLI Validator 对 `--cluster` 的 boolean 解析 | ✅ |
| QST-05 | 抽取唯一 `ExtractionResult` / `extract_output_values`，供 Produces 与 Matcher 共同使用 | ✅ |
| QST-06 | `match.extract` 引用同一 Schema；完整输出、output filter、requires 都覆盖它 | ✅ |
| QST-07 | Admin 复用 `TextExtractEditor`，并补充 aCLI 执行域/参数/集群的中文说明 | ✅ |
| QST-08 | 生成 Schema、补齐 Python/Schema/前端构建验证与事件归档 | ✅ |

## 后续人工验收

1. 在 Admin 编辑 KBD40061，设置 `df`、`/sf/log`、第 5 列、数值、全部行、最大值、`>80`；
2. 保存后查看最终 KBD JSON，确认 `match.extract` 和自动推导的 `requires` 一致；
3. 真实 HCI 运行，确认 Bridge 记录的是 HOST-OS 上完整的 `acli --container …`，不是容器内 acli；
4. 用 54%、80%、83% 和两条挂载行分别验证 false、边界 false、true、max 聚合；
5. 回归旧 `qfk_system.container=host` 的已发布 KBD，确认不被保存/运行阻断。
