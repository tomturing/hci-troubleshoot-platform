---
status: active
category: task
audience: developer
last_updated: 2026-08-20
owner: team
---

# 仿真 KBD 权威候选绑定与 CDD 调度任务

## 目标

修复 `sim-ssh` TestRun 已绑定 KBD 时分类内其他 KBD 被错误加入 CDD 的问题，并同步纠正 KBD 调度文档。

## 实施清单

- [x] 在 `InvestigationAgent` 增加 `support_id + resource_revision` 唯一绑定。
- [x] 仿真绑定缺失、失配、重复时 fail-closed，输出人工升级事件。
- [x] 仿真路径禁止 SOP 恢复上下文绕过 KBD authority。
- [x] 增加结构化日志和 `agent_sim_kbd_authority_binding_total` 指标。
- [x] 增加真实环境全量候选、仿真唯一候选、失配不回退单元测试。
- [x] 更新 Agent/KBD/C DD 现行文档、历史草案勘误和验证避坑索引。

## 验收标准

1. TestRun `support_id=27123,kbd_revision=25` 只编译 KBD27123。
2. KBD27123 三个信号全部 `SATISFIED` 时结果为 `SUPPORTED`，且唯一候选时 `DEFINITIVE`。
3. 不会因 KBD30880 缺 fixture 的 `ERROR` 产生 `PARTIAL`。
4. 真实环境仍保留分类内完整可执行 KBD 集合和 CDD 排序。

