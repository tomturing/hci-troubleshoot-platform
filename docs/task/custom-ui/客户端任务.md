---
status: active
category: task
audience: developer
last_updated: 2026-04-23
owner: team
update_trigger: 客户端功能新增/重构/问题修复任务
---

# 任务：客户端

> 对应方案文档：[../../solution/custom-ui/客户端设计.md](../../solution/custom-ui/客户端设计.md)

## 变更历史

| 日期 | 版本 | 变更内容 | 关联事件文档 |
|------|------|---------|------------|
| 2026-04-23 | v1.2 | SSH UX 全场景重构任务：CaseCreateDialog 视图状态机（6视图）、前置 Bridge 检测、TerminalPanel 真实三态、输入框触发弹框、状态刷新修复 | [SSH-UX全场景重构任务.md](events/2026-04-23-SSH-UX全场景重构任务.md) |
| 2026-04-23 | v1.1 | SSH 集成体验重构任务：SshFlowPanel/SshConnectDialog 新建、CaseCreateDialog/TerminalPanel 精简、upsert 机制、protocol helpers 迁移 | [PR #203](https://github.com/tomturing/hci-troubleshoot-platform/pull/203) |
| 2026-04-05 | v1.0 | 初版 | — |

---

## 当前任务清单

| 状态 | 任务 | 创建日期 | 关联方案 |
|------|------|---------|---------|
| 🔲 未开始 | T-FE-1: `checkBridgeBeforeOpen()` 前置检测函数提取（供 CaseCreateDialog 和 SshConnectDialog 共用） | 2026-04-23 | [SSH-UX全场景重构方案](../../solution/custom-ui/events/2026-04-23-SSH-UX全场景重构方案.md) |
| 🔲 未开始 | T-FE-2: `CaseCreateDialog.vue` 重构为含 6 视图状态机的完整组件（内嵌 SSH 表单和进度展示） | 2026-04-23 | 同上 |
| 🔲 未开始 | T-FE-3: `TerminalPanel.vue` + `chat.ts` 三态按钮（无工单/有工单无SSH/已连接）实现 | 2026-04-23 | 同上 |
| 🔲 未开始 | T-FE-4: `SshConnectDialog.vue` 补充 `bridgeStatus` prop + Bridge 引导视图 | 2026-04-23 | 同上 |
| 🔲 未开始 | T-FE-5: `ChatInput.vue` 无工单时自动触发 Bridge 检测 + 弹框流程 | 2026-04-23 | 同上 |
| 🔲 未开始 | T-FE-6: 弹框关闭后调用 `fetchEnvironments(caseId)` 修复状态不刷新问题 | 2026-04-23 | 同上 |
| ✅ 已完成 | T2: 新建 `SshFlowPanel.vue`（统一 SSH 连接流程，`create-case` / `terminal-only` 双模式） | 2026-04-23 | [SSH集成创建工单方案 v2](../../solution/custom-ui/events/2026-04-21-SSH集成创建工单方案.md) |
| ✅ 已完成 | T3: 新建 `SshConnectDialog.vue`（弹框薄包装层） | 2026-04-23 | 同上 |
| ✅ 已完成 | T4: `CaseCreateDialog.vue` 精简（795行→195行，移除内嵌 SSH 状态和表单） | 2026-04-23 | 同上 |
| ✅ 已完成 | T5: `TerminalPanel.vue` 移除 SSH 登录表单，改为"连接 SSH"按钮 | 2026-04-23 | 同上 |
| ✅ 已完成 | T6: `App.vue` 挂载 `SshConnectDialog` | 2026-04-23 | 同上 |
| ✅ 已完成 | T-terminal-ts: protocol helpers（buildBridgeMarker 等）从 store 迁移至 `api/terminal.ts` | 2026-04-23 | 同上 |
| ✅ 已完成 | T1: store `submitCollectedData` 全面改造（stripAnsi/parseJsonOutput 重写、全量日志、upsert 替换） | 2026-04-23 | 同上 |
| ✅ 已完成 | T-SDK: shared `environmentApi.upsert()` 方法，调用 `PUT /environments/case/{caseId}/type/{envType}` | 2026-04-23 | 同上 |
| 🔲 未开始 | 前端日志面板优化：折叠/展开动画、关键错误高亮 | — | — |
| 🔲 未开始 | SshFlowPanel 错误恢复：Bridge 断线后自动重连 | — | — |

---

## 验收标准（PR #203）

### T2 SshFlowPanel 验收
- [ ] `create-case` 模式：完成采集后调用 `completeCaseCreationFlow()`，工单创建成功
- [ ] `terminal-only` 模式：完成连接后调用 `openTerminalSidebar()`，终端可用
- [ ] 日志面板默认折叠，点击可展开
- [ ] "无 SSH 创建"按钮仅在 `create-case` 模式显示
- [ ] Bridge 未运行时显示下载说明

### T4 CaseCreateDialog 验收
- [ ] 表单包含：标题、描述（可选）、助手选择
- [ ] "连接 SSH 并创建工单"按钮触发 SshFlowPanel
- [ ] "无 SSH 创建工单"带 tooltip 告警（提示环境数据不完整）
- [ ] 取消按钮正常关闭

### T5 TerminalPanel 验收
- [ ] 未连接时显示"连接 SSH"按钮
- [ ] 点击按钮触发 `checkAndOpenTerminal()` 三态决策
- [ ] 已连接时 xterm 终端正常显示

### T-SDK upsert 验收
- [ ] 相同 `(case_id, env_type)` 重复采集时不产生重复数据库记录
- [ ] 后端日志包含 `created=True/False` 标记

---

## BUG 修复记录（2026-04-25）

| 修复点 | 文件 | 说明 |
|--------|------|------|
| **终端回放导航失效** | `TerminalReplay.vue` | 原 `nextOperation/prevOperation` 按 `seq_number+1` 递增，`seq_number` 不保证从 1 开始连续，导致 `find()` 找不到记录，进度显示 0/0；新增 `currentIndex`（0-based 数组索引）替代 seq_number 算术导航，`progressPercent`、按钮禁用逻辑、进度文字均改用 `currentIndex` |
## T-E7：ops-agent 交互卡片 UI（2026-05-08）

| 任务 | 文件 | 说明 | 状态 |
|------|------|------|------|
| T-E7a | InteractiveRequestCard.vue | 新增组件：sop_step 分支（路径/目标/指引/预期结果/风险提示）+ info_request 分支（问题/上下文），选项按钝 + 自由文本双模式；doSubmit 不再在 finally 无条件关闭，改为仅 HTTP ok 时关闭 | ✅ 完成 |
| T-E7b | chat.ts | 新增 `pendingInteractive` state 及类型（`metadata: Record<string, unknown>`）；SSE `interactive_request` 分支；`clearInteractiveRequest`；`handleCloseCase`/`startNewConversation`/`switchToCase` 均同步清除 | ✅ 完成 |

## T-E8：交互选项持久化显示（2026-05-10）

| 任务 | 文件 | 说明 | 状态 |
|------|------|------|------|
| T-E8a | InteractiveOptions.vue | 新增共用组件，props: `options`/`selectedOptionId`/`submitting`；emit: `select`；已选项 `primary` 蓝色，其余 `opacity:0.45` 置灰 | ✅ 完成 |
| T-E8b | MessageBubble.vue | 新增 `selectedInteractiveOptionId` computed（从后续消息 metadata 读取，支持刷新恢复）；`handleInteractiveOption` 将 `selectedOptionId` 存入 metadata；选项区改用 `InteractiveOptions` 组件，删除 "✓ 已提交选择" 占位 | ✅ 完成 |
| T-E8c | InteractiveRequestCard.vue | 使用共用 `InteractiveOptions` 组件；`doSubmit` 改为 `Promise<boolean>`；提交成功后保持弹框，设置 `selectedOptionId` 展示已选状态 | ✅ 完成 |
| T-E8d | InteractiveOptions.vue, MessageBubble.vue | 修复 4+1 模式：`forceDisabled` prop 屏蔽自由文本提交后选项未置灰的问题；`MessageBubble` 传入 `:force-disabled="interactiveSubmitted"` | ✅ 完成 |
| T-E7c | ChatWindow.vue | 挂载 `InteractiveRequestCard`（v-if + @submitted） | ✅ 完成 |